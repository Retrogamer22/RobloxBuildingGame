diff --git a/public/src/Client/DailyRewardsUI.client.luau b/public/src/Client/DailyRewardsUI.client.luau
index 0dd9733..9304387 100644
--- a/public/src/Client/DailyRewardsUI.client.luau
+++ b/public/src/Client/DailyRewardsUI.client.luau
@@ -1381,6 +1381,11 @@ end
 local DEV_MODE = RunService:IsStudio()
 local DevStreak = 0 -- Continuous streak counter for dev testing (keeps growing past 7)
 local DevRng = Random.new()
+-- Dev preview: cycle through reward sets deterministically instead of rolling random,
+-- so you can step through each set's burst/look on demand. Each set-hit claim advances
+-- to the next set in RewardSetPicker.GetAllSets() order. Studio-only.
+local DevForceSetCycle = true
+local DevSetIndex = 0
 
 local function onClaimClicked()
 	if IsClaiming then return end
@@ -1398,15 +1403,26 @@ local function onClaimClicked()
 		-- Roll loot table (week-aware set chance)
 		local setChance = LootConfig.GetSetChance(claimedDay, claimedWeek)
 		local roll = DevRng:NextNumber()
-		local gotSet = roll < setChance
+		-- When previewing via the set-cycle, force every claim to land on a set so you
+		-- don't have to keep claiming through coin rolls to see each burst.
+		local gotSet = DevForceSetCycle or (roll < setChance)
 		local rewardBlock = nil
 
 		if not gotSet then
 			print(string.format("[BB-DR] LOOT ROLL: COINS (%d) | W%d D%d setChance=%.0f%% | roll=%.2f",
 				coinReward, claimedWeek, claimedDay, setChance * 100, roll))
 		else
-			-- Dev mode: pick from RewardSets folder with no ownership filter
-			rewardBlock = RewardSetPicker.PickRandom(DevRng, nil)
+			-- Dev mode: either cycle through sets deterministically (DevForceSetCycle)
+			-- so you can preview each one in turn, or fall back to a random weighted pick.
+			if DevForceSetCycle then
+				local allSets = RewardSetPicker.GetAllSets()
+				if #allSets > 0 then
+					DevSetIndex = (DevSetIndex % #allSets) + 1
+					rewardBlock = allSets[DevSetIndex]
+				end
+			else
+				rewardBlock = RewardSetPicker.PickRandom(DevRng, nil)
+			end
 			if rewardBlock then
 				print(string.format("[BB-DR] LOOT ROLL: SET! %s (%s) | grants=%d | W%d D%d setChance=%.0f%% | roll=%.2f",
 					rewardBlock.name, rewardBlock.rarity, #rewardBlock.grants, claimedWeek, claimedDay, setChance * 100, roll))
diff --git a/public/src/Client/InventoryController.client.luau b/public/src/Client/InventoryController.client.luau
index a6ecc85..dbd449c 100644
--- a/public/src/Client/InventoryController.client.luau
+++ b/public/src/Client/InventoryController.client.luau
@@ -13,6 +13,7 @@ local Lighting = game:GetService("Lighting")
 
 local BlockPresets = require(ReplicatedStorage:WaitForChild("BlockPresets", 10))
 local UITheme = require(ReplicatedStorage:WaitForChild("Config"):WaitForChild("UITheme"))
+local BetaConfig = require(ReplicatedStorage:WaitForChild("Config"):WaitForChild("BetaConfig"))
 local BlockTemplates = ReplicatedStorage:WaitForChild("BlockTemplates", 10)
 local BlocksFolder = ReplicatedStorage:WaitForChild("Blocks", 10)
 local DecorationsFolder = ReplicatedStorage:WaitForChild("Decorations", 10)
@@ -232,6 +233,58 @@ local MarkTabSeenRemote = nil -- RemoteEvent populated on init
 local DEBUG_NEW_BLOCKS = false
 
 -- Map new block IDs to their categories/tabs so we know which tabs need "!" badges
+
+-- Single source of truth: is this preset actually visible in the inventory grid
+-- for THIS player right now? Mirrors the grid's own gating (HideFromInventory,
+-- IsRainbow unlock, IsRewardBlock unlock) so the "!" badge system and the
+-- mark-as-seen RPC never disagree with what the player can actually see.
+--
+-- Owner bypass uses BetaConfig.OWNER_USER_ID (NOT game.CreatorId) ΓÇö this is a
+-- group-owned game, so game.CreatorId returns the group id, not the user id.
+local function getOwnerUserId()
+	if BetaConfig.OWNER_USER_ID and BetaConfig.OWNER_USER_ID ~= 0 then
+		return BetaConfig.OWNER_USER_ID
+	end
+	return game.CreatorId
+end
+
+local function isPresetVisible(preset)
+	if preset.HideFromInventory then return false end
+	local isOwner = (LocalPlayer.UserId == getOwnerUserId())
+	-- Owner preview mode: pretend we're a fresh player so we can see what
+	-- the inventory looks like before unlocks. Toggled via the /preview chat
+	-- command (server-side BetaAdminCommands handler).
+	local previewingAsFresh = isOwner and (LocalPlayer:GetAttribute("PreviewAsFresh") == true)
+	local effectivelyOwner = isOwner and not previewingAsFresh
+	if preset.IsRainbow and not effectivelyOwner then
+		if not LocalPlayer:GetAttribute("RainbowBlocksUnlocked") then return false end
+	end
+	if preset.IsRewardBlock and not effectivelyOwner then
+		-- Variants of the same base block (CandyCaneLog_Red, CandyCaneLog_Blue, ...)
+		-- share an unlock under the BaseBlock name. Unlocking CandyCaneLog reveals
+		-- ALL 17 colored variants. Non-variant rewards check by their own Id.
+		local unlockKey = "Unlocked_" .. (preset.BaseBlock or preset.Id)
+		if not LocalPlayer:GetAttribute(unlockKey) then return false end
+	end
+	return true
+end
+
+-- Should a themed-pack tab (e.g. "Candy") be visible to this player right now?
+-- Visible if the player has unlocked at least one block from any set that maps
+-- to this tab ΓÇö OR if they're the owner (and not in /preview mode).
+local function isTabVisible(tabName)
+	local isOwner = (LocalPlayer.UserId == getOwnerUserId())
+	local previewingAsFresh = isOwner and (LocalPlayer:GetAttribute("PreviewAsFresh") == true)
+	if isOwner and not previewingAsFresh then return true end
+	-- Non-owner (or owner-in-preview): visible only if at least one block in the
+	-- tab is unlocked. Reuses isPresetVisible so the logic stays in one place.
+	local presets = BlockPresets.GetPresetsByCategory(tabName)
+	for _, preset in ipairs(presets) do
+		if isPresetVisible(preset) then return true end
+	end
+	return false
+end
+
 local function computeNewCategoriesFromIds()
 	NewCategories = {}
 	NewRightTabs = {}
@@ -241,7 +294,7 @@ local function computeNewCategoriesFromIds()
 	-- Check each new block ID against all presets to find its category
 	local allPresets = BlockPresets.GetAllPresets()
 	for _, preset in ipairs(allPresets) do
-		if NewBlockIds[preset.Id] and not preset.HideFromInventory then
+		if NewBlockIds[preset.Id] and isPresetVisible(preset) then
 			local cat = preset.Category
 			if cat then
 				-- Frames is a right tab, not a left category
@@ -324,10 +377,12 @@ local function markTabBlocksSeen(categoryOrTabName)
 	local presets = BlockPresets.GetPresetsByCategory(categoryOrTabName)
 	if not presets or #presets == 0 then return end
 	
-	-- Only send IDs that are actually new (minimize network payload)
+	-- Only send IDs that are actually new AND visible to this player.
+	-- Locked reward/rainbow blocks stay "new" until they actually appear in
+	-- the grid for the first time, so the NEW badge fires when it should.
 	local idsToMark = {}
 	for _, preset in ipairs(presets) do
-		if NewBlockIds[preset.Id] then
+		if NewBlockIds[preset.Id] and isPresetVisible(preset) then
 			table.insert(idsToMark, preset.Id)
 		end
 	end
@@ -374,7 +429,7 @@ local function findTemplate(templateName)
 	return BlockTemplates:FindFirstChild(templateName)
 end
 
-local function setupViewportModel(viewport, templateName, color, material, materialVariant, useBlockModel, blockModelName, isDecoration, decorationName, blockModelFolder, applyColor, leatherTint, isRainbow)
+local function setupViewportModel(viewport, templateName, color, material, materialVariant, useBlockModel, blockModelName, isDecoration, decorationName, blockModelFolder, applyColor, leatherTint, isRainbow, stripeColorMode)
 	local worldModel = viewport:FindFirstChild("World")
 	if not worldModel then return nil end
 	
@@ -385,6 +440,13 @@ local function setupViewportModel(viewport, templateName, color, material, mater
 	local model
 	local isBlockModel = false
 	local shouldApplyColor = applyColor -- Whether to apply the preset color
+	-- Texture mode: skip Part/Model coloring; we'll tint Texture descendants below
+	-- instead, so the underlying Part stays its template color and the grayscale
+	-- texture pattern takes the variant color via Texture.Color3.
+	local isTextureMode = stripeColorMode == "Texture"
+	if isTextureMode then
+		shouldApplyColor = false  -- prevent the Part-tinting branches from running
+	end
 	
 	-- Use decoration from Decorations folder if specified
 	if isDecoration and decorationName and DecorationsFolder then
@@ -715,6 +777,7 @@ local function setupViewportModel(viewport, templateName, color, material, mater
 	end
 
 	local camera = viewport.CurrentCamera
+	local isCandySapling = blockModelName == "CandyCaneSapling"
 	if camera then
 		-- Check if this is a door (needs camera further back)
 		local isDoorModel = blockModelName and (string.find(blockModelName, "Door") ~= nil)
@@ -723,6 +786,9 @@ local function setupViewportModel(viewport, templateName, color, material, mater
 		if isToolModel then
 			-- Tools: look straight at the flat face (matches hotbar tool rendering)
 			camera.CFrame = CFrame.new(Vector3.new(0, 0, 5), Vector3.new(0, 0, 0))
+		elseif isCandySapling then
+			-- Candy sapling: straight-on (no 45 degrees) so the stripes read cleanly in the slot
+			camera.CFrame = CFrame.new(Vector3.new(0, 0, 6), Vector3.new(0, 0, 0))
 		elseif isDoorModel then
 			camera.CFrame = CFrame.new(Vector3.new(5.5, 5.5, 5.5), Vector3.new(0, 0, 0))
 		elseif isLadderModel then
@@ -734,6 +800,46 @@ local function setupViewportModel(viewport, templateName, color, material, mater
 		end
 	end
 	
+	-- Candy sapling inventory icon: tint Background parts red so it reads as a
+	-- classic red+white candy cane (the live placed sapling tints to its random
+	-- grow-color instead; this is just the inventory/hotbar default appearance).
+	if isCandySapling and model then
+		local red = Color3.fromRGB(240, 30, 50)
+		local function tintBg(inst)
+			if inst:IsA("BasePart") and inst.Name == "Background" then
+				inst.Color = red
+			elseif (inst:IsA("Texture") or inst:IsA("Decal")) and inst.Parent and inst.Parent.Name == "Background" then
+				inst.Color3 = red
+			elseif inst:IsA("SurfaceAppearance") and inst.Parent and inst.Parent.Name == "Background" then
+				inst.Color = red
+			end
+		end
+		if model:IsA("BasePart") then tintBg(model) end
+		for _, desc in ipairs(model:GetDescendants()) do tintBg(desc) end
+	end
+	
+	-- Texture-mode tinting: walk descendants for Textures/Decals (the stripes)
+	-- AND any Part named "Core" (the swirl on a candy-cane log's end caps).
+	-- Other Parts (e.g. "Bark") keep their template color so the background
+	-- between stripes stays white. Done after model creation so children are
+	-- visible to GetDescendants().
+	if isTextureMode and model and applyColor and color then
+		for _, desc in ipairs(model:GetDescendants()) do
+			if desc:IsA("Texture") or desc:IsA("Decal") then
+				desc.Color3 = color
+			elseif desc:IsA("BasePart") and desc.Name == "Core" then
+				desc.Color = color
+			end
+		end
+		if model:IsA("BasePart") then
+			for _, child in ipairs(model:GetChildren()) do
+				if child:IsA("Texture") or child:IsA("Decal") then
+					child.Color3 = color
+				end
+			end
+		end
+	end
+	
 	-- Tag rainbow viewports so ColorCycleController can animate them
 	-- Also immediately apply the current cycle color so there's no white flash
 	if isRainbow then
@@ -957,7 +1063,8 @@ local function updateHotbarSlotViewport(slotNumber)
 				preset.BlockModelFolder,
 				preset.ApplyColor,
 				preset.LeatherTint,
-				preset.IsRainbow
+				preset.IsRainbow,
+				preset.StripeColorMode
 			)
 			end
 		end
@@ -1187,7 +1294,8 @@ local function createHeldItemUI(presetId, count)
 			preset.BlockModelFolder,
 			preset.ApplyColor,
 			preset.LeatherTint,
-			preset.IsRainbow
+			preset.IsRainbow,
+			preset.StripeColorMode
 		)
 	end
 	
@@ -1873,7 +1981,8 @@ local function createPresetButton(preset, parent)
 			preset.BlockModelFolder,
 			preset.ApplyColor,
 			preset.LeatherTint,
-			preset.IsRainbow
+			preset.IsRainbow,
+			preset.StripeColorMode
 		)
 	end
 	
@@ -2130,7 +2239,8 @@ local function updateSurvivalSlotViewport(slotIndex)
 			preset.BlockModelFolder,
 			preset.ApplyColor,
 			preset.LeatherTint,
-			preset.IsRainbow
+			preset.IsRainbow,
+			preset.StripeColorMode
 		)
 	end
 end
@@ -2503,7 +2613,7 @@ local function refreshPresetDisplay()
 	end
 	
 	if SelectedRightTab == "Frames" then
-		-- Show frame block presets from the Frames category
+		-- Frames is a built-in right-tab. Show frame block presets from the Frames category.
 		local framePresets = BlockPresets.GetPresetsByCategory("Frames")
 		for _, preset in ipairs(framePresets) do
 			if not hasSearch or preset.Name:lower():find(searchLower, 1, true) then
@@ -2515,16 +2625,43 @@ local function refreshPresetDisplay()
 		return
 	end
 	
+	-- Themed-pack right-tabs (Candy, future Roman, etc). Same rendering as a normal
+	-- left category ΓÇö just routed through the right side. Locked blocks within the
+	-- tab are still filtered by isPresetVisible below.
+	if SelectedRightTab then
+		local themedTabs = BlockPresets.GetThemedPackTabs and BlockPresets.GetThemedPackTabs() or {}
+		for _, tab in ipairs(themedTabs) do
+			if tab.Name == SelectedRightTab then
+				local presets = BlockPresets.GetPresetsByCategory(SelectedRightTab)
+				for _, preset in ipairs(presets) do
+					if isPresetVisible(preset) and (not hasSearch or preset.Name:lower():find(searchLower, 1, true)) then
+						local btn = createPresetButton(preset, PresetsContainer)
+						btn.LayoutOrder = layoutOrder
+						layoutOrder = layoutOrder + 1
+					end
+				end
+				return
+			end
+		end
+	end
+	
 	-- Normal block preset display
 	-- Helper: check if a preset should be hidden (rainbow blocks require unlock)
-	local rainbowUnlocked = LocalPlayer:GetAttribute("RainbowBlocksUnlocked") or LocalPlayer.UserId == game.CreatorId
-	local isCreator = LocalPlayer.UserId == game.CreatorId
+	-- Owner check uses BetaConfig.OWNER_USER_ID (group-owned game; see top of file).
+	-- Owner in /preview mode is treated as a fresh player so we can test the
+	-- pre-unlock experience.
+	local isOwnerRaw = (LocalPlayer.UserId == getOwnerUserId())
+	local previewingAsFresh = isOwnerRaw and (LocalPlayer:GetAttribute("PreviewAsFresh") == true)
+	local isCreator = isOwnerRaw and not previewingAsFresh
+	local rainbowUnlocked = LocalPlayer:GetAttribute("RainbowBlocksUnlocked") or isCreator
 	
 	-- Helper: is this reward-gated preset unlocked for this player?
 	local function isRewardUnlocked(preset)
 		if not preset.IsRewardBlock then return true end
 		if isCreator then return true end
-		return LocalPlayer:GetAttribute("Unlocked_" .. preset.Id) == true
+		-- Variants share an unlock under BaseBlock; non-variants check by their own Id.
+		local unlockKey = "Unlocked_" .. (preset.BaseBlock or preset.Id)
+		return LocalPlayer:GetAttribute(unlockKey) == true
 	end
 	
 	if hasSearch then
@@ -3234,6 +3371,125 @@ local function createInventoryUI()
 		return btn
 	end
 	
+	-- Generic themed-pack tab button (Candy, future Roman, etc). The icon comes
+	-- from the set's TabIcon model in RewardSets/<SetName>/<IconBlockName>.
+	-- Mirrors createFramesTabButton's geometry/styling.
+	local function createThemedPackTabButton(tabInfo, parent)
+		local btnSize = IsMobile and 48 or 86
+		local totalHeight = IsMobile and 48 or 64
+		
+		local btn = Instance.new("TextButton")
+		btn.Name = tabInfo.Name
+		btn.Size = UDim2.new(0, btnSize, 0, totalHeight)
+		btn.BackgroundColor3 = Colors.Surface
+		btn.BorderSizePixel = 0
+		btn.Text = ""
+		btn.AutoButtonColor = false
+		btn.Parent = parent
+		
+		addBevel(btn)
+		
+		local viewportSize = IsMobile and 32 or 56
+		local viewport = createViewportPreview(btn, UDim2.new(0, viewportSize, 0, viewportSize))
+		if IsMobile then
+			viewport.Position = UDim2.new(0.5, -viewportSize/2, 0, 4)
+		else
+			viewport.Position = UDim2.new(1, -(viewportSize + 4), 0.5, -viewportSize/2)
+		end
+		
+		-- Locate the icon model: RewardSets/<SetName>/<TabIcon or first block>
+		local rewardSets = ReplicatedStorage:FindFirstChild("RewardSets")
+		local setFolder = rewardSets and rewardSets:FindFirstChild(tabInfo.SetName)
+		local iconTemplate = nil
+		if setFolder then
+			if tabInfo.IconBlockName then
+				iconTemplate = setFolder:FindFirstChild(tabInfo.IconBlockName)
+			end
+			-- Fallback: first model in the set folder (skip the Manifest ModuleScript)
+			if not iconTemplate then
+				for _, child in ipairs(setFolder:GetChildren()) do
+					if child:IsA("Model") or child:IsA("BasePart") then
+						iconTemplate = child
+						break
+					end
+				end
+			end
+		end
+		
+		if iconTemplate then
+			local worldModel = viewport:FindFirstChild("World")
+			if worldModel then
+				local model = iconTemplate:Clone()
+				if model:IsA("BasePart") then
+					model.Anchored = true
+					model.CFrame = CFrame.new(0, 0, 0)
+				elseif model:IsA("Model") then
+					for _, part in ipairs(model:GetDescendants()) do
+						if part:IsA("BasePart") then
+							part.Anchored = true
+						end
+					end
+					-- Leave the model wherever its pivot puts it; the camera below frames
+					-- to the model's real bounding-box center, so no pre-centering needed.
+				end
+				model.Parent = worldModel
+				
+				local camera = viewport.CurrentCamera
+				if camera then
+					-- Frame the camera to the model's actual size so any icon model
+					-- (CandyBricks now, future Roman columns, etc.) fills the viewport
+					-- uniformly instead of relying on a fixed distance tuned to a 3x3x3
+					-- block. Distance = boundingRadius / sin(FOV/2), times a small pad.
+					local radius = 2.6 -- fallback ~ half-diagonal of a 3-stud cube
+					local center = Vector3.new(0, 0, 0)
+					if model:IsA("Model") then
+						local okb, bbCF, bbSize = pcall(function() return model:GetBoundingBox() end)
+						if okb then
+							radius = bbSize.Magnitude / 2
+							center = bbCF.Position
+						end
+					elseif model:IsA("BasePart") then
+						radius = model.Size.Magnitude / 2
+						center = model.Position
+					end
+					local fov = math.rad(camera.FieldOfView)
+					local dist = (radius / math.sin(fov / 2)) * 1.15 -- 1.15 = breathing room
+					-- Same isometric view direction as before (down a (1, 0.8, 1) diagonal).
+					local dir = Vector3.new(1, 0.8, 1).Unit
+					camera.CFrame = CFrame.new(center + dir * dist, center)
+				end
+			end
+		end
+		
+		btn.MouseButton1Click:Connect(function()
+			showRightTab(tabInfo.Name)
+		end)
+		
+		btn.MouseEnter:Connect(function()
+			if SelectedRightTab ~= tabInfo.Name then
+				TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Colors.SurfaceHover}):Play()
+			end
+			if CategoryTooltip and not IsMobile then
+				CategoryTooltipText.Text = tabInfo.Name
+				local btnPos = btn.AbsolutePosition
+				local btnSizeAbs = btn.AbsoluteSize
+				CategoryTooltip.Position = UDim2.new(0, btnPos.X - CategoryTooltip.AbsoluteSize.X - 8, 0, btnPos.Y + btnSizeAbs.Y/2 - 12)
+				CategoryTooltip.Visible = true
+			end
+		end)
+		
+		btn.MouseLeave:Connect(function()
+			if SelectedRightTab ~= tabInfo.Name then
+				TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Colors.Surface}):Play()
+			end
+			if CategoryTooltip and not IsMobile then
+				CategoryTooltip.Visible = false
+			end
+		end)
+		
+		return btn
+	end
+	
 	local framesTabBtn = createFramesTabButton(rightTabsFrame)
 	framesTabBtn.LayoutOrder = 0
 	RightTabButtons["Frames"] = framesTabBtn
@@ -3246,6 +3502,40 @@ local function createInventoryUI()
 	toolsTabBtn.LayoutOrder = 1
 	RightTabButtons["Tools"] = toolsTabBtn
 	
+	-- Themed-pack tabs (Candy, future Roman, etc). Each tab is only shown if the
+	-- player has unlocked at least one block from the set (owner sees them all,
+	-- unless in /preview mode). The buttons are CREATED here but their .Visible
+	-- property is toggled at startup AND whenever a relevant attribute changes,
+	-- by refreshThemedPackTabVisibility() below.
+	local ThemedPackTabButtons = {}  -- { [tabName] = button }
+	local themedTabsLayoutStart = 2
+	local themedTabsList = BlockPresets.GetThemedPackTabs and BlockPresets.GetThemedPackTabs() or {}
+	for i, tabInfo in ipairs(themedTabsList) do
+		local btn = createThemedPackTabButton(tabInfo, rightTabsFrame)
+		btn.LayoutOrder = themedTabsLayoutStart + (i - 1)
+		RightTabButtons[tabInfo.Name] = btn
+		ThemedPackTabButtons[tabInfo.Name] = btn
+		if NewRightTabs[tabInfo.Name] then
+			addTabNotificationBadge(btn, "right")
+		end
+	end
+	
+	-- Apply initial visibility, plus a refresh on player attribute changes
+	local function refreshThemedPackTabVisibility()
+		for tabName, btn in pairs(ThemedPackTabButtons) do
+			local shouldShow = isTabVisible(tabName)
+			btn.Visible = shouldShow
+			-- If the player is currently looking at a tab that just became hidden
+			-- (e.g. they toggled /preview on), bounce them back to a safe default.
+			if not shouldShow and SelectedRightTab == tabName then
+				SelectedCategory = "Building"
+				SelectedRightTab = nil
+				refreshPresetDisplay()
+			end
+		end
+	end
+	refreshThemedPackTabVisibility()
+	
 	-- Listen for Rainbow Blocks unlock during session (daily reward claim, etc.)
 	-- When unlocked, refresh the display so rainbow blocks appear in Colors tab
 	LocalPlayer.AttributeChanged:Connect(function(attrName)
@@ -3257,6 +3547,13 @@ local function createInventoryUI()
 		-- Reward-set block unlocks (Unlocked_<BlockId>) - refresh inventory so the new block appears
 		if string.sub(attrName, 1, 9) == "Unlocked_" then
 			refreshPresetDisplay()
+			-- A new unlock may make a previously-hidden themed-pack tab appear.
+			refreshThemedPackTabVisibility()
+		end
+		-- Owner preview toggle: re-evaluate the whole inventory's visibility.
+		if attrName == "PreviewAsFresh" then
+			refreshPresetDisplay()
+			refreshThemedPackTabVisibility()
 		end
 	end)
 	
diff --git a/public/src/Client/VineClimber.client.luau b/public/src/Client/VineClimber.client.luau
index 290e2fd..575a85f 100644
--- a/public/src/Client/VineClimber.client.luau
+++ b/public/src/Client/VineClimber.client.luau
@@ -99,7 +99,7 @@ end
 -- one of the four horizontal sides. "Free-hanging" vines (no wall behind)
 -- just slow the player a bit and don't start climbing. The vine itself is
 -- already CanCollide=false server-side, so the player can walk through.
-local DEBUG_VINES = true -- set to false to silence [BB-VINE] logs
+local DEBUG_VINES = false -- set to false to silence [BB-VINE] logs
 
 local function getVineModel(sensor)
 	-- Walk up the sensor's parents to find the node with IsVine attribute.
diff --git a/public/src/Server/BetaAdminCommands.server.luau b/public/src/Server/BetaAdminCommands.server.luau
index a870356..c611741 100644
--- a/public/src/Server/BetaAdminCommands.server.luau
+++ b/public/src/Server/BetaAdminCommands.server.luau
@@ -12,7 +12,10 @@
 	- /changelog - Force show the changelog popup (for testing)
 	- /feedback - View recent player feedback
 	- /clearfeedback - Clear all stored feedback
+	- /scanfloatinggrass - READ-ONLY diagnostic: log floating-grass candidates (deletes nothing)
+	- /clearfloatinggrass - Delete floating-grass debris (same test as scan); SAVE after
 	- /urgentupdate [message] - Force shutdown all servers with countdown
+	- /preview - Toggle owner inventory preview mode (see what a fresh player sees)
 ]]
 
 local Players = game:GetService("Players")
@@ -21,6 +24,8 @@ local ReplicatedStorage = game:GetService("ReplicatedStorage")
 local MessagingService = game:GetService("MessagingService")
 
 local BetaConfig = require(ReplicatedStorage:WaitForChild("Config"):WaitForChild("BetaConfig"))
+local GridConfig = require(ReplicatedStorage:WaitForChild("Config"):WaitForChild("GridConfig"))
+local BlockSpatialIndex = require(ReplicatedStorage:WaitForChild("BlockSpatialIndex", 10))
 
 -- Get version management functions
 local TriggerUpdate = ReplicatedStorage:WaitForChild("TriggerUpdate", 10)
@@ -371,6 +376,202 @@ local function handleChatCommand(player, message)
 			warn("[BetaAdmin] Feedback system not initialized")
 		end
 		
+	elseif lowerMessage == "/scanfloatinggrass" then
+		-- READ-ONLY diagnostic. Deletes nothing. For every grass block, logs its flags
+		-- plus a lazy-aware verdict on the cell directly below: is there a real block
+		-- there, SHOULD terrain be there (heightmap), and is that cell marked dug. This
+		-- lets us tell true floaters (dug pocket below) from grass resting on lazy
+		-- terrain that simply hasn't spawned yet.
+		local BLOCK_SIZE = GridConfig.BLOCK_SIZE
+		local TerrainGen
+		do
+			local ok, mod = pcall(function()
+				local ServerScriptService = game:GetService("ServerScriptService")
+				return require(ServerScriptService:WaitForChild("Modules", 10):WaitForChild("TerrainGenerator", 10))
+			end)
+			if ok then TerrainGen = mod end
+		end
+		local function isGrass(b)
+			local bt = b:GetAttribute("BlockType")
+			return bt == "GrassBlock" or bt == "SnowyGrassBlock"
+				or bt == "SnowGrassBlock" or bt == "TaigaGrassBlock"
+		end
+		local function plotNumberFor(buildArea)
+			if buildArea.Name == "MegaPlotBuildArea" then return 0 end
+			local prefix = GridConfig.BUILD_AREA_PREFIX
+			if prefix and string.sub(buildArea.Name, 1, #prefix) == prefix then
+				return tonumber(string.sub(buildArea.Name, #prefix + 1))
+			end
+			return nil
+		end
+		local totalGrass = 0
+		local floaterCount = 0
+		local areasScanned = 0
+		for _, child in ipairs(workspace:GetChildren()) do
+			local isBuildArea = child.Name == "MegaPlotBuildArea"
+				or (GridConfig.BUILD_AREA_PREFIX and string.sub(child.Name, 1, #GridConfig.BUILD_AREA_PREFIX) == GridConfig.BUILD_AREA_PREFIX)
+			if isBuildArea then
+				local blocksFolder = child:FindFirstChild("Blocks")
+				if blocksFolder and BlockSpatialIndex then
+					local index = BlockSpatialIndex.GetOrCreate(blocksFolder)
+					if index then
+						areasScanned += 1
+						local plotNum = plotNumberFor(child)
+						print(string.format("[BB-SCAN] ==== %s (plot=%s) ====", child.Name, tostring(plotNum)))
+						for _, b in ipairs(blocksFolder:GetChildren()) do
+							if isGrass(b) then
+								totalGrass += 1
+								local px = b:GetAttribute("BlockPosX")
+								local py = b:GetAttribute("BlockPosY")
+								local pz = b:GetAttribute("BlockPosZ")
+								if px and py and pz then
+									local belowPos = Vector3.new(px, py - BLOCK_SIZE, pz)
+									local blockBelow = index:HasBlockAt(belowPos)
+									local shouldHaveBelow = false
+									local dugBelow = false
+									if TerrainGen then
+										if TerrainGen.ShouldHaveTerrainAt then
+											local okS, sh = pcall(TerrainGen.ShouldHaveTerrainAt, belowPos, child)
+											if okS then shouldHaveBelow = sh and true or false end
+										end
+										if TerrainGen.IsPositionDug and plotNum then
+											local okD, dug = pcall(TerrainGen.IsPositionDug, belowPos, plotNum, child)
+											if okD then dugBelow = dug and true or false end
+										end
+									end
+									-- A true floater: nothing real below AND (terrain shouldn't be there OR it was dug)
+									local isFloater = (not blockBelow) and ((not shouldHaveBelow) or dugBelow)
+									if isFloater then floaterCount += 1 end
+									print(string.format("[BB-SCAN] %s pos=(%.1f,%.1f,%.1f) grown=%s terrain=%s lazy=%s placedBy=%s | below: block=%s shouldHave=%s dug=%s => FLOATER=%s",
+										b.Name, px, py, pz,
+										tostring(b:GetAttribute("GrownFromSpread")), tostring(b:GetAttribute("IsTerrainBlock")),
+										tostring(b:GetAttribute("IsLazyLoaded")), tostring(b:GetAttribute("PlacedBy")),
+										tostring(blockBelow), tostring(shouldHaveBelow), tostring(dugBelow), tostring(isFloater)))
+								end
+							end
+						end
+					end
+				end
+			end
+		end
+		print(string.format("[BB-SCAN] DONE: %d grass blocks across %d area(s), %d flagged as floaters (TerrainGen=%s). Nothing was deleted.",
+			totalGrass, areasScanned, floaterCount, tostring(TerrainGen ~= nil)))
+		game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", {
+			Text = string.format("[Scan] %d grass, %d look like floaters. See output (read-only, nothing deleted).", totalGrass, floaterCount),
+			Color = Color3.fromRGB(64, 156, 220),
+			Font = Enum.Font.SourceSansBold,
+		})
+		
+	elseif lowerMessage == "/clearfloatinggrass" then
+		-- Deletes floating grass debris using the SAME lazy-aware test that
+		-- /scanfloatinggrass validated: a block is removed only if there is no real
+		-- block directly below AND (terrain shouldn't be there per heightmap OR that
+		-- cell was dug out). Grass resting on real or not-yet-spawned lazy terrain is
+		-- never touched. Run /scanfloatinggrass first to see the count, then this,
+		-- then /scanfloatinggrass again to confirm 0. SAVE the plot to persist.
+		local BLOCK_SIZE = GridConfig.BLOCK_SIZE
+		local TerrainGen
+		do
+			local ok, mod = pcall(function()
+				local ServerScriptService = game:GetService("ServerScriptService")
+				return require(ServerScriptService:WaitForChild("Modules", 10):WaitForChild("TerrainGenerator", 10))
+			end)
+			if ok then TerrainGen = mod end
+		end
+		if not TerrainGen then
+			warn("[BetaAdmin] /clearfloatinggrass aborted: TerrainGenerator unavailable (need lazy-aware checks)")
+			game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", {
+				Text = "[Cleanup] Aborted: terrain module unavailable. Nothing deleted.",
+				Color = Color3.fromRGB(220, 80, 80),
+				Font = Enum.Font.SourceSansBold,
+			})
+			return
+		end
+		local function isGrass(b)
+			local bt = b:GetAttribute("BlockType")
+			return bt == "GrassBlock" or bt == "SnowyGrassBlock"
+				or bt == "SnowGrassBlock" or bt == "TaigaGrassBlock"
+		end
+		local function plotNumberFor(buildArea)
+			if buildArea.Name == "MegaPlotBuildArea" then return 0 end
+			local prefix = GridConfig.BUILD_AREA_PREFIX
+			if prefix and string.sub(buildArea.Name, 1, #prefix) == prefix then
+				return tonumber(string.sub(buildArea.Name, #prefix + 1))
+			end
+			return nil
+		end
+		local totalRemoved = 0
+		local areasScanned = 0
+		for _, child in ipairs(workspace:GetChildren()) do
+			local isBuildArea = child.Name == "MegaPlotBuildArea"
+				or (GridConfig.BUILD_AREA_PREFIX and string.sub(child.Name, 1, #GridConfig.BUILD_AREA_PREFIX) == GridConfig.BUILD_AREA_PREFIX)
+			if isBuildArea then
+				local blocksFolder = child:FindFirstChild("Blocks")
+				if blocksFolder and BlockSpatialIndex then
+					local index = BlockSpatialIndex.GetOrCreate(blocksFolder)
+					if index then
+						areasScanned += 1
+						local plotNum = plotNumberFor(child)
+						-- Collect first (don't mutate the folder mid-iteration).
+						local toRemove = {}
+						for _, b in ipairs(blocksFolder:GetChildren()) do
+							if isGrass(b) then
+								local px = b:GetAttribute("BlockPosX")
+								local py = b:GetAttribute("BlockPosY")
+								local pz = b:GetAttribute("BlockPosZ")
+								if px and py and pz then
+									local belowPos = Vector3.new(px, py - BLOCK_SIZE, pz)
+									local blockBelow = index:HasBlockAt(belowPos)
+									local shouldHaveBelow = false
+									local dugBelow = false
+									if TerrainGen.ShouldHaveTerrainAt then
+										local okS, sh = pcall(TerrainGen.ShouldHaveTerrainAt, belowPos, child)
+										if okS then shouldHaveBelow = sh and true or false end
+									end
+									if TerrainGen.IsPositionDug and plotNum then
+										local okD, dug = pcall(TerrainGen.IsPositionDug, belowPos, plotNum, child)
+										if okD then dugBelow = dug and true or false end
+									end
+									-- Identical signature to /scanfloatinggrass's FLOATER verdict.
+									local isFloater = (not blockBelow) and ((not shouldHaveBelow) or dugBelow)
+									if isFloater then
+										table.insert(toRemove, b)
+										print(string.format("[BB-SCAN] CLEAR removing %s pos=(%.1f,%.1f,%.1f)", b.Name, px, py, pz))
+									end
+								end
+							end
+						end
+						for _, b in ipairs(toRemove) do
+							b:Destroy()
+							totalRemoved += 1
+						end
+					end
+				end
+			end
+		end
+		print(string.format("[BB-SCAN] CLEAR DONE by %s: removed %d floating grass across %d area(s). SAVE the plot to persist.",
+			player.Name, totalRemoved, areasScanned))
+		game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", {
+			Text = string.format("[Cleanup] Removed %d floating grass block(s). SAVE the plot to make it permanent.", totalRemoved),
+			Color = Color3.fromRGB(64, 156, 220),
+			Font = Enum.Font.SourceSansBold,
+		})
+		
+	elseif lowerMessage == "/preview" then
+		-- Toggle owner preview mode: see the inventory exactly as a fresh player
+		-- would (locked reward blocks hidden, themed-pack tabs hidden until
+		-- something's unlocked). InventoryController watches the PreviewAsFresh
+		-- attribute and refreshes automatically.
+		local newValue = not (player:GetAttribute("PreviewAsFresh") == true)
+		player:SetAttribute("PreviewAsFresh", newValue)
+		local stateText = newValue and "ON (viewing as fresh player)" or "OFF (full owner access)"
+		game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", {
+			Text = "[Preview] " .. stateText,
+			Color = Color3.fromRGB(255, 200, 100),
+			Font = Enum.Font.SourceSansBold,
+		})
+		print("[BetaAdmin] Preview mode toggled by", player.Name, "->", stateText)
+		
 	elseif string.match(lowerMessage, "^/urgentupdate") then
 		-- Urgent server shutdown with countdown (ALL SERVERS)
 		if isShuttingDown then
@@ -419,4 +620,4 @@ for _, player in ipairs(Players:GetPlayers()) do
 	end)
 end
 
-print("[BetaAdminCommands] Initialized - Owner commands: /betastatus, /betalist, /betacount, /update, /version, /resetversion, /changelog, /feedback, /clearfeedback, /urgentupdate")
+print("[BetaAdminCommands] Initialized - Owner commands: /betastatus, /betalist, /betacount, /update, /version, /resetversion, /changelog, /feedback, /clearfeedback, /scanfloatinggrass, /clearfloatinggrass, /urgentupdate, /preview")
diff --git a/public/src/Server/BlockHandler.server.luau b/public/src/Server/BlockHandler.server.luau
index 3b3a067..82a0089 100644
--- a/public/src/Server/BlockHandler.server.luau
+++ b/public/src/Server/BlockHandler.server.luau
@@ -28,6 +28,29 @@ local WallpaperPlacement = require(ReplicatedStorage:WaitForChild("WallpaperPlac
 local CarpetPlacement = require(ReplicatedStorage:WaitForChild("CarpetPlacement", 10))
 local VinePlacement = require(ReplicatedStorage:WaitForChild("VinePlacement", 10))
 local ColumnPlacement = require(ReplicatedStorage:WaitForChild("ColumnPlacement", 10))
+
+-- Resolve a glass-style block's template Model from its type + generic shape name.
+-- Standard glass lives in Decorations/Glass; glass-style reward sets (CandyLattice)
+-- live in their RewardSets folder. GlassPlacement.ResolveTemplate is the single
+-- source of truth for the folder + model name per type. Returns the template
+-- Instance or nil. Used by the in-place glass-rebuild closures below.
+local function resolveGlassTemplate(glassType, genericTemplateName)
+	local folderPath, modelName = GlassPlacement.ResolveTemplate(glassType, genericTemplateName)
+	local folder = ReplicatedStorage
+	for _, folderName in ipairs(string.split(folderPath, "/")) do
+		folder = folder and folder:FindFirstChild(folderName)
+	end
+	if not folder then
+		warn(string.format("[Glass] Folder not found: %s (type=%s)", tostring(folderPath), tostring(glassType)))
+		return nil
+	end
+	local template = folder:FindFirstChild(modelName)
+	if not template then
+		warn(string.format("[Glass] Template not found: %s in %s (type=%s)", tostring(modelName), tostring(folderPath), tostring(glassType)))
+		return nil
+	end
+	return template
+end
 local ArchitravePlacement = require(ReplicatedStorage:WaitForChild("ArchitravePlacement", 10))
 local BlockCollision = require(ReplicatedStorage:WaitForChild("BlockCollision", 10))
 local BlockTemplates = ReplicatedStorage:WaitForChild("BlockTemplates", 10)
@@ -566,7 +589,7 @@ local function applyColorToBlock(block, color, material, materialVariant)
 	end
 end
 
-local function createBlockFromTemplate(templateName, blockId, useBlockModel, blockModelName, blockModelFolder, applyColor, defaultColor, customVariantChild, presetMaterial, presetMaterialVariant)
+local function createBlockFromTemplate(templateName, blockId, useBlockModel, blockModelName, blockModelFolder, applyColor, defaultColor, customVariantChild, presetMaterial, presetMaterialVariant, stripeColorMode)
 	local template
 	local actuallyUsedBlockModel = false
 	
@@ -635,6 +658,50 @@ local function createBlockFromTemplate(templateName, blockId, useBlockModel, blo
 	local block = template:Clone()
 	block.Name = blockId or templateName
 	
+	-- TEXTURE mode (stripeColorMode == "Texture"): the block's grayscale Texture
+	-- children carry the stripe pattern; tinting them produces the colored variant.
+	-- Tint targets:
+	--   * Every Texture/Decal descendant (the stripes)
+	--   * Any Part named "Core" (the swirl on candy-cane log end caps)
+	-- Everything else (the white "Bark" part) keeps its template color so the
+	-- background between stripes stays white. Used by Candy Cane variants.
+	if stripeColorMode == "Texture" then
+		if block:IsA("BasePart") then
+			block.Anchored = true
+		elseif block:IsA("Model") then
+			for _, part in ipairs(block:GetDescendants()) do
+				if part:IsA("BasePart") then part.Anchored = true end
+			end
+		end
+		if applyColor and defaultColor then
+			-- Tint all Texture/Decal descendants (the stripes painted on the bark/faces).
+			for _, desc in ipairs(block:GetDescendants()) do
+				if desc:IsA("Texture") or desc:IsA("Decal") then
+					desc.Color3 = defaultColor
+				end
+			end
+			-- Tint Parts NAMED "Core" ΓÇö the swirl-pattern end-caps of a candy-cane log.
+			-- The body (Bark) intentionally stays its template color (white) so the
+			-- background between stripes remains white.
+			for _, desc in ipairs(block:GetDescendants()) do
+				if desc:IsA("BasePart") and desc.Name == "Core" then
+					desc.Color = defaultColor
+				end
+			end
+			-- Top-level Texture/Decal children on a Part template (CandyCaneFull is a
+			-- Union with Texture children directly).
+			if block:IsA("BasePart") then
+				for _, child in ipairs(block:GetChildren()) do
+					if child:IsA("Texture") or child:IsA("Decal") then
+						child.Color3 = defaultColor
+					end
+				end
+			end
+		end
+		print("[Server] Created block: " .. tostring(blockId or templateName) .. " | UsedBlockModel: " .. tostring(actuallyUsedBlockModel) .. " | ApplyColor: " .. tostring(applyColor) .. " (Texture mode)")
+		return block, actuallyUsedBlockModel
+	end
+	
 	if block:IsA("BasePart") then
 		block.Anchored = true
 		-- Apply color, material, and materialVariant if specified
@@ -747,15 +814,34 @@ local function createBlockFromTemplate(templateName, blockId, useBlockModel, blo
 	return block, actuallyUsedBlockModel
 end
 
-local function createDecorationFromTemplate(decorationName)
-	if not DecorationsFolder then
+local function createDecorationFromTemplate(decorationName, customFolder)
+	-- customFolder (optional): nested path like "RewardSets/CandyBlocks" that
+	-- overrides the default Decorations folder. Used by reward-set decorations
+	-- (e.g. CandyCaneSapling) which live alongside their themed pack rather
+	-- than in the global Decorations folder.
+	local sourceFolder = DecorationsFolder
+	if customFolder then
+		local folder = ReplicatedStorage
+		for _, folderName in ipairs(string.split(customFolder, "/")) do
+			folder = folder:FindFirstChild(folderName)
+			if not folder then break end
+		end
+		if folder then
+			sourceFolder = folder
+		else
+			warn("[Server] Decoration custom folder not found: " .. tostring(customFolder))
+		end
+	end
+	
+	if not sourceFolder then
 		warn("[Server] Decorations folder not found!")
 		return nil
 	end
 	
-	local template = DecorationsFolder:FindFirstChild(decorationName)
+	local template = sourceFolder:FindFirstChild(decorationName)
 	if not template then
-		warn("[Server] Decoration template not found: " .. tostring(decorationName))
+		warn("[Server] Decoration template not found: " .. tostring(decorationName)
+			.. " (folder: " .. (customFolder or "Decorations") .. ")")
 		return nil
 	end
 	
@@ -1098,7 +1184,7 @@ PlaceBlock.OnServerEvent:Connect(function(player, data)
 		local decoName = data.DecorationData.DecorationName
 		local decoOffset = data.DecorationData.Offset or Vector3.new(0, 0, 0)
 		
-		local decoration = createDecorationFromTemplate(decoName)
+		local decoration = createDecorationFromTemplate(decoName, preset.BlockModelFolder)
 		if not decoration then return end
 		
 		-- Calculate final position with offset
@@ -1902,10 +1988,14 @@ PlaceBlock.OnServerEvent:Connect(function(player, data)
 	local useBlockModelName = (not isFence and not isGlass and not isArchitrave) and preset.BlockModelName or nil
 	local useBlockId = (not isFence and not isGlass and not isArchitrave) and preset.Id or nil
 	
-	-- For glass, look in Decorations/Glass folder
+	-- For glass, resolve the model folder + name from the glass-type registry.
+	-- Standard glass -> Decorations/Glass; CandyLattice -> RewardSets/CandyBlocks; etc.
+	-- templateName here is the generic shape name (Glass_0 / Glass_Corner / ...);
+	-- ResolveTemplate translates it to the real model name for this block's type.
 	if isGlass then
-		useBlockModelFolder = "Decorations/Glass"
-		useBlockModelName = templateName -- e.g. "Glass_2" or "Glass_3"
+		local glassFolderPath, glassModelName = GlassPlacement.ResolveTemplate(data.BlockType, templateName)
+		useBlockModelFolder = glassFolderPath
+		useBlockModelName = glassModelName
 	end
 	
 	-- For torches, look in Decorations folder and use the correct model (TorchStraight or TorchAngled)
@@ -2015,7 +2105,8 @@ PlaceBlock.OnServerEvent:Connect(function(player, data)
 		preset.DefaultColor,
 		preset.CustomVariantChild, -- For custom Union stair/slab models
 		preset.Material,
-		preset.MaterialVariant
+		preset.MaterialVariant,
+		preset.StripeColorMode  -- "Texture" tints Texture descendants instead of the Part
 	)
 	if not block then return end
 	
@@ -2246,8 +2337,12 @@ PlaceBlock.OnServerEvent:Connect(function(player, data)
 		))
 	end
 	
-	-- Apply color/material for template blocks, or block models with ApplyColor set
-	if not usedBlockModel or preset.ApplyColor then
+	-- Apply color/material for template blocks, or block models with ApplyColor set.
+	-- BUT skip this entirely for Texture-mode blocks (candy canes etc.): their
+	-- coloring was already done correctly inside createBlockFromTemplate, which
+	-- tints only Texture descendants + the Core part ΓÇö NOT the white Bark.
+	-- applyColorToBlock would naively tint every part, undoing that work.
+	if (not usedBlockModel or preset.ApplyColor) and preset.StripeColorMode ~= "Texture" then
 		local material = preset.Material
 		if data.MaterialName and type(data.MaterialName) == "string" then
 			local success, result = pcall(function()
@@ -3437,9 +3532,7 @@ PlaceBlock.OnServerEvent:Connect(function(player, data)
 		
 		-- Also update adjacent glass panes (they may need to become / un-become L-corners)
 		local function createGlassBlockForSolid(glassTemplateName, glassType, glassPos, glassRotationCF, placedBy, connectionType, primaryDir, secondaryDir, orientation)
-			local glassFolder = DecorationsFolder and DecorationsFolder:FindFirstChild("Glass")
-			if not glassFolder then return nil end
-			local template = glassFolder:FindFirstChild(glassTemplateName)
+			local template = resolveGlassTemplate(glassType, glassTemplateName)
 			if not template then return nil end
 
 			local newGlass = template:Clone()
@@ -3653,16 +3746,9 @@ PlaceBlock.OnServerEvent:Connect(function(player, data)
 	if isGlass then
 		-- Create a function to create glass blocks for the update
 		local function createGlassBlock(glassTemplateName, glassType, glassPos, glassRotationCF, placedBy, connectionType, primaryDir, secondaryDir, orientation)
-			-- Look for template in Decorations/Glass folder
-			local glassFolder = DecorationsFolder and DecorationsFolder:FindFirstChild("Glass")
-			if not glassFolder then
-				warn("[Glass] Glass folder not found in Decorations!")
-				return nil
-			end
-			
-			local template = glassFolder:FindFirstChild(glassTemplateName)
+			-- Resolve template from the glass-type registry (Decorations/Glass, RewardSets, etc.)
+			local template = resolveGlassTemplate(glassType, glassTemplateName)
 			if not template then
-				warn("[Glass] Template not found: " .. tostring(glassTemplateName))
 				return nil
 			end
 			
@@ -4022,7 +4108,18 @@ RemoveBlock.OnServerEvent:Connect(function(player, block, hitPosition)
 							local py = item:GetAttribute("ParentBlockY")
 							local pz = item:GetAttribute("ParentBlockZ")
 							if px and py and pz then
-								if (Vector3.new(px, py, pz) - hostCell).Magnitude < 1 then
+								-- Snap the stored ParentBlock to the grid the SAME way hostCell
+								-- is snapped before comparing. ParentBlockX/Y/Z were stored as
+								-- math.round(worldPos) at placement, which on an off-grid plot
+								-- doesn't equal the multiple-of-BLOCK_SIZE hostCell. Snapping both
+								-- sides puts them in the same space so saplings/decorations on
+								-- off-grid blocks (e.g. candy cane saplings) still get cleaned up.
+								local snappedParent = Vector3.new(
+									math.round(px / BLOCK_SIZE) * BLOCK_SIZE,
+									math.round(py / BLOCK_SIZE) * BLOCK_SIZE,
+									math.round(pz / BLOCK_SIZE) * BLOCK_SIZE
+								)
+								if (snappedParent - hostCell).Magnitude < 1 then
 									seenItems[item] = true
 									table.insert(itemsToDestroy, item)
 								end
@@ -4092,8 +4189,22 @@ RemoveBlock.OnServerEvent:Connect(function(player, block, hitPosition)
 					local px = item:GetAttribute("ParentBlockX")
 					local py = item:GetAttribute("ParentBlockY")
 					local pz = item:GetAttribute("ParentBlockZ")
-					if px and py and pz and (Vector3.new(px, py, pz) - blockPos).Magnitude < 1 then
-						table.insert(itemsToDestroy, item)
+					if px and py and pz then
+						-- Grid-snap both sides before comparing (see note in the spatial-index
+						-- path above) so decorations on off-grid blocks still match.
+						local snappedParent = Vector3.new(
+							math.round(px / BLOCK_SIZE) * BLOCK_SIZE,
+							math.round(py / BLOCK_SIZE) * BLOCK_SIZE,
+							math.round(pz / BLOCK_SIZE) * BLOCK_SIZE
+						)
+						local snappedBlock = Vector3.new(
+							math.round(blockPos.X / BLOCK_SIZE) * BLOCK_SIZE,
+							math.round(blockPos.Y / BLOCK_SIZE) * BLOCK_SIZE,
+							math.round(blockPos.Z / BLOCK_SIZE) * BLOCK_SIZE
+						)
+						if (snappedParent - snappedBlock).Magnitude < 1 then
+							table.insert(itemsToDestroy, item)
+						end
 					end
 				elseif item:GetAttribute("IsTorch") or item:GetAttribute("IsLantern") or item:GetAttribute("IsSignStanding") or item:GetAttribute("IsWallSign") then
 					local hx = item:GetAttribute("HostBlockX")
@@ -4477,9 +4588,7 @@ RemoveBlock.OnServerEvent:Connect(function(player, block, hitPosition)
 
 		-- Also update adjacent glass panes (corner anchors may have been removed)
 		local function createGlassBlockForSolid(glassTemplateName, glassType, glassPos, glassRotationCF, placedBy, connectionType, primaryDir, secondaryDir, orientation)
-			local glassFolder = DecorationsFolder and DecorationsFolder:FindFirstChild("Glass")
-			if not glassFolder then return nil end
-			local template = glassFolder:FindFirstChild(glassTemplateName)
+			local template = resolveGlassTemplate(glassType, glassTemplateName)
 			if not template then return nil end
 
 			local newGlass = template:Clone()
@@ -4592,16 +4701,9 @@ RemoveBlock.OnServerEvent:Connect(function(player, block, hitPosition)
 	-- If glass was broken, update adjacent glass
 	if isGlass and blockPos then
 		local function createGlassBlock(glassTemplateName, glassType, glassPos, glassRotationCF, placedBy, connectionType, primaryDir, secondaryDir, orientation)
-			-- Look for template in Decorations/Glass folder
-			local glassFolder = DecorationsFolder and DecorationsFolder:FindFirstChild("Glass")
-			if not glassFolder then
-				warn("[Glass] Glass folder not found in Decorations!")
-				return nil
-			end
-			
-			local template = glassFolder:FindFirstChild(glassTemplateName)
+			-- Resolve template from the glass-type registry (Decorations/Glass, RewardSets, etc.)
+			local template = resolveGlassTemplate(glassType, glassTemplateName)
 			if not template then
-				warn("[Glass] Template not found: " .. tostring(glassTemplateName))
 				return nil
 			end
 			
diff --git a/public/src/Server/LightingManager.server.luau b/public/src/Server/LightingManager.server.luau
index 325a4dc..2a71e5d 100644
--- a/public/src/Server/LightingManager.server.luau
+++ b/public/src/Server/LightingManager.server.luau
@@ -336,8 +336,6 @@ local function recalcBuildArea(buildArea)
 	end
 	
 	if adjustedCount > 0 then
-		print(string.format("[LightingManager] Recalculated %d lights (%d adjusted) in %s",
-			processedCount, adjustedCount, buildArea.Name))
 	end
 end
 
@@ -379,7 +377,6 @@ local function trackLight(light)
 	
 	local buildArea = getBuildArea(light)
 	if not buildArea then
-		print(string.format("[LightingManager] Skipped light '%s' - no build area found", light:GetFullName()))
 		return
 	end
 	
@@ -451,9 +448,6 @@ local function trackLight(light)
 	
 	local totalInArea = countLightsInArea(buildArea)
 	local flickerTag = isFlicker and " [flicker]" or ""
-	print(string.format("[LightingManager] Tracking %s%s (original: %.2f, target: %.2f, range: %.0f) in %s [%d total]",
-		light.ClassName, flickerTag, originalBrightness, targetBrightness, getLightRange(light),
-		buildArea.Name, totalInArea))
 	
 	-- Listen for Enabled changes (lamp toggling, etc.)
 	local enabledConn = light:GetPropertyChangedSignal("Enabled"):Connect(function()
@@ -475,7 +469,6 @@ local function trackLight(light)
 			enabledConn:Disconnect()
 			destroyConn:Disconnect()
 			local remaining = countLightsInArea(buildArea)
-			print(string.format("[LightingManager] Untracked light from %s [%d remaining]", buildArea.Name, remaining))
 			scheduleRecalc(buildArea)
 		end
 	end)
@@ -536,7 +529,6 @@ local function monitorBuildArea(buildArea)
 		end
 		
 		if lightCount > 0 then
-			print(string.format("[LightingManager] Found %d existing lights in %s", lightCount, buildArea.Name))
 		end
 		
 		local addedConn = folder.ChildAdded:Connect(function(block)
@@ -587,7 +579,6 @@ local function checkDeferredAreas()
 				end
 			end
 			
-			print(string.format("[LightingManager] Bulk operation ended in %s - rescanning", buildArea.Name))
 			scheduleRecalc(buildArea)
 		end
 	end
@@ -646,4 +637,3 @@ end
 
 initialize()
 
-print("[LightingManager] Initialized - monitoring light sources for blowout prevention")
diff --git a/public/src/Server/LikesHandler.server.luau b/public/src/Server/LikesHandler.server.luau
index 77ed191..60f09c3 100644
--- a/public/src/Server/LikesHandler.server.luau
+++ b/public/src/Server/LikesHandler.server.luau
@@ -17,6 +17,7 @@ local DataStoreService = game:GetService("DataStoreService")
 local Players = game:GetService("Players")
 local ReplicatedStorage = game:GetService("ReplicatedStorage")
 local ServerScriptService = game:GetService("ServerScriptService")
+local TweenService = game:GetService("TweenService")
 
 local GridConfig = require(ReplicatedStorage:WaitForChild("Config"):WaitForChild("GridConfig"))
 
@@ -327,6 +328,125 @@ local function updateSignText(buildArea, likeCount)
 	end
 end
 
+-- Debug toggle for the owner-name banner. Set false to silence [BB-LIKE] logs.
+local DEBUG_LIKE_NAMES = false
+
+-- Cache of resolved owner display names so offline-owner plots don't hit the
+-- web API on every refresh pass.
+local OwnerDisplayNameCache = {} -- [userId] = displayName
+
+-- Best-available DISPLAY name for a plot owner WITHOUT yielding (matches the names
+-- shown in the player list). Order: live player -> cached lookup -> stored username
+-- attribute as a stopgap. Returns nil if nothing is known yet and kicks off a one-shot
+-- background resolve so the next pass (runs every 10s) can show the real display name.
+local function resolveOwnerDisplayName(ownerUserId, ownerNameAttr)
+	local online = Players:GetPlayerByUserId(ownerUserId)
+	if online then return online.DisplayName end
+	if OwnerDisplayNameCache[ownerUserId] then return OwnerDisplayNameCache[ownerUserId] end
+	task.spawn(function()
+		local ok, infos = pcall(function()
+			return Players:GetUserInfosByUserIdsAsync({ ownerUserId })
+		end)
+		if ok and infos and infos[1] and infos[1].DisplayName then
+			OwnerDisplayNameCache[ownerUserId] = infos[1].DisplayName
+		end
+	end)
+	-- Stopgap (username) until the async display-name lookup populates the cache.
+	if ownerNameAttr and ownerNameAttr ~= "" then return ownerNameAttr end
+	return nil
+end
+
+-- Tracks which NamePlates already have a bob tween running (start it only once).
+local namePlateBobStarted = {} -- [part] = true
+
+-- Gently float the NamePlate board up and down forever (subtle bob).
+local function startNamePlateBob(part)
+	if namePlateBobStarted[part] then return end
+	namePlateBobStarted[part] = true
+
+	-- Make sure it's kinematic so the tween fully controls it (no falling).
+	part.Anchored = true
+
+	local base = part.Position
+	local info = TweenInfo.new(
+		1.8,                          -- seconds for each leg of the bob
+		Enum.EasingStyle.Sine,
+		Enum.EasingDirection.InOut,
+		-1,                           -- repeat forever
+		true                          -- reverse (float up, then back down)
+	)
+	local tween = TweenService:Create(part, info, {
+		Position = base + Vector3.new(0, 0.35, 0),
+	})
+	tween:Play()
+end
+
+-- Plot owner's username painted onto the LikeSign's NamePlate board (a flat
+-- SurfaceGui locked to the board's face), plus a gentle up/down float.
+-- Pass username=nil to leave the board blank (e.g. an unowned plot).
+local function updateSignOwnerName(buildArea, username)
+	local likeSign = buildArea:FindFirstChild("LikeSign")
+	if not likeSign then return end
+
+	-- The board may be authored under the LikeSign model OR directly under the
+	-- BuildArea (depending on how it was placed in Studio) ΓÇö check both.
+	local namePlate = likeSign:FindFirstChild("NamePlate")
+		or buildArea:FindFirstChild("NamePlate")
+	if not namePlate or not namePlate:IsA("BasePart") then return end
+
+	-- Reuse the SurfaceGui authored on the board; create one only if it's missing
+	-- (don't override the Face the board was set up with).
+	local gui = namePlate:FindFirstChildWhichIsA("SurfaceGui")
+	if not gui then
+		gui = Instance.new("SurfaceGui")
+		gui.Name = "NameGui"
+		gui.Face = Enum.NormalId.Front
+		gui.SizingMode = Enum.SurfaceGuiSizingMode.PixelsPerStud
+		gui.PixelsPerStud = 50
+		gui.LightInfluence = 0
+		gui.Brightness = 1
+		gui.Parent = namePlate
+	end
+
+	-- Reuse an existing label, else create one that fills the board.
+	local label = gui:FindFirstChildWhichIsA("TextLabel")
+	if not label then
+		label = Instance.new("TextLabel")
+		label.Name = "OwnerName"
+		label.Size = UDim2.new(1, 0, 1, 0)
+		label.BackgroundTransparency = 1
+		label.TextColor3 = Color3.fromRGB(255, 255, 255)
+		label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
+		label.TextStrokeTransparency = 0.4
+		label.Font = Enum.Font.Arcade
+		label.TextScaled = true
+		label.Text = ""
+		label.Parent = gui
+
+		local padding = Instance.new("UIPadding")
+		padding.PaddingLeft = UDim.new(0, 8)
+		padding.PaddingRight = UDim.new(0, 8)
+		padding.PaddingTop = UDim.new(0, 4)
+		padding.PaddingBottom = UDim.new(0, 4)
+		padding.Parent = label
+	end
+
+	-- Always enforce a crisp canvas resolution and the arcade (pixel) font,
+	-- even on a reused SurfaceGui/label.
+	gui.SizingMode = Enum.SurfaceGuiSizingMode.PixelsPerStud
+	gui.PixelsPerStud = 100
+	label.Font = Enum.Font.Arcade
+	label.Text = username or ""
+
+	-- Float the board gently (runs once per board).
+	startNamePlateBob(namePlate)
+
+	if DEBUG_LIKE_NAMES then
+		print(string.format("[BB-LIKE] NamePlate %s | face=%s text=%s",
+			buildArea.Name, tostring(gui.Face), tostring(label.Text)))
+	end
+end
+
 -- Update all signs when plot owners change
 local function updateAllSigns()
 	for i = 1, GridConfig.TOTAL_PLOTS do
@@ -336,8 +456,17 @@ local function updateAllSigns()
 			if ownerUserId then
 				local totalLikes = getTotalLikes(ownerUserId)
 				updateSignText(buildArea, totalLikes)
+
+				local ownerNameAttr = buildArea:GetAttribute("OwnerName")
+				local resolved = resolveOwnerDisplayName(ownerUserId, ownerNameAttr)
+				if DEBUG_LIKE_NAMES then
+					print(string.format("[BB-LIKE] plot %d owned by %s | OwnerName attr=%s | resolved=%s",
+						i, tostring(ownerUserId), tostring(ownerNameAttr), tostring(resolved)))
+				end
+				updateSignOwnerName(buildArea, resolved)
 			else
 				updateSignText(buildArea, 0)
+				updateSignOwnerName(buildArea, nil)
 			end
 		end
 	end
diff --git a/public/src/Server/MegaPlotSaveManager.server.luau b/public/src/Server/MegaPlotSaveManager.server.luau
index 0e10b69..295349b 100644
--- a/public/src/Server/MegaPlotSaveManager.server.luau
+++ b/public/src/Server/MegaPlotSaveManager.server.luau
@@ -57,6 +57,19 @@ local SlabPlacement = require(ReplicatedStorage:WaitForChild("SlabPlacement", 10
 local StairPlacement = require(ReplicatedStorage:WaitForChild("StairPlacement", 10))
 local FencePlacement = require(ReplicatedStorage:WaitForChild("FencePlacement", 10))
 local GlassPlacement = require(ReplicatedStorage:WaitForChild("GlassPlacement", 10))
+
+-- Resolve a glass-style block's template Model from its type + generic shape name.
+-- Mirrors the helper in SaveManager/BlockHandler. Standard glass -> Decorations/Glass;
+-- glass-style reward sets (CandyLattice) -> their RewardSets folder. Returns Instance or nil.
+local function resolveGlassTemplate(glassType, genericTemplateName)
+	local folderPath, modelName = GlassPlacement.ResolveTemplate(glassType, genericTemplateName)
+	local folder = ReplicatedStorage
+	for _, folderName in ipairs(string.split(folderPath, "/")) do
+		folder = folder and folder:FindFirstChild(folderName)
+	end
+	if not folder then return nil end
+	return folder:FindFirstChild(modelName)
+end
 local LadderPlacement = require(ReplicatedStorage:WaitForChild("LadderPlacement", 10))
 local TorchPlacement = require(ReplicatedStorage:WaitForChild("TorchPlacement", 10))
 local LanternPlacement = require(ReplicatedStorage:WaitForChild("LanternPlacement", 10))
@@ -474,8 +487,7 @@ local function createBlockFromSave(blockData, blocksFolder, originOffset, templa
 			if BlockTemplates then template = BlockTemplates:FindFirstChild(templateName) end
 		end
 	elseif isGlass then
-		local glassFolder = DecorationsSourceFolder and DecorationsSourceFolder:FindFirstChild("Glass")
-		if glassFolder then template = glassFolder:FindFirstChild(templateName) end
+		template = resolveGlassTemplate(blockData.blockType, templateName)
 	elseif isColumn then
 		if BlocksSourceFolder then
 			template = BlocksSourceFolder:FindFirstChild(columnModelName)
@@ -1521,10 +1533,6 @@ end
 -- ============================================
 
 local function reconnectGlass(blocksFolder)
-	local DecorationsSourceFolder = getDecorationsFolder()
-	local glassFolder = DecorationsSourceFolder and DecorationsSourceFolder:FindFirstChild("Glass")
-	if not glassFolder then return end
-
 	local function snapToGrid(pos)
 		return Vector3.new(
 			math.round(pos.X / BLOCK_SIZE) * BLOCK_SIZE,
@@ -1553,7 +1561,7 @@ local function reconnectGlass(blocksFolder)
 
 			if newInfo.connectionType ~= currentType then
 				glass:Destroy()
-				local newTemplate = glassFolder:FindFirstChild(newInfo.templateName)
+				local newTemplate = resolveGlassTemplate(glassType, newInfo.templateName)
 				if newTemplate then
 					local newGlass = newTemplate:Clone()
 					newGlass.Name = glassType
diff --git a/public/src/Server/Modules/GrassSpreadManager.luau b/public/src/Server/Modules/GrassSpreadManager.luau
index f047686..a67be0f 100644
--- a/public/src/Server/Modules/GrassSpreadManager.luau
+++ b/public/src/Server/Modules/GrassSpreadManager.luau
@@ -486,12 +486,15 @@ local function convertGrassToDirt(grassBlock, blocksFolder)
 		dirtBlock:SetAttribute("PlacedBy", placedBy)
 	end
 	
-	if isTerrainBlock then
+	-- FIX: A block with no PlacedBy is natural terrain. Always flag decayed dirt as terrain
+	-- so its lineage isn't lost (which previously orphaned grass that later regrew on it).
+	local treatAsTerrain = isTerrainBlock or not placedBy
+	if treatAsTerrain then
 		dirtBlock:SetAttribute("IsTerrainBlock", true)
 		dirtBlock:SetAttribute("TerrainDepth", 1) -- Now underground
 	end
 	
-	if isLazyLoaded then
+	if isLazyLoaded or treatAsTerrain then
 		dirtBlock:SetAttribute("IsLazyLoaded", true)
 	end
 	
@@ -667,12 +670,21 @@ local function convertDirtToGrass(dirtBlock, blocksFolder, biomeName, seasonName
 		grassBlock:SetAttribute("PlacedBy", placedBy)
 	end
 	
-	if isTerrainBlock then
+	-- FIX: A block with no PlacedBy is, by definition, natural terrain. Always flag it as
+	-- terrain so that breaking it routes through OnTerrainBlockBroken (marks the position
+	-- dug). Previously this was inherited only if the source dirt still had IsTerrainBlock,
+	-- so a grass block whose lineage lost the flag during decay/spread became an orphan:
+	-- not saved, not mark-dug, and thus regenerated on every load.
+	local treatAsTerrain = isTerrainBlock or not placedBy
+	if treatAsTerrain then
 		grassBlock:SetAttribute("IsTerrainBlock", true)
 		grassBlock:SetAttribute("TerrainDepth", 0) -- Grass is always surface
 	end
 	
-	if isLazyLoaded then
+	-- Grown grass is a state change from the originally generated surface, so it must be
+	-- saved to persist. Lazy-load it if it was already lazy OR if it's terrain (so the
+	-- grown state round-trips and the block is mark-dug correctly when broken).
+	if isLazyLoaded or treatAsTerrain then
 		grassBlock:SetAttribute("IsLazyLoaded", true)
 	end
 	
diff --git a/public/src/Server/Modules/TerrainGenerator.luau b/public/src/Server/Modules/TerrainGenerator.luau
index fc92c82..f1019a1 100644
--- a/public/src/Server/Modules/TerrainGenerator.luau
+++ b/public/src/Server/Modules/TerrainGenerator.luau
@@ -682,6 +682,50 @@ function TerrainGenerator.OnTerrainBlockBroken(position, buildArea, plotNumber,
 		if DEBUG_TERRAIN_NET then print(string.format("[BB-NET] TERRAIN marked as dug: key=%s", getPositionKey(snappedPos))) end
 	end
 	
+	-- Clean up orphaned spread-grass left floating above the dug cell. When dirt is
+	-- removed from under a block of GrownFromSpread grass, that grass has no support
+	-- and should fall away too (otherwise it becomes a floating phantom that persists
+	-- in the save via IsLazyLoaded and reappears every load). We walk upward from the
+	-- dug position and remove each consecutive GrownFromSpread grass block that now has
+	-- nothing solid directly below it. Hand-placed grass never has GrownFromSpread, so
+	-- this can only ever remove auto-spread debris, never an intentional build.
+	if BlockSpatialIndex then
+		local index = BlockSpatialIndex.GetOrCreate(blocksFolder)
+		if index then
+			local function isSpreadGrass(b)
+				if not b or not b:GetAttribute("GrownFromSpread") then return false end
+				local bt = b:GetAttribute("BlockType")
+				return bt == "GrassBlock" or bt == "SnowyGrassBlock"
+					or bt == "SnowGrassBlock" or bt == "TaigaGrassBlock"
+			end
+			-- Track cells we clear this pass; the spatial index's ChildRemoved update is
+			-- deferred, so we can't rely on it reflecting our removals mid-loop.
+			local clearedThisPass = {}
+			local function cellEmpty(p)
+				if clearedThisPass[getPositionKey(p)] then return true end
+				return not index:HasBlockAt(p)
+			end
+			clearedThisPass[getPositionKey(snappedPos)] = true -- the cell we just dug
+			local checkPos = snappedPos + Vector3.new(0, BLOCK_SIZE, 0)
+			local belowPos = snappedPos
+			local guard = 0
+			while guard < TERRAIN.MAX_DEPTH + 2 do
+				guard += 1
+				-- Only remove if the cell directly below is unsupported (empty / dug).
+				if not cellEmpty(belowPos) then break end
+				local above = index:GetBlockAt(checkPos)
+				if not isSpreadGrass(above) then break end
+				if DEBUG_LAZY_LOAD then
+					print(string.format("[LazyLoad] Removing orphaned spread-grass at (%.1f,%.1f,%.1f)", checkPos.X, checkPos.Y, checkPos.Z))
+				end
+				above:Destroy()
+				clearedThisPass[getPositionKey(checkPos)] = true
+				belowPos = checkPos
+				checkPos = checkPos + Vector3.new(0, BLOCK_SIZE, 0)
+			end
+		end
+	end
+	
 	local blocksSpawned = 0
 	local skippedDugPositions = {} -- Track positions skipped because they're dug (for client prediction cleanup)
 	local surfaceY = getSurfaceYAtPosition(snappedPos, buildArea)
@@ -1091,16 +1135,10 @@ local function spawnBlocksInChunks(params)
 			task.wait()
 		end
 		
-		-- Progress logging every 25%
-		if chunkIndex == 1 or chunkIndex == totalChunks or chunkIndex % math.max(1, math.floor(totalChunks / 4)) == 0 then
-			print(string.format("[TerrainGenerator] Chunk %d/%d spawned (%d blocks)", chunkIndex, totalChunks, spawnedCount))
-		end
 	end
 	
 	if progressValue then progressValue.Value = 1 end
 	
-	print(string.format("[TerrainGenerator] Chunk loading complete: %d terrain + %d deco + %d tree blocks",
-		blocksCreated, decorationsCreated, treeBlocksCreated))
 	
 	return blocksCreated, decorationsCreated, treeBlocksCreated
 end
@@ -1173,7 +1211,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 	-- FLAT TERRAIN: Simple grass-only generation
 	-- ============================================
 	if terrainStyle == "Flat" then
-		print("[TerrainGenerator] Generating flat grass terrain (Season: " .. currentSeason .. ")...")
 		
 		local grassType = TERRAIN.GRASS_BLOCK
 		local blocksCreated = 0
@@ -1197,7 +1234,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 		end
 		
 		local elapsed = tick() - startTime
-		print(string.format("[TerrainGenerator] Done! %d grass blocks in %.2fs", blocksCreated, elapsed))
 		GeneratingPlots[plotNumber] = nil  -- Mark generation complete
 		buildArea:SetAttribute("TerrainGenerationInProgress", nil)  -- Clear flag
 		BulkOperationFlags.EndBulkOperation(buildArea)  -- Re-enable handlers
@@ -1208,7 +1244,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 	-- NATURAL TERRAIN: Full biome with hills, trees, decorations
 	-- ============================================
 	if not biomeConfig then
-		print("[TerrainGenerator] No biome config found, falling back to flat terrain")
 		-- Fall back to flat if no biome config
 		GeneratedPlots[plotNumber] = nil
 		return TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, biomeName, "Flat", savedSeed)
@@ -1219,9 +1254,7 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 	local useFastMode = isMegaPlot and GENERATION_SETTINGS.MEGA_PLOT_FAST_MODE
 	
 	if useFastMode then
-		print("[TerrainGenerator] Generating " .. biomeName .. " (Natural) - FAST MODE for MegaPlot...")
 	else
-		print("[TerrainGenerator] Generating " .. biomeName .. " (Natural) 3D-printer style...")
 	end
 	
 	-- Noise settings from biome
@@ -1233,7 +1266,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 	
 	buildArea:SetAttribute("TerrainSeed", noiseSeed)
 	terrainRng = Random.new(noiseSeed)  -- Isolated RNG for deterministic generation
-	print("[TerrainGenerator] Using seed: " .. noiseSeed)
 	
 	-- Pre-calculate height map (or restore from saved data for exact terrain reproduction)
 	local heightMap = {}
@@ -1258,14 +1290,11 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 				maxHeight = math.max(maxHeight, h)
 			end
 		end
-		print(string.format("[TerrainGenerator] Using SAVED height map for exact terrain restoration (%dx%d, range %d to %d)",
-			gridSizeX, gridSizeZ, minHeight, maxHeight))
 	else
 		-- Generate new height map from noise (first-time generation)
 		if savedHeightMapStr then
 			-- Clear stale height map that doesn't match current grid dimensions
 			buildArea:SetAttribute("TerrainHeightMap", nil)
-			print("[TerrainGenerator] Cleared stale saved height map (grid size mismatch)")
 		end
 		
 		local terrainShape = biomeConfig.TerrainShape  -- Optional shaping curve
@@ -1320,9 +1349,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 				maxHeight = math.max(maxHeight, h)
 			end
 		end
-		
-		print(string.format("[TerrainGenerator] Generated NEW height map from noise (%dx%d, range %d to %d)",
-			gridSizeX, gridSizeZ, minHeight, maxHeight))
 	end
 	
 	-- Collect all blocks by Y level (terrain + trees)
@@ -1539,7 +1565,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 	-- Edge walls disabled - floor is deep enough now that they aren't needed
 	local edgeBlocksCreated = 0
 	if false then -- Previously: not isMegaPlot
-		print("[TerrainGenerator] Spawning edge walls for individual plot...")
 		local edgeBatchCount = 0
 		local edgeYieldInterval = 50
 		
@@ -1605,7 +1630,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 			if gridX % 10 == 0 then task.wait() end
 		end
 	else
-		print("[TerrainGenerator] MegaPlot: Skipping edge walls")
 	end
 	
 	-- ============================================
@@ -1649,8 +1673,6 @@ function TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, bi
 	buildArea:SetAttribute("TerrainHeightMap", table.concat(heightData, ","))
 	
 	local elapsed = tick() - startTime
-	print(string.format("[TerrainGenerator] Done! %d terrain + %d edge + %d deco + %d tree/log/cactus blocks (%d trees, %d fallen logs, %d cacti) in %.2fs",
-		blocksCreated, edgeBlocksCreated, decorationsCreated, treeBlocksCreated, treesPlanned, fallenLogsPlanned, cactusPlanned, elapsed))
 	
 	if LeafDecayManager and treesPlanned > 0 then
 		LeafDecayManager.Initialize(buildArea)
@@ -1707,7 +1729,6 @@ function TerrainGenerator.GenerateFlatTerrain(buildArea, plotNumber)
 	-- Initialize decoration cleanup
 	TerrainGenerator.InitializeDecorationCleanup(buildArea)
 	
-	print("[TerrainGenerator] Flat terrain: " .. blocksCreated .. " blocks")
 	return blocksCreated
 end
 
@@ -1921,8 +1942,6 @@ function TerrainGenerator.UpdateTerrainColors(buildArea, biomeName, seasonName)
 	end
 	
 	local biomeColors = BiomeConfig.GetBiomeColors(biomeName)
-	print(string.format("[TerrainGenerator] UpdateTerrainColors: biome=%s season=%s grassColor=%s",
-		biomeName, seasonName, biomeColors.GrassColor and tostring(biomeColors.GrassColor) or "nil"))
 	
 	local updated = 0
 	local skipped = 0
@@ -1937,7 +1956,6 @@ function TerrainGenerator.UpdateTerrainColors(buildArea, biomeName, seasonName)
 	}
 	
 	local children = blocksFolder:GetChildren()
-	print("[TerrainGenerator] Total children in Blocks folder:", #children)
 	
 	for i, child in ipairs(children) do
 		local blockType = child:GetAttribute("BlockType")
@@ -1958,8 +1976,6 @@ function TerrainGenerator.UpdateTerrainColors(buildArea, biomeName, seasonName)
 		-- Yield occasionally to avoid freezing on large plots
 		if i % 500 == 0 then task.wait() end
 	end
-	
-	print(string.format("[TerrainGenerator] Color update done: %d updated, %d skipped (biome=%s season=%s)", updated, skipped, biomeName, seasonName))
 	return updated
 end
 
@@ -2108,9 +2124,6 @@ function TerrainGenerator.GenerateDefaultTerrain(buildArea, plotNumber, savedSee
 	
 	local numBiomes = #selectedBiomes
 	
-	print("[TerrainGenerator] Default terrain with biomes: " .. table.concat(selectedBiomes, ", "))
-	print("[TerrainGenerator] Using seed: " .. noiseSeed)
-	
 	-- Generate Voronoi control points for biome regions
 	local biomePoints = {}
 	for i, biomeName in ipairs(selectedBiomes) do
@@ -2235,7 +2248,6 @@ function TerrainGenerator.GenerateDefaultTerrain(buildArea, plotNumber, savedSee
 	local useFastMode = isMegaPlot and GENERATION_SETTINGS.MEGA_PLOT_FAST_MODE
 	
 	local startTime = tick()
-	print("[TerrainGenerator] Generating Default multi-biome terrain (Season: " .. currentSeason .. ")...")
 	
 	-- Pre-calculate height map using per-cell biome settings
 	-- Heights are blended at biome borders for smooth transitions
@@ -2261,13 +2273,10 @@ function TerrainGenerator.GenerateDefaultTerrain(buildArea, plotNumber, savedSee
 				maxHeight = math.max(maxHeight, h)
 			end
 		end
-		print(string.format("[TerrainGenerator] Default: Using SAVED height map for exact terrain restoration (%dx%d, range %d to %d)",
-			gridSizeX, gridSizeZ, minHeight, maxHeight))
 	else
 		-- Generate new height map from noise (first-time generation)
 		if savedHeightMapStr then
 			buildArea:SetAttribute("TerrainHeightMap", nil)
-			print("[TerrainGenerator] Default: Cleared stale saved height map (grid size mismatch)")
 		end
 		
 		-- Helper: compute raw height for a biome config at world position
@@ -2347,9 +2356,6 @@ function TerrainGenerator.GenerateDefaultTerrain(buildArea, plotNumber, savedSee
 				maxHeight = math.max(maxHeight, h)
 			end
 		end
-		
-		print(string.format("[TerrainGenerator] Default: Generated NEW height map from noise (%dx%d, range %d to %d)",
-			gridSizeX, gridSizeZ, minHeight, maxHeight))
 	end
 	
 	-- Store height map
@@ -2584,9 +2590,6 @@ function TerrainGenerator.GenerateDefaultTerrain(buildArea, plotNumber, savedSee
 	})
 	
 	local elapsed = tick() - startTime
-	print(string.format("[TerrainGenerator] Default terrain done! %d terrain + %d deco + %d tree/cactus blocks (%d trees, %d logs, %d cacti) in %.2fs",
-		blocksCreated, decorationsCreated, treeBlocksCreated, treesPlanned, fallenLogsPlanned, cactusPlanned, elapsed))
-	print("[TerrainGenerator] Biomes: " .. table.concat(selectedBiomes, ", "))
 	
 	if LeafDecayManager and treesPlanned > 0 then
 		LeafDecayManager.Initialize(buildArea)
@@ -2822,6 +2825,20 @@ function TerrainGenerator.GetSurfaceY(buildArea)
 	return buildArea:GetAttribute("TerrainSurfaceY") or 0
 end
 
+-- Public, lazy-aware check: has the given world position been dug out on this plot?
+-- Snaps to the terrain grid first so callers can pass any nearby world position.
+-- Used by cleanup tools to tell "floating debris" (terrain below was dug) apart from
+-- grass resting on lazy terrain that simply hasn't spawned yet.
+function TerrainGenerator.IsPositionDug(position, plotNumber, buildArea)
+	if not plotNumber then return false end
+	local checkPos = position
+	if buildArea then
+		local ok, snapped = pcall(snapToTerrainGrid, position, buildArea)
+		if ok and snapped then checkPos = snapped end
+	end
+	return isPositionDug(checkPos, plotNumber)
+end
+
 function TerrainGenerator.GetTerrainDepth()
 	return TERRAIN.MAX_DEPTH
 end
@@ -2978,11 +2995,20 @@ function TerrainGenerator.RepairMissingTerrain(buildArea, plotNumber)
 					local worldZ = startZ + gz * BLOCK_SIZE
 					local worldPos = Vector3.new(worldX, expectedSurfaceY, worldZ)
 					
-					local grassType = TERRAIN.GRASS_BLOCK
-					local block = createBlock(grassType, worldPos, blocksFolder, 0, false, currentBiome, currentSeason)
-					if block then
-						block:SetAttribute("SurfaceY", expectedSurfaceY)
-						repaired = repaired + 1
+					-- GUARD: never "repair" a surface cell the player deliberately dug out.
+					-- A dug pit has nothing below it within range, which looks identical to a
+					-- strip bug to the hasBlocksBelow heuristic. DugPositions is the authoritative
+					-- record of intentional digging; restoring grass here is exactly what created
+					-- floating grass over hollowed pits. Genuine strip-bug cells were never dug,
+					-- so they aren't in DugPositions and still get repaired below.
+					local isDug = plotNumber and isPositionDug(worldPos, plotNumber)
+					if not isDug then
+						local grassType = TERRAIN.GRASS_BLOCK
+						local block = createBlock(grassType, worldPos, blocksFolder, 0, false, currentBiome, currentSeason)
+						if block then
+							block:SetAttribute("SurfaceY", expectedSurfaceY)
+							repaired = repaired + 1
+						end
 					end
 				end
 			end
diff --git a/public/src/Server/PlotManager.server.luau b/public/src/Server/PlotManager.server.luau
index bba9a6f..63858eb 100644
--- a/public/src/Server/PlotManager.server.luau
+++ b/public/src/Server/PlotManager.server.luau
@@ -41,13 +41,11 @@ do
 		if snowyGrassModule then
 			SnowyGrassManager = require(snowyGrassModule)
 			SnowyGrassManager.Initialize()
-			print("[PlotManager] SnowyGrassManager loaded")
 		end
 		
 		local weatherModule = modulesFolder:WaitForChild("WeatherManager", 10)
 		if weatherModule then
 			WeatherManager = require(weatherModule)
-			print("[PlotManager] WeatherManager loaded")
 		end
 	end
 end
@@ -142,17 +140,14 @@ local function assignPlot(player, plotNumber)
 			local defaultBiome = BiomeConfig and BiomeConfig.DefaultBiome or "Plains"
 			local defaultTerrainStyle = "Flat"
 			
-			print("[PlotManager] Generating terrain on-demand for plot " .. plotNumber)
 			TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, defaultBiome, defaultTerrainStyle)
 			
 			buildArea:SetAttribute("TerrainStyle", defaultTerrainStyle)
 			buildArea:SetAttribute("CurrentBiome", defaultBiome)
 			PlotTerrainReady[plotNumber] = true
-			print("[PlotManager] Plot " .. plotNumber .. " terrain ready for " .. player.Name)
 		end
 	end
 	
-	print(player.Name .. " assigned to plot " .. plotNumber)
 	PlotAssigned:FireClient(player, plotNumber, buildArea)
 	
 	-- Register plot for grass spreading
@@ -178,9 +173,7 @@ local function assignPlot(player, plotNumber)
 		if AutoLoadPlot then
 			local success, result = AutoLoadPlot:Invoke(player, buildArea, plotNumber)
 			if success then
-				print("[PlotManager] Auto-loaded save for " .. player.Name)
 			else
-				print("[PlotManager] No save to load for " .. player.Name .. " (" .. tostring(result) .. ")")
 			end
 		else
 			warn("[PlotManager] AutoLoadPlot not found after 10s timeout - SaveManager may have failed to initialize")
@@ -271,13 +264,11 @@ local function releasePlot(player)
 				local defaultBiome = BiomeConfig and BiomeConfig.DefaultBiome or "Plains"
 				local defaultTerrainStyle = "Flat"
 				
-				print("[PlotManager] Regenerating terrain for released plot " .. plotNumber)
 				TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, plotNumber, defaultBiome, defaultTerrainStyle)
 				
 				buildArea:SetAttribute("TerrainStyle", defaultTerrainStyle)
 				buildArea:SetAttribute("CurrentBiome", defaultBiome)
 				PlotTerrainReady[plotNumber] = true
-				print("[PlotManager] Plot " .. plotNumber .. " terrain regenerated")
 			end)
 		else
 			PlotTerrainReady[plotNumber] = false
@@ -286,13 +277,11 @@ local function releasePlot(player)
 	
 	PlotOwners[plotNumber] = nil
 	PlayerPlots[player] = nil
-	print(player.Name .. " released plot " .. plotNumber)
 end
 
 local function teleportToPlot(player)
 	-- Skip teleport if MegaPlot mode is active - MegaPlotVisibility handles spawning
 	if isMegaPlotMode() then
-		print("[PlotManager] Skipping teleport for", player.Name, "- MegaPlot mode active")
 		return
 	end
 	
@@ -349,7 +338,6 @@ local function teleportToPlot(player)
 	
 	-- Teleport
 	character:PivotTo(CFrame.new(spawnPos))
-	print("[PlotManager] Teleported", player.Name, "to plot", plotNumber)
 	
 	-- Wait a moment for terrain to register, then unanchor
 	task.delay(0.3, function()
@@ -443,7 +431,6 @@ Players.PlayerAdded:Connect(function(player)
 end)
 
 Players.PlayerRemoving:Connect(function(player)
-	print("[PlotManager] Player leaving: " .. player.Name .. " - starting save-before-release")
 	
 	-- IMPORTANT: Wait for SaveManager to finish auto-save BEFORE clearing the plot
 	-- Otherwise the save happens while blocks are being destroyed, corrupting dug detection
@@ -455,7 +442,6 @@ Players.PlayerRemoving:Connect(function(player)
 		end)
 		local saveTime = tick() - saveStart
 		if success then
-			print(string.format("[PlotManager] SavePlotSync completed for %s in %.2fs", player.Name, saveTime))
 		else
 			warn(string.format("[PlotManager] SavePlotSync FAILED for %s after %.2fs: %s", player.Name, saveTime, tostring(err)))
 		end
@@ -464,7 +450,6 @@ Players.PlayerRemoving:Connect(function(player)
 	end
 	
 	releasePlot(player)
-	print("[PlotManager] Plot released for " .. player.Name)
 end)
 
 GetPlotInfo.OnServerInvoke = function(player)
@@ -500,7 +485,6 @@ CheckOwnership.OnInvoke = doesPlayerOwnPosition
 -- ============================================
 
 local function initializeAllPlots()
-	print("[PlotManager] Preparing " .. GridConfig.TOTAL_PLOTS .. " plots (terrain deferred until player joins)...")
 	
 	for i = 1, GridConfig.TOTAL_PLOTS do
 		local buildArea = getBuildArea(i)
@@ -534,11 +518,9 @@ local function initializeAllPlots()
 				blocksFolder.Parent = buildArea
 			end
 			
-			print("[PlotManager] Plot " .. i .. " prepared (no terrain yet)")
 		end
 	end
 	
-	print("[PlotManager] All plots prepared! Terrain will generate on-demand when players join.")
 end
 
 -- Shared state: clients can check this on join to know if loading screen is needed
@@ -573,19 +555,16 @@ local function startBackgroundTerrainGen()
 		local defaultBiome = BiomeConfig and BiomeConfig.DefaultBiome or "Plains"
 		local defaultTerrainStyle = "Flat"
 		
-		print("[PlotManager] Starting background terrain generation for unclaimed plots...")
 		
 		for i = 1, GridConfig.TOTAL_PLOTS do
 			-- Skip plots that already have terrain (claimed by a player)
 			if not PlotTerrainReady[i] then
 				local buildArea = getBuildArea(i)
 				if buildArea and TerrainGenerator then
-					print("[PlotManager] Background generating terrain for plot " .. i)
 					TerrainGenerator.GenerateFlatTerrainWithBiome(buildArea, i, defaultBiome, defaultTerrainStyle)
 					buildArea:SetAttribute("TerrainStyle", defaultTerrainStyle)
 					buildArea:SetAttribute("CurrentBiome", defaultBiome)
 					PlotTerrainReady[i] = true
-					print("[PlotManager] Plot " .. i .. " background terrain ready")
 					
 					-- Send progress to all clients showing loading screen
 					local readyCount = 0
@@ -603,7 +582,6 @@ local function startBackgroundTerrainGen()
 		-- Signal all clients that plots are ready
 		allPlotsReadyValue.Value = true
 		PlotsReady:FireAllClients("ready")
-		print("[PlotManager] Background terrain generation complete!")
 	end)
 end
 
@@ -618,5 +596,3 @@ backgroundGenConnection = Players.PlayerAdded:Connect(function()
 		backgroundGenConnection = nil
 	end
 end)
-
-print("PlotManager initialized with " .. GridConfig.TOTAL_PLOTS .. " plots")
diff --git a/public/src/Server/SaplingHandler.server.luau b/public/src/Server/SaplingHandler.server.luau
index e25ffb5..f1af8b8 100644
--- a/public/src/Server/SaplingHandler.server.luau
+++ b/public/src/Server/SaplingHandler.server.luau
@@ -28,7 +28,7 @@ task.spawn(function()
 		else
 			warn("[SaplingHandler] TreeGenerator module not found!")
 		end
-		
+
 		-- Also load LeafDecayManager for tree decay support
 		local leafDecayModule = modulesFolder:WaitForChild("LeafDecayManager", 10)
 		if leafDecayModule then
@@ -55,8 +55,76 @@ local SAPLING_TO_TREE = {
 	["DarkOakSapling"] = "DarkOak",
 	["EbonySapling"] = "Ebony",
 	["RedwoodSapling"] = "Redwood",
+	-- Candy reward sapling (Step 5). Grows into a J-shape candy cane structure
+	-- via a custom path ΓÇö NOT through TreeGenerator/TreeStructures, since candy
+	-- canes are programmatically generated (random height/curl) and use different
+	-- block types (CandyCaneLog + CandyCaneFull) than normal trees.
+	["CandyCaneSapling"] = "CandyCane",
+}
+
+-- Candy cane growth config. All saplings grow into a J-shape with random:
+--   * Height (pole length, 3-8 blocks)
+--   * Curl direction (left or right of the pole, on a random axis)
+--   * Color (one of these 7 candy-flavor colors, same for every block in the cane).
+-- The flavor is rolled at PLACEMENT time and stored on the sapling as the
+-- "CandyCaneFlavor" attribute, so the sapling itself shows the color it'll
+-- grow into. growCandyCane reads the attribute back to ensure the grown cane
+-- matches the sapling's color.
+local CANDY_PALETTE = {
+	{ name = "White",  color = Color3.fromRGB(255, 255, 255) },
+	{ name = "Red",    color = Color3.fromRGB(240, 30, 50) },
+	{ name = "Green",  color = Color3.fromRGB(50, 200, 70) },
+	{ name = "Pink",   color = Color3.fromRGB(255, 110, 185) },
+	{ name = "Purple", color = Color3.fromRGB(165, 70, 230) },
+	{ name = "Orange", color = Color3.fromRGB(255, 130, 25) },
+	{ name = "Yellow", color = Color3.fromRGB(255, 220, 40) },
 }
 
+-- Find a palette entry by flavor name. Returns nil if not found.
+local function getCandyFlavor(name)
+	for _, f in ipairs(CANDY_PALETTE) do
+		if f.name == name then return f end
+	end
+	return nil
+end
+
+-- Apply a candy color to a sapling. If the sapling already has a saved
+-- CandyCaneFlavor attribute (from a previous placement or a save reload),
+-- re-applies that color; otherwise rolls a fresh random flavor, saves it,
+-- and applies. Either way the sapling ends up tinted to its grow-color.
+local function tintCandyCaneSapling(sapling)
+	if not sapling then return end
+	local flavor = getCandyFlavor(sapling:GetAttribute("CandyCaneFlavor"))
+	if not flavor then
+		flavor = CANDY_PALETTE[math.random(1, #CANDY_PALETTE)]
+		sapling:SetAttribute("CandyCaneFlavor", flavor.name)
+	end
+	local color = flavor.color
+	-- The sapling is built like the canes: white "Stripes" over colored
+	-- "Background" geometry. Only tint the Background parts (and any Texture/
+	-- Decal/SurfaceAppearance on them) so the stripes stay white, matching how
+	-- the grown cane will look.
+	local function paint(inst)
+		if inst:IsA("BasePart") then
+			if inst.Name == "Background" then
+				inst.Color = color
+			end
+		elseif inst:IsA("Texture") or inst:IsA("Decal") then
+			if inst.Parent and inst.Parent.Name == "Background" then
+				inst.Color3 = color
+			end
+		elseif inst:IsA("SurfaceAppearance") then
+			if inst.Parent and inst.Parent.Name == "Background" then
+				inst.Color = color
+			end
+		end
+	end
+	paint(sapling)
+	for _, desc in ipairs(sapling:GetDescendants()) do
+		paint(desc)
+	end
+end
+
 -- Saplings that require 2x2 formation to grow (large trees)
 local REQUIRES_2X2 = {
 	["RedwoodSapling"] = true,
@@ -67,7 +135,7 @@ local REQUIRES_2X2 = {
 local plantedSaplings = {}
 
 -- Debug flag
-local DEBUG_SAPLINGS = true
+local DEBUG_SAPLINGS = false
 
 -- ============================================
 -- HELPER FUNCTIONS
@@ -86,15 +154,15 @@ end
 -- Find a sapling of the same type at a specific world position
 local function findSaplingAt(blocksFolder, saplingType, worldX, worldZ, baseY)
 	local tolerance = BLOCK_SIZE * 0.7 -- Generous tolerance for position matching
-	
+
 	for _, block in ipairs(blocksFolder:GetChildren()) do
 		local blockType = block:GetAttribute("BlockType")
 		if blockType == saplingType then
 			local pos = getSaplingPosition(block)
 			if pos then
 				if math.abs(pos.X - worldX) < tolerance and 
-				   math.abs(pos.Z - worldZ) < tolerance and
-				   math.abs(pos.Y - baseY) < BLOCK_SIZE then
+					math.abs(pos.Z - worldZ) < tolerance and
+					math.abs(pos.Y - baseY) < BLOCK_SIZE then
 					return block
 				end
 			end
@@ -107,19 +175,19 @@ end
 -- Returns: isComplete, allSaplings (array of 4 saplings if complete), centerPosition
 local function check2x2Formation(sapling, blocksFolder, buildArea)
 	local saplingType = sapling:GetAttribute("BlockType")
-	
+
 	-- Use ParentBlock position for accurate grid alignment
 	local parentX = sapling:GetAttribute("ParentBlockX")
 	local parentY = sapling:GetAttribute("ParentBlockY")
 	local parentZ = sapling:GetAttribute("ParentBlockZ")
-	
+
 	if not (parentX and parentY and parentZ) then
 		-- Fallback to sapling position if no parent block data
 		local pos = getSaplingPosition(sapling)
 		if not pos then return false, nil, nil end
 		parentX, parentY, parentZ = pos.X, pos.Y - BLOCK_SIZE, pos.Z
 	end
-	
+
 	-- Calculate terrain origin for proper grid snapping
 	local originX, originZ = 0, 0
 	if buildArea then
@@ -127,20 +195,20 @@ local function check2x2Formation(sapling, blocksFolder, buildArea)
 		local posZ = buildArea:GetAttribute("BasePlatePositionZ")
 		local sizeX = buildArea:GetAttribute("BasePlateSizeX")
 		local sizeZ = buildArea:GetAttribute("BasePlateSizeZ")
-		
+
 		if posX and posZ and sizeX and sizeZ then
 			originX = posX - sizeX / 2 + BLOCK_SIZE / 2
 			originZ = posZ - sizeZ / 2 + BLOCK_SIZE / 2
 		end
 	end
-	
+
 	-- Convert parent block position to grid coordinates relative to terrain origin
 	local gridX = math.round((parentX - originX) / BLOCK_SIZE)
 	local gridZ = math.round((parentZ - originZ) / BLOCK_SIZE)
-	
+
 	print(string.format("[SaplingHandler] 2x2 check: sapling parent at grid (%d, %d), origin (%.1f, %.1f)", 
 		gridX, gridZ, originX, originZ))
-	
+
 	-- Check all 4 possible 2x2 arrangements this sapling could be part of
 	-- (this sapling could be in any corner of the 2x2)
 	local offsets = {
@@ -149,24 +217,24 @@ local function check2x2Formation(sapling, blocksFolder, buildArea)
 		{{0, -1}, {1, -1}, {0, 0}, {1, 0}}, -- This sapling is top-left
 		{{-1, -1}, {0, -1}, {-1, 0}, {0, 0}}, -- This sapling is top-right
 	}
-	
+
 	for arrangementIndex, arrangement in ipairs(offsets) do
 		local allFound = true
 		local saplings = {}
 		local minGridX, minGridZ = math.huge, math.huge
-		
+
 		for _, offset in ipairs(arrangement) do
 			local checkGridX = gridX + offset[1]
 			local checkGridZ = gridZ + offset[2]
-			
+
 			-- Track min position for center calculation
 			minGridX = math.min(minGridX, checkGridX)
 			minGridZ = math.min(minGridZ, checkGridZ)
-			
+
 			-- Convert grid position back to world position
 			local worldX = originX + checkGridX * BLOCK_SIZE
 			local worldZ = originZ + checkGridZ * BLOCK_SIZE
-			
+
 			-- Check if this is the original sapling
 			if offset[1] == 0 and offset[2] == 0 then
 				table.insert(saplings, sapling)
@@ -180,7 +248,7 @@ local function check2x2Formation(sapling, blocksFolder, buildArea)
 				end
 			end
 		end
-		
+
 		if allFound and #saplings == 4 then
 			-- Use bottom-left corner position (grid-aligned) as tree base
 			-- BUT: The tree structure has its 2x2 trunk at offsets (-3,-3), (0,-3), (-3,0), (0,0)
@@ -190,14 +258,14 @@ local function check2x2Formation(sapling, blocksFolder, buildArea)
 			-- Y: tree base goes half a block above parent block center (tested value)
 			local cornerY = parentY + BLOCK_SIZE / 2
 			local cornerPos = Vector3.new(cornerX, cornerY, cornerZ)
-			
+
 			print(string.format("[SaplingHandler] 2x2 COMPLETE with arrangement %d! Base: (%.1f, %.1f, %.1f) for grid (%d,%d)",
 				arrangementIndex, cornerX, cornerY, cornerZ, minGridX, minGridZ))
-			
+
 			return true, saplings, cornerPos
 		end
 	end
-	
+
 	print("[SaplingHandler] 2x2 formation NOT complete")
 	return false, nil, nil
 end
@@ -224,7 +292,7 @@ end
 local function buildBlockPositionSet(blocksFolder)
 	local posSet = {}
 	local snapSize = BLOCK_SIZE * 0.5 -- Snap to half-block grid for reliable matching
-	
+
 	for _, block in ipairs(blocksFolder:GetChildren()) do
 		local blockPos
 		if block:IsA("BasePart") then
@@ -233,7 +301,7 @@ local function buildBlockPositionSet(blocksFolder)
 			local storedX = block:GetAttribute("BlockPosX")
 			local storedY = block:GetAttribute("BlockPosY")
 			local storedZ = block:GetAttribute("BlockPosZ")
-			
+
 			if storedX and storedY and storedZ then
 				blockPos = Vector3.new(storedX, storedY, storedZ)
 			else
@@ -243,7 +311,7 @@ local function buildBlockPositionSet(blocksFolder)
 				end
 			end
 		end
-		
+
 		if blockPos then
 			-- Snap to grid for consistent key generation
 			local key = string.format("%d,%d,%d",
@@ -253,7 +321,7 @@ local function buildBlockPositionSet(blocksFolder)
 			posSet[key] = true
 		end
 	end
-	
+
 	return posSet
 end
 
@@ -267,17 +335,272 @@ local function hasBlockAtPositionFast(position, posSet)
 	return posSet[key] == true
 end
 
+-- ============================================
+-- CANDY CANE GROWTH (Step 5)
+-- ============================================
+-- Bespoke path for candy cane saplings. We DON'T go through TreeGenerator/
+-- TreeStructures because:
+--   * Candy canes are programmatically generated (random height + curl), not
+--     authored as fixed structures.
+--   * They use two block types (CandyCaneLog + CandyCaneFull) that aren't in
+--     the normal Blocks folder ΓÇö they're variant presets in the Candy reward
+--     set, with stripe colors tinted via Texture-mode.
+--   * The pole's horizontal-curl piece needs a non-Y log axis, which would
+--     require special structure character support.
+
+-- Locate the Candy reward set folder once, cached for reuse.
+local CandyBlocksFolder = nil
+local function getCandyBlocksFolder()
+	if CandyBlocksFolder and CandyBlocksFolder.Parent then return CandyBlocksFolder end
+	local rewardSets = ReplicatedStorage:FindFirstChild("RewardSets")
+	CandyBlocksFolder = rewardSets and rewardSets:FindFirstChild("CandyBlocks") or nil
+	return CandyBlocksFolder
+end
+
+-- Clone the given template (CandyCaneLog or CandyCaneFull) from the Candy set,
+-- tint its Texture descendants and any Part named "Core" to the variant color,
+-- and return the new block. Mirrors BlockHandler's Texture-mode tinting branch.
+local function buildCandyBlock(templateName, color)
+	local folder = getCandyBlocksFolder()
+	if not folder then
+		warn("[SaplingHandler] CandyBlocks folder not found in ReplicatedStorage.RewardSets")
+		return nil
+	end
+	local template = folder:FindFirstChild(templateName)
+	if not template then
+		warn("[SaplingHandler] Template missing in CandyBlocks: " .. templateName)
+		return nil
+	end
+	
+	local block = template:Clone()
+	
+	-- Anchor everything and tint stripes + Core to the chosen color.
+	local function tintAndAnchor(part)
+		if part:IsA("BasePart") then
+			part.Anchored = true
+			if part.Name == "Core" then
+				part.Color = color
+			end
+		end
+	end
+	if block:IsA("BasePart") then
+		tintAndAnchor(block)
+		for _, child in ipairs(block:GetChildren()) do
+			if child:IsA("Texture") or child:IsA("Decal") then
+				child.Color3 = color
+			end
+		end
+	elseif block:IsA("Model") then
+		for _, desc in ipairs(block:GetDescendants()) do
+			tintAndAnchor(desc)
+			if desc:IsA("Texture") or desc:IsA("Decal") then
+				desc.Color3 = color
+			end
+		end
+	end
+	
+	return block
+end
+
+-- Compute the offset from a model's pivot to its visual bounding-box center,
+-- in the model's LOCAL frame. We use this so we can place bottom-pivoted (or
+-- otherwise off-center-pivoted) models such that their visual center ends up
+-- exactly at the desired world position ΓÇö even if the model author put the
+-- pivot somewhere weird relative to the geometry. Rotates correctly with the
+-- model too, since it's a local-space offset.
+local function computeModelCenterOffset(model)
+	if not model or not model:IsA("Model") then
+		return Vector3.new(0, BLOCK_SIZE / 2, 0) -- fallback: assume bottom-pivot
+	end
+	local pivot = model:GetPivot()
+	local bbCframe = model:GetBoundingBox()
+	-- bbCframe.Position is the bb center in world space (with model in its
+	-- natural pose). PointToObjectSpace converts it to the pivot's local frame.
+	return pivot:PointToObjectSpace(bbCframe.Position)
+end
+
+-- One-time cache for the candy log's center-vs-pivot offset. Resolved on first
+-- candy growth (templates aren't guaranteed to exist at script load).
+local candyLogCenterOffsetCache = nil
+local function getCandyLogCenterOffset()
+	if candyLogCenterOffsetCache then return candyLogCenterOffsetCache end
+	local folder = getCandyBlocksFolder()
+	local template = folder and folder:FindFirstChild("CandyCaneLog")
+	candyLogCenterOffsetCache = computeModelCenterOffset(template)
+	print(string.format("[SaplingHandler] CandyCaneLog center-vs-pivot offset: (%.2f, %.2f, %.2f)",
+		candyLogCenterOffsetCache.X, candyLogCenterOffsetCache.Y, candyLogCenterOffsetCache.Z))
+	return candyLogCenterOffsetCache
+end
+
+-- Place a candy block at the given world position with optional log axis.
+-- logAxis: "X", "Y", or "Z" ΓÇö only meaningful for CandyCaneLog. Horizontal logs
+-- in the curl get "X" or "Z"; the vertical pole gets "Y" (default).
+local function placeCandyBlock(templateName, variantId, color, position, logAxis, blocksFolder, placedBy)
+	local block = buildCandyBlock(templateName, color)
+	if not block then return nil end
+	
+	block.Name = variantId .. "_" .. math.random(100000, 999999)
+	
+	-- Candy templates use TWO different pivot conventions, and we have to
+	-- account for both to get the J-shape's pieces flush with each other:
+	--
+	--   * CandyCaneLog (Model) is BOTTOM-PIVOTED. Its pivot sits at the bottom
+	--     face of the visual cylinder. For vertical placement we drop the pivot
+	--     half a block below the target so the model extends UP to be centered.
+	--     For HORIZONTAL placement the rotation re-orients which way "up" points
+	--     in world space, so the half-block offset must align with the rotated
+	--     extend direction (┬▒X or ┬▒Z) instead of ΓÇôY.
+	--
+	--   * CandyCaneFull (BasePart Union) is CENTER-PIVOTED. Its CFrame sits at
+	--     the visual center of the cube, so no offset is needed at allΓÇöjust
+	--     place the CFrame directly at the target position.
+	local rotation = CFrame.new()
+	if logAxis == "X" then
+		rotation = CFrame.Angles(0, 0, math.rad(90))
+	elseif logAxis == "Z" then
+		rotation = CFrame.Angles(math.rad(90), 0, 0)
+	end
+	
+	if block:IsA("BasePart") then
+		-- Center-pivoted: CFrame = position centers the block on `position`.
+		block.CFrame = CFrame.new(position) * rotation
+	elseif block:IsA("Model") then
+		-- Compute pivot position such that the model's visual bounding-box
+		-- center ends up at world `position` after rotation. The local-space
+		-- offset between pivot and visual center is constant for the template,
+		-- so we cache it once and rotate it into world space here for each
+		-- placement. This auto-adapts to whatever pivot the model was authored
+		-- with ΓÇö no hardcoded BLOCK_SIZE/2 assumptions, no magic floating fudge.
+		local localCenterOffset = getCandyLogCenterOffset()
+		local worldCenterOffset = rotation:VectorToWorldSpace(localCenterOffset)
+		block:PivotTo(CFrame.new(position - worldCenterOffset) * rotation)
+	end
+	
+	-- Attributes that the save/load + spatial-index systems expect.
+	block:SetAttribute("BlockType", variantId)
+	block:SetAttribute("BlockPosX", position.X)
+	block:SetAttribute("BlockPosY", position.Y)
+	block:SetAttribute("BlockPosZ", position.Z)
+	block:SetAttribute("PlacedBy", placedBy)
+	block:SetAttribute("GrownFromSapling", true)
+	block:SetAttribute("IsTreeBlock", true)
+	if logAxis and templateName == "CandyCaneLog" then
+		block:SetAttribute("LogAxis", logAxis)
+	end
+	
+	block.Parent = blocksFolder
+	return block
+end
+
+-- Grow a candy cane from the sapling's position. Returns true on success.
+-- The structure (Option B from chat plan): a J-shape made of N+4 blocks.
+--   * Vertical pole: N CandyCaneLog blocks (N = random 3-8), all CandyCaneLog
+--     (including the base, which sits on the ground so its swirl is hidden).
+--   * Top corner (pole-top + 1 in Y): CandyCaneFull ΓÇö the bend where the pole
+--     meets the curl. Full because the chain changes axis here.
+--   * Horizontal middle: CandyCaneLog, oriented on the curl axis, placed 1
+--     block in the curl direction. Log because both its ends are covered
+--     (top corner on one side, right corner on the other).
+--   * Right corner: CandyCaneFull, placed 1 block further in the curl direction
+--     (= 2 blocks from the top corner). Covers the horizontal log's far end
+--     AND the stub's top ΓÇö without this, the log's swirl shows through and
+--     the cane looks janky.
+--   * Stub: CandyCaneFull, 1 block DOWN from the right corner. The tip of the
+--     J: its bottom face is exposed (nothing below), so Full for stripes
+--     on all faces.
+-- Plan view of the curl (looking down at the top two layers, curl going +X):
+--   Y=N:   F L F      <- top corner, horizontal log, right corner
+--   Y=N-1: P . F      <- pole top, gap, stub
+local function growCandyCane(sapling, treeBasePos, blocksFolder)
+	local placedBy = sapling:GetAttribute("PlacedBy")
+	
+	-- The CandyCaneLog model's pivot is ~0.1 studs below its visual bottom face,
+	-- so the cane sat half a block too low. Net offset: -1.6 + 1.5 = -0.1 keeps
+	-- the pole base flush on grass.
+	treeBasePos = treeBasePos - Vector3.new(0, 0.1, 0)
+	
+	-- Roll randomness
+	local height = math.random(3, 8)
+	-- Color: prefer the flavor that was already chosen when the sapling was
+	-- planted (stored on the sapling so it visually matched the grow-color).
+	-- Fall back to a fresh random roll if the attribute is missing.
+	local flavor = getCandyFlavor(sapling:GetAttribute("CandyCaneFlavor"))
+		 or CANDY_PALETTE[math.random(1, #CANDY_PALETTE)]
+	local color = flavor.color
+	local variantSuffix = "_" .. flavor.name
+	-- Curl direction: random +X, -X, +Z, or -Z. The chosen axis is the curl axis;
+	-- the horizontal-log gets LogAxis = that axis.
+	local directions = {
+		{ axis = "X", offset = Vector3.new(BLOCK_SIZE, 0, 0) },
+		{ axis = "X", offset = Vector3.new(-BLOCK_SIZE, 0, 0) },
+		{ axis = "Z", offset = Vector3.new(0, 0, BLOCK_SIZE) },
+		{ axis = "Z", offset = Vector3.new(0, 0, -BLOCK_SIZE) },
+	}
+	local dir = directions[math.random(1, #directions)]
+	
+	print(string.format("[SaplingHandler] Growing candy cane: %s, height=%d, curlAxis=%s, curlDir=(%.0f,%.0f,%.0f)",
+		flavor.name, height, dir.axis, dir.offset.X, dir.offset.Y, dir.offset.Z))
+	
+	local placedAny = false
+	
+	-- 1. Pole ΓÇö N vertical CandyCaneLog blocks
+	for i = 0, height - 1 do
+		local pos = treeBasePos + Vector3.new(0, i * BLOCK_SIZE, 0)
+		local blk = placeCandyBlock(
+			"CandyCaneLog",
+			"CandyCaneLog" .. variantSuffix,
+			color, pos, "Y", blocksFolder, placedBy)
+		if blk then placedAny = true end
+	end
+	
+	-- 2. Top corner ΓÇö CandyCaneFull at pole-top + 1 in Y
+	local topCornerPos = treeBasePos + Vector3.new(0, height * BLOCK_SIZE, 0)
+	placeCandyBlock(
+		"CandyCaneFull",
+		"CandyCaneFull" .. variantSuffix,
+		color, topCornerPos, nil, blocksFolder, placedBy)
+	
+	-- 3. Horizontal middle ΓÇö CandyCaneLog, oriented on curl axis, 1 block in
+	--    the curl direction from the top corner. Both ends covered (by top
+	--    corner and right corner) so the log's swirl stays hidden.
+	local curlMidPos = topCornerPos + dir.offset
+	placeCandyBlock(
+		"CandyCaneLog",
+		"CandyCaneLog" .. variantSuffix,
+		color, curlMidPos, dir.axis, blocksFolder, placedBy)
+	
+	-- 4. Right corner ΓÇö CandyCaneFull, 1 block further in the curl direction
+	--    from the horizontal log. Covers the log's far end and sits above
+	--    the stub.
+	local rightCornerPos = curlMidPos + dir.offset
+	placeCandyBlock(
+		"CandyCaneFull",
+		"CandyCaneFull" .. variantSuffix,
+		color, rightCornerPos, nil, blocksFolder, placedBy)
+	
+	-- 5. Stub ΓÇö CandyCaneFull, 1 block down from the right corner. This is
+	--    the tip of the J: bottom face exposed, so Full for stripes-everywhere.
+	local stubPos = rightCornerPos - Vector3.new(0, BLOCK_SIZE, 0)
+	placeCandyBlock(
+		"CandyCaneFull",
+		"CandyCaneFull" .. variantSuffix,
+		color, stubPos, nil, blocksFolder, placedBy)
+	
+	print("[SaplingHandler] Candy cane grown successfully")
+	return placedAny
+end
+
 -- Grow a sapling into a tree
 local function growSapling(sapling, data)
 	print("[SaplingHandler] ========== GROW SAPLING CALLED ==========")
-	
+
 	if not TreeGenerator then
 		warn("[SaplingHandler] Cannot grow sapling - TreeGenerator not loaded!")
 		warn("[SaplingHandler] Check that ServerScriptService.Modules.TreeGenerator exists")
 		return false
 	end
 	print("[SaplingHandler] TreeGenerator is loaded Γ£ô")
-	
+
 	local blocksFolder = getBlocksFolder(sapling)
 	if not blocksFolder then
 		warn("[SaplingHandler] Cannot find blocks folder for sapling")
@@ -285,7 +608,7 @@ local function growSapling(sapling, data)
 		return false
 	end
 	print("[SaplingHandler] Blocks folder found: " .. blocksFolder:GetFullName())
-	
+
 	local buildArea = getBuildArea(sapling)
 	local saplingPos = getSaplingPosition(sapling)
 	if not saplingPos then
@@ -293,7 +616,7 @@ local function growSapling(sapling, data)
 		return false
 	end
 	print(string.format("[SaplingHandler] Sapling position: (%.1f, %.1f, %.1f)", saplingPos.X, saplingPos.Y, saplingPos.Z))
-	
+
 	-- Get tree type from sapling
 	local treeType = data.treeType
 	if not treeType then
@@ -301,44 +624,62 @@ local function growSapling(sapling, data)
 		return false
 	end
 	print("[SaplingHandler] Tree type: " .. treeType)
-	
+
 	-- Check if this sapling requires 2x2 formation
 	local blockType = sapling:GetAttribute("BlockType")
 	local saplingsToRemove = {sapling}
-	
+
 	-- Get grid-aligned position for tree base
 	-- Saplings are decorations - use ParentBlock attributes for accurate grid position
 	local parentX = sapling:GetAttribute("ParentBlockX")
 	local parentY = sapling:GetAttribute("ParentBlockY")
 	local parentZ = sapling:GetAttribute("ParentBlockZ")
-	
+
 	local treeBasePos
-	
+
 	if parentX and parentY and parentZ then
-		-- Use stored parent block position (grid-aligned)
-		-- ParentBlock Y is the block CENTER, tree base Y goes one full block above
-		treeBasePos = Vector3.new(parentX, parentY + BLOCK_SIZE, parentZ)
-		print(string.format("[SaplingHandler] Using ParentBlock position: (%.1f, %.1f, %.1f) -> tree base: (%.1f, %.1f, %.1f)", 
-			parentX, parentY, parentZ, treeBasePos.X, treeBasePos.Y, treeBasePos.Z))
+		-- ParentBlock attributes record where the sapling was placed in WORLD
+		-- coordinates, but that's not always the parent block's grid center ΓÇö
+		-- decorations can land at offset positions on top of a block, and the
+		-- parent block itself may not be perfectly grid-aligned. Snap X/Z to
+		-- the build area's grid so trees always grow centered on the cell they
+		-- were planted on, regardless of where the sapling visually sits.
+		local snappedX, snappedZ = parentX, parentZ
+		if buildArea then
+			local posX = buildArea:GetAttribute("BasePlatePositionX")
+			local posZ = buildArea:GetAttribute("BasePlatePositionZ")
+			local sizeX = buildArea:GetAttribute("BasePlateSizeX")
+			local sizeZ = buildArea:GetAttribute("BasePlateSizeZ")
+			if posX and posZ and sizeX and sizeZ then
+				local originX = posX - sizeX / 2 + BLOCK_SIZE / 2
+				local originZ = posZ - sizeZ / 2 + BLOCK_SIZE / 2
+				snappedX = originX + math.round((parentX - originX) / BLOCK_SIZE) * BLOCK_SIZE
+				snappedZ = originZ + math.round((parentZ - originZ) / BLOCK_SIZE) * BLOCK_SIZE
+			end
+		end
+		-- ParentBlock Y is the block CENTER; tree base Y goes one full block above.
+		treeBasePos = Vector3.new(snappedX, parentY + BLOCK_SIZE, snappedZ)
+		print(string.format("[SaplingHandler] ParentBlock (%.1f, %.1f, %.1f) snapped to (%.1f, _, %.1f) -> tree base: (%.1f, %.1f, %.1f)", 
+			parentX, parentY, parentZ, snappedX, snappedZ, treeBasePos.X, treeBasePos.Y, treeBasePos.Z))
 	elseif buildArea then
 		-- Fallback: snap current position to terrain grid
 		local posX = buildArea:GetAttribute("BasePlatePositionX")
 		local posZ = buildArea:GetAttribute("BasePlatePositionZ")
 		local sizeX = buildArea:GetAttribute("BasePlateSizeX")
 		local sizeZ = buildArea:GetAttribute("BasePlateSizeZ")
-		
+
 		if posX and posZ and sizeX and sizeZ then
 			-- Calculate terrain origin (same as TerrainGenerator/PlotSerializer)
 			local originX = posX - sizeX / 2 + BLOCK_SIZE / 2
 			local originZ = posZ - sizeZ / 2 + BLOCK_SIZE / 2
-			
+
 			-- Snap to nearest grid position relative to terrain origin
 			local gridIndexX = math.round((saplingPos.X - originX) / BLOCK_SIZE)
 			local gridIndexZ = math.round((saplingPos.Z - originZ) / BLOCK_SIZE)
-			
+
 			local snappedX = originX + gridIndexX * BLOCK_SIZE
 			local snappedZ = originZ + gridIndexZ * BLOCK_SIZE
-			
+
 			treeBasePos = Vector3.new(snappedX, saplingPos.Y, snappedZ)
 			print(string.format("[SaplingHandler] Snapped to terrain grid: (%.1f, %.1f, %.1f) from origin (%.1f, %.1f)", 
 				treeBasePos.X, treeBasePos.Y, treeBasePos.Z, originX, originZ))
@@ -360,35 +701,52 @@ local function growSapling(sapling, data)
 		)
 		print(string.format("[SaplingHandler] Basic snap (no buildArea): (%.1f, %.1f, %.1f)", treeBasePos.X, treeBasePos.Y, treeBasePos.Z))
 	end
-	
+
 	if REQUIRES_2X2[blockType] then
 		print("[SaplingHandler] This sapling requires 2x2 formation...")
 		local isComplete, allSaplings, centerPos = check2x2Formation(sapling, blocksFolder, buildArea)
-		
+
 		if not isComplete then
 			print("[SaplingHandler] 2x2 formation not complete - need 4 saplings in a square")
 			return false -- Will retry later
 		end
-		
+
 		print(string.format("[SaplingHandler] 2x2 formation complete! Center at (%.1f, %.1f, %.1f)", 
 			centerPos.X, centerPos.Y, centerPos.Z))
-		
+
 		saplingsToRemove = allSaplings
 		treeBasePos = centerPos
 	end
-	
+
 	print(string.format("[SaplingHandler] Tree base position: (%.1f, %.1f, %.1f)", treeBasePos.X, treeBasePos.Y, treeBasePos.Z))
 	
+	-- CANDY CANE: bespoke path ΓÇö doesn't go through TreeGenerator at all.
+	if treeType == "CandyCane" then
+		local placedBy = sapling:GetAttribute("PlacedBy")
+		local ok = growCandyCane(sapling, treeBasePos, blocksFolder)
+		if ok then
+			-- Destroy the sapling on success.
+			for _, sap in ipairs(saplingsToRemove) do
+				plantedSaplings[sap] = nil
+				sap:Destroy()
+			end
+			return true
+		else
+			warn("[SaplingHandler] Candy cane growth failed")
+			return false
+		end
+	end
+
 	-- Get biome and season from build area for leaf coloring
 	local biomeName = buildArea and buildArea:GetAttribute("CurrentBiome") or nil
 	local seasonName = buildArea and buildArea:GetAttribute("CurrentSeason") or nil
 	print(string.format("[SaplingHandler] Biome: %s, Season: %s", tostring(biomeName), tostring(seasonName)))
-	
+
 	-- Get tree blocks using TreeGenerator
 	-- Pass true for skipDistanceCheck since player saplings can grow near other trees
 	print("[SaplingHandler] Calling TreeGenerator.GetTreeBlocks...")
 	local treeBlocks = TreeGenerator.GetTreeBlocks(treeBasePos, treeType, blocksFolder, biomeName, seasonName, true)
-	
+
 	if not treeBlocks then
 		warn(string.format("[SaplingHandler] TreeGenerator.GetTreeBlocks RETURNED NIL for %s at (%.1f, %.1f, %.1f)", 
 			treeType, treeBasePos.X, treeBasePos.Y, treeBasePos.Z))
@@ -396,10 +754,10 @@ local function growSapling(sapling, data)
 		return false
 	end
 	print(string.format("[SaplingHandler] TreeGenerator returned %d blocks", #treeBlocks))
-	
+
 	-- Store who planted the sapling for attribution
 	local placedBy = sapling:GetAttribute("PlacedBy")
-	
+
 	-- Destroy all saplings (handles both single and 2x2)
 	print(string.format("[SaplingHandler] Destroying %d sapling(s)...", #saplingsToRemove))
 	for _, sap in ipairs(saplingsToRemove) do
@@ -407,10 +765,10 @@ local function growSapling(sapling, data)
 		plantedSaplings[sap] = nil
 		sap:Destroy()
 	end
-	
+
 	-- Build a fast spatial lookup of existing blocks ONCE (O(n) instead of O(n) per leaf)
 	local existingBlockSet = buildBlockPositionSet(blocksFolder)
-	
+
 	-- Build a set of log positions from THIS tree for fast overlap checks
 	local logPosSet = {}
 	local snapSize = BLOCK_SIZE * 0.5
@@ -423,11 +781,11 @@ local function growSapling(sapling, data)
 			logPosSet[key] = true
 		end
 	end
-	
+
 	-- Spawn the tree blocks (logs first, then leaves)
 	local blocksCreated = 0
 	local YIELD_EVERY = 10 -- Yield every N blocks to prevent frame freezes
-	
+
 	-- Create logs
 	for _, blockData in ipairs(treeBlocks) do
 		if blockData.blockType == "L" then
@@ -442,7 +800,7 @@ local function growSapling(sapling, data)
 			end
 		end
 	end
-	
+
 	-- Create leaves
 	for _, blockData in ipairs(treeBlocks) do
 		if blockData.blockType == "l" then
@@ -454,12 +812,12 @@ local function growSapling(sapling, data)
 			if logPosSet[key] then
 				continue
 			end
-			
+
 			-- Skip if there's already a block at this position (O(1) lookup)
 			if hasBlockAtPositionFast(blockData.position, existingBlockSet) then
 				continue
 			end
-			
+
 			local leaves = TreeGenerator.CreateLeaves(
 				blockData.position, 
 				blocksFolder, 
@@ -478,16 +836,16 @@ local function growSapling(sapling, data)
 			end
 		end
 	end
-	
+
 	print(string.format("[SaplingHandler] SUCCESS! Grew %s tree with %d blocks at (%.1f, %.1f, %.1f)", 
 		treeType, blocksCreated, treeBasePos.X, treeBasePos.Y, treeBasePos.Z))
 	print("[SaplingHandler] ========================================")
-	
+
 	-- Initialize leaf decay for this build area so leaves can decay when logs are broken
 	if LeafDecayManager and buildArea then
 		LeafDecayManager.Initialize(buildArea)
 	end
-	
+
 	return true
 end
 
@@ -504,47 +862,54 @@ function SaplingHandler.ScanBuildAreaForSaplings(buildArea, restartTimers)
 		warn("[SaplingHandler] ScanBuildAreaForSaplings: buildArea is nil")
 		return 0 
 	end
-	
+
 	local blocksFolder = buildArea:FindFirstChild("Blocks")
 	if not blocksFolder then 
 		print("[SaplingHandler] ScanBuildAreaForSaplings: No Blocks folder in " .. buildArea.Name)
 		return 0 
 	end
-	
+
 	local registered = 0
 	local totalBlocks = #blocksFolder:GetChildren()
-	
+
 	print(string.format("[SaplingHandler] Scanning %s for saplings (%d total blocks)...", buildArea.Name, totalBlocks))
-	
+
 	for _, block in ipairs(blocksFolder:GetChildren()) do
 		local blockType = block:GetAttribute("BlockType")
-		
+
 		-- Check if it's a sapling by BlockType (primary method)
 		local treeType = SAPLING_TO_TREE[blockType]
-		
+
 		-- Also check IsSapling attribute as fallback
 		if not treeType and block:GetAttribute("IsSapling") then
 			treeType = block:GetAttribute("TreeType")
 		end
-		
+
 		if treeType then
 			-- Skip if already tracked
 			if plantedSaplings[block] then 
 				print(string.format("[SaplingHandler] Skipping already-tracked sapling: %s", blockType or "unknown"))
 				continue 
 			end
-			
+
 			-- Mark as sapling if not already
 			block:SetAttribute("IsSapling", true)
 			block:SetAttribute("TreeType", treeType)
 			
+			-- Candy cane saplings: re-apply the visual color from their saved
+			-- CandyCaneFlavor attribute. If no saved flavor (legacy save), rolls
+			-- a new one and stores it.
+			if blockType == "CandyCaneSapling" then
+				tintCandyCaneSapling(block)
+			end
+
 			local startTime
-			
+
 			if restartTimers then
 				-- Restart timer from scratch
 				block:SetAttribute("PlantedTime", os.time())
 				startTime = tick()
-				
+
 				print(string.format("[SaplingHandler] Registered loaded %s sapling, timer restarted (grows in %d seconds)", 
 					blockType or "unknown", GROWTH_TIME))
 			else
@@ -554,32 +919,32 @@ function SaplingHandler.ScanBuildAreaForSaplings(buildArea, restartTimers)
 					local age = os.time() - plantedTime
 					local remainingTime = math.max(0, GROWTH_TIME - age)
 					startTime = tick() - (GROWTH_TIME - remainingTime)
-					
+
 					print(string.format("[SaplingHandler] Restored sapling timer (grows in %.1f seconds)", remainingTime))
 				else
 					-- No planted time saved, restart timer
 					block:SetAttribute("PlantedTime", os.time())
 					startTime = tick()
-					
+
 					print(string.format("[SaplingHandler] Registered loaded sapling, no saved time (grows in %d seconds)", GROWTH_TIME))
 				end
 			end
-			
+
 			plantedSaplings[block] = {
 				plantedTime = startTime,
 				treeType = treeType,
 			}
-			
+
 			registered = registered + 1
 		end
 	end
-	
+
 	if registered > 0 then
 		print(string.format("[SaplingHandler] Scanned %s - found and registered %d saplings", buildArea.Name, registered))
 	else
 		print(string.format("[SaplingHandler] Scanned %s - no saplings found", buildArea.Name))
 	end
-	
+
 	return registered
 end
 
@@ -589,7 +954,7 @@ function SaplingHandler.RegisterSapling(sapling)
 		warn("[SaplingHandler] RegisterSapling called with nil sapling!")
 		return
 	end
-	
+
 	-- Check if already tracked
 	if plantedSaplings[sapling] then
 		if DEBUG_SAPLINGS then
@@ -597,15 +962,15 @@ function SaplingHandler.RegisterSapling(sapling)
 		end
 		return
 	end
-	
+
 	local blockType = sapling:GetAttribute("BlockType")
 	local treeType = SAPLING_TO_TREE[blockType]
-	
+
 	if DEBUG_SAPLINGS then
 		print(string.format("[SaplingHandler] RegisterSapling called - BlockType: %s, TreeType: %s", 
 			tostring(blockType), tostring(treeType)))
 	end
-	
+
 	if not treeType then
 		if DEBUG_SAPLINGS then
 			print("[SaplingHandler] Unknown sapling type: " .. tostring(blockType))
@@ -613,24 +978,31 @@ function SaplingHandler.RegisterSapling(sapling)
 		end
 		return
 	end
-	
+
 	-- Mark as sapling
 	sapling:SetAttribute("IsSapling", true)
 	sapling:SetAttribute("TreeType", treeType)
 	sapling:SetAttribute("PlantedTime", os.time())
 	
+	-- Candy cane saplings: roll a random flavor color and apply it visually.
+	-- The flavor is stored on the sapling, so growCandyCane will pick the same
+	-- color when growth happens ΓÇö the visual sapling matches the grown cane.
+	if blockType == "CandyCaneSapling" then
+		tintCandyCaneSapling(sapling)
+	end
+
 	-- Track the sapling
 	plantedSaplings[sapling] = {
 		plantedTime = tick(),
 		treeType = treeType,
 	}
-	
+
 	-- Count total tracked
 	local count = 0
 	for _ in pairs(plantedSaplings) do
 		count = count + 1
 	end
-	
+
 	if DEBUG_SAPLINGS then
 		local pos = getSaplingPosition(sapling)
 		print(string.format("[SaplingHandler] SUCCESS! Registered %s sapling (grows %s tree) at (%.1f, %.1f, %.1f)", 
@@ -669,19 +1041,19 @@ local lastDebugTime = tick()
 
 RunService.Heartbeat:Connect(function()
 	local currentTime = tick()
-	
+
 	-- Only check every GROWTH_CHECK_INTERVAL seconds
 	if currentTime - lastCheckTime < GROWTH_CHECK_INTERVAL then
 		return
 	end
 	lastCheckTime = currentTime
-	
+
 	-- Count tracked saplings for debug
 	local saplingCount = 0
 	for _ in pairs(plantedSaplings) do
 		saplingCount = saplingCount + 1
 	end
-	
+
 	-- Print debug status every 5 seconds (reduced for debugging)
 	if DEBUG_SAPLINGS and currentTime - lastDebugTime >= 5 then
 		lastDebugTime = currentTime
@@ -693,7 +1065,7 @@ RunService.Heartbeat:Connect(function()
 			print(string.format("[SaplingHandler]   - %s: age=%.1f/%d, parent=%s", sapData.treeType, age, GROWTH_TIME, parentName))
 		end
 	end
-	
+
 	-- Check each sapling for growth
 	for sapling, data in pairs(plantedSaplings) do
 		-- Check if sapling still exists
@@ -704,10 +1076,10 @@ RunService.Heartbeat:Connect(function()
 			plantedSaplings[sapling] = nil
 			continue
 		end
-		
+
 		-- Check if enough time has passed
 		local age = currentTime - data.plantedTime
-		
+
 		if DEBUG_SAPLINGS and saplingCount > 0 then
 			-- Only print once per sapling per 10 seconds
 			if currentTime - lastDebugTime < 1 then
@@ -716,16 +1088,16 @@ RunService.Heartbeat:Connect(function()
 					data.treeType, age, GROWTH_TIME))
 			end
 		end
-		
+
 		if age >= GROWTH_TIME then
 			if DEBUG_SAPLINGS then
 				print(string.format("[SaplingHandler] Sapling ready to grow! Type: %s, Age: %.1f", 
 					data.treeType, age))
 			end
-			
+
 			-- Try to grow the sapling
 			local success = growSapling(sapling, data)
-			
+
 			if success then
 				-- Remove from tracking (sapling was destroyed)
 				plantedSaplings[sapling] = nil
@@ -749,37 +1121,35 @@ end)
 -- NOTE: We wait longer to ensure saves have fully loaded
 task.spawn(function()
 	task.wait(10) -- Wait 10 seconds for game and saves to fully load
-	
-	print("[SaplingHandler] Running startup scan for existing saplings...")
+
 	local totalFound = 0
-	
+
 	for i = 1, GridConfig.TOTAL_PLOTS do
 		local buildArea = workspace:FindFirstChild(GridConfig.BUILD_AREA_PREFIX .. i)
 		if buildArea then
 			local blocksFolder = buildArea:FindFirstChild("Blocks")
 			if blocksFolder then
 				local blockCount = #blocksFolder:GetChildren()
-				print(string.format("[SaplingHandler] Startup scan: Plot %d has %d blocks", i, blockCount))
-				
+
 				for _, block in ipairs(blocksFolder:GetChildren()) do
 					local blockType = block:GetAttribute("BlockType")
 					local treeType = SAPLING_TO_TREE[blockType]
-					
+
 					-- Also check IsSapling attribute
 					if not treeType and block:GetAttribute("IsSapling") then
 						treeType = block:GetAttribute("TreeType")
 					end
-					
+
 					if treeType then
 						-- Skip if already tracked
 						if plantedSaplings[block] then continue end
-						
+
 						local plantedTime = block:GetAttribute("PlantedTime")
-						
+
 						-- Calculate how much time has passed since planted
 						local age = plantedTime and (os.time() - plantedTime) or 0
 						local remainingTime = math.max(0, GROWTH_TIME - age)
-						
+
 						-- Mark attributes
 						block:SetAttribute("IsSapling", true)
 						block:SetAttribute("TreeType", treeType)
@@ -788,20 +1158,23 @@ task.spawn(function()
 							remainingTime = GROWTH_TIME
 						end
 						
+						-- Candy cane saplings: re-apply visual color from saved flavor.
+						if blockType == "CandyCaneSapling" then
+							tintCandyCaneSapling(block)
+						end
+
 						plantedSaplings[block] = {
 							plantedTime = tick() - (GROWTH_TIME - remainingTime),
 							treeType = treeType,
 						}
-						
+
 						totalFound = totalFound + 1
-						print(string.format("[SaplingHandler] Startup: Found %s sapling in plot %d (grows in %.1f seconds)", 
-							blockType, i, remainingTime))
 					end
 				end
 			end
 		end
 	end
-	
+
 	-- Also scan MegaPlot build area (for private server MegaPlot mode)
 	local megaPlotRoom = workspace:FindFirstChild("MegaPlotRoom")
 	if megaPlotRoom then
@@ -825,19 +1198,20 @@ task.spawn(function()
 							block:SetAttribute("PlantedTime", os.time())
 							remainingTime = GROWTH_TIME
 						end
+						if blockType == "CandyCaneSapling" then
+							tintCandyCaneSapling(block)
+						end
 						plantedSaplings[block] = {
 							plantedTime = tick() - (GROWTH_TIME - remainingTime),
 							treeType = treeType,
 						}
 						totalFound = totalFound + 1
-						print(string.format("[SaplingHandler] Startup: Found %s sapling in MegaPlot (grows in %.1f seconds)", blockType, remainingTime))
 					end
 				end
 			end
 		end
 	end
-	
-	print(string.format("[SaplingHandler] Startup scan complete - found %d saplings total", totalFound))
+
 end)
 
 -- Make module accessible via _G and BindableFunction
@@ -855,8 +1229,4 @@ RegisterSaplingBindable.OnInvoke = function(sapling)
 	return true
 end
 
-print("[SaplingHandler] Initialized - Saplings grow after " .. GROWTH_TIME .. " seconds")
-print("[SaplingHandler] _G.SaplingHandler set:", _G.SaplingHandler ~= nil)
-print("[SaplingHandler] RegisterSapling BindableFunction created:", ReplicatedStorage:FindFirstChild("RegisterSapling") ~= nil)
-
 return SaplingHandler
diff --git a/public/src/Server/SaveManager.server.luau b/public/src/Server/SaveManager.server.luau
index c99e834..792d8fb 100644
--- a/public/src/Server/SaveManager.server.luau
+++ b/public/src/Server/SaveManager.server.luau
@@ -27,7 +27,6 @@ local BetaConfig = require(ReplicatedStorage:WaitForChild("Config"):WaitForChild
 
 -- Unique instance ID for debugging duplicate script detection
 local INSTANCE_ID = math.random(100000, 999999)
-print("[SaveManager] Script instance starting, ID:", INSTANCE_ID)
 
 -- Configuration
 local DATASTORE_NAME = "PlotSaves_v2"  -- New datastore for multi-slot system
@@ -48,6 +47,21 @@ local SlabPlacement = require(ReplicatedStorage:WaitForChild("SlabPlacement", 10
 local StairPlacement = require(ReplicatedStorage:WaitForChild("StairPlacement", 10))
 local FencePlacement = require(ReplicatedStorage:WaitForChild("FencePlacement", 10))
 local GlassPlacement = require(ReplicatedStorage:WaitForChild("GlassPlacement", 10))
+
+-- Resolve a glass-style block's template Model from its type + generic shape name.
+-- Mirrors the same-named helper in BlockHandler. Standard glass lives in
+-- Decorations/Glass; glass-style reward sets (CandyLattice) live in their
+-- RewardSets folder. GlassPlacement.ResolveTemplate is the single source of
+-- truth for folder + model name per type. Returns the template Instance or nil.
+local function resolveGlassTemplate(glassType, genericTemplateName)
+	local folderPath, modelName = GlassPlacement.ResolveTemplate(glassType, genericTemplateName)
+	local folder = ReplicatedStorage
+	for _, folderName in ipairs(string.split(folderPath, "/")) do
+		folder = folder and folder:FindFirstChild(folderName)
+	end
+	if not folder then return nil end
+	return folder:FindFirstChild(modelName)
+end
 local LadderPlacement = require(ReplicatedStorage:WaitForChild("LadderPlacement", 10))
 local TorchPlacement = require(ReplicatedStorage:WaitForChild("TorchPlacement", 10))
 local LanternPlacement = require(ReplicatedStorage:WaitForChild("LanternPlacement", 10))
@@ -61,7 +75,7 @@ local ColorPalette = require(ReplicatedStorage:WaitForChild("Config"):WaitForChi
 local BulkOperationFlags = require(ReplicatedStorage:WaitForChild("BulkOperationFlags", 10))
 
 -- Glass debug levels: 0=off, 1=summary, 2=verbose
-local DEBUG_GLASS_LEVEL = 1
+local DEBUG_GLASS_LEVEL = 0
 
 -- Wait for terrain generator and weather manager
 local TerrainGenerator = nil
@@ -97,7 +111,6 @@ task.spawn(function()
 			task.wait(0.5)
 			if _G.SaplingHandler then
 				SaplingHandler = _G.SaplingHandler
-				print("[SaveManager] SaplingHandler loaded")
 				return
 			end
 		end
@@ -244,7 +257,6 @@ local function checkForDuplicateRemotes()
 				if child.Name == name then
 					if foundFirst then
 						child:Destroy()
-						print("[SaveManager] Removed duplicate remote: " .. name)
 					else
 						foundFirst = true
 					end
@@ -265,6 +277,12 @@ local PurchaseSlotWithCoins = getOrCreateRemote("PurchaseSlotWithCoins")
 local RenameSlot = getOrCreateRemote("RenameSlot")
 local ImportLegacy = getOrCreateRemote("ImportLegacy")
 local PlotClearing = getOrCreateRemote("PlotClearing")  -- Notify clients when plot is being cleared/loaded
+-- Seed the client's dug-position set at load time so its terrain prediction never spawns
+-- phantom blocks into saved holes. Same remote the BlockHandler uses for after-the-fact
+-- cleanup; ClientPrediction's handler marks each position dug and removes any prediction
+-- there. getOrCreateRemote is idempotent: returns BlockHandler's instance if it already
+-- made one, otherwise creates the shared remote here (order-independent).
+local TerrainDugPositions = getOrCreateRemote("TerrainDugPositions")
 
 -- Check for and remove any duplicate remotes
 checkForDuplicateRemotes()
@@ -402,8 +420,6 @@ local function migrateLegacySave(player)
 		return false, "No legacy save found"
 	end
 	
-	print("[SaveManager] Found legacy save for " .. player.Name .. ", migrating to slot 1...")
-	
 	-- Get or create metadata
 	local meta = getPlayerMeta(player.UserId)
 	if not meta then
@@ -441,7 +457,6 @@ local function migrateLegacySave(player)
 	
 	savePlayerMeta(player.UserId, meta)
 	
-	print("[SaveManager] Successfully migrated legacy save for " .. player.Name)
 	return true, 1  -- Return success and slot number
 end
 
@@ -613,7 +628,6 @@ local function savePlotToSlot(player, buildArea, slotNumber)
 			end
 			if #removedRelative > 0 then
 				saveData.removedBlocks = removedRelative
-				print(string.format("[SaveManager] Saving %d removed terrain-generated blocks", #removedRelative))
 			end
 		end
 	end
@@ -624,10 +638,6 @@ local function savePlotToSlot(player, buildArea, slotNumber)
 		return false, "Save data too large: " .. math.floor(size / 1000) .. "KB (max 3500KB)"
 	end
 	
-	-- Debug: Log dug positions before save
-	print(string.format("[SaveManager] DEBUG: Saving %d blocks, %d dug positions, %d removed terrain blocks to slot %d", 
-		#saveData.blocks, #(saveData.dug or {}), #(saveData.removedBlocks or {}), slotNumber))
-	
 	-- CORRUPTION GUARD: Refuse to save if the serializer returned 0 blocks but the
 	-- Blocks folder has player-placed blocks. This indicates a serializer error or
 	-- mid-operation race -- saving the empty payload would wipe the player's work.
@@ -687,9 +697,6 @@ local function savePlotToSlot(player, buildArea, slotNumber)
 		}
 		meta.activeSlot = slotNumber
 		savePlayerMeta(player.UserId, meta)
-		
-		print(string.format("[SaveManager] Saved plot for %s to slot %d (%d blocks, %d lifetime, %.1fKB)", 
-			player.Name, slotNumber, blockCount, lifetimeBlocks, size / 1000))
 		return true, blockCount, lifetimeBlocks
 	else
 		warn("[SaveManager] Save failed for " .. player.Name .. ": " .. tostring(err))
@@ -757,13 +764,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 		blockData.gridZ * BLOCK_SIZE + offset.Z
 	)
 	
-	-- Debug logging for lazy-loaded blocks
-	if blockData.attributes.IsLazyLoaded then
-		print(string.format("[LoadDebug] %s LazyLoaded -> Grid(%d,%d,%d) -> WorldPos(%.2f,%.2f,%.2f)",
-			blockData.blockType, blockData.gridX, blockData.gridY, blockData.gridZ,
-			worldPos.X, worldPos.Y, worldPos.Z))
-	end
-	
 	-- Determine template to use
 	local templateName = shapeData.TemplateName
 	local template = nil
@@ -877,10 +877,11 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 			end
 		end
 	elseif isGlass then
-		-- GLASS: Use template from Decorations/Glass folder based on connection type
-		local glassFolder = DecorationsSourceFolder and DecorationsSourceFolder:FindFirstChild("Glass")
-		if glassFolder then
-			template = glassFolder:FindFirstChild(templateName)
+		-- GLASS: Resolve template from the glass-type registry. Standard glass uses
+		-- Decorations/Glass; CandyLattice uses RewardSets/CandyBlocks. templateName is
+		-- the generic shape (Glass_0 / Glass_Corner / Glass_Tee / Glass_4_Way).
+		template = resolveGlassTemplate(blockData.blockType, templateName)
+		if template then
 			usedBlockModel = false
 		end
 	elseif isTorch then
@@ -918,8 +919,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 			end
 			template = BlockTemplates:FindFirstChild(chestModelName)
 			usedBlockModel = false
-			print(string.format("[Chest] LOAD template=%s found=%s isDouble=%s",
-				chestModelName, tostring(template ~= nil), tostring(blockData.attributes.IsDoubleChest or false)))
 		end
 	elseif isBed then
 		-- BED: Use template from BlockTemplates
@@ -998,7 +997,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 		if solidName and BlockTemplates then
 			template = BlockTemplates:FindFirstChild(solidName)
 			usedBlockModel = false
-			print("[SaveManager] Using solid template " .. solidName .. " for filled frame " .. blockData.blockType)
 		end
 	end
 	
@@ -1020,7 +1018,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 			template = frameBlocksFolder:FindFirstChild(frameCornerName)
 			if template then
 				usedBlockModel = true
-				print("[SaveManager] Using frame corner model " .. frameCornerName .. " for " .. blockData.blockType)
 			end
 		end
 	end
@@ -1065,7 +1062,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 	local block = template:Clone()
 	block.Name = blockData.blockType
 	if isChest then
-		print(string.format("[Chest] LOAD cloned template=%s for type=%s isDouble=%s", template.Name, blockData.blockType, tostring(blockData.attributes.IsDoubleChest)))
 	end
 	
 	-- Check if this is a decoration (need this early for CanCollide)
@@ -1253,9 +1249,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 		-- Wall sign: use saved face and fence post flag for flush positioning
 		local signFace = blockData.attributes.SignFace or "North"
 		local isOnFencePost = blockData.attributes.IsOnFencePost or false
-		print(string.format("[SaveManager] Loading wall sign: face=%s, isOnFencePost=%s, worldPos=(%.2f,%.2f,%.2f), grid=(%d,%d,%d)", 
-			signFace, tostring(isOnFencePost), worldPos.X, worldPos.Y, worldPos.Z,
-			blockData.gridX, blockData.gridY, blockData.gridZ))
 		cframe = SignPlacement.GetWallSignCFrame(worldPos, signFace, isOnFencePost)
 		-- Store grid position so subsequent saves use correct position (not visual offset position)
 		block:SetAttribute("GridPosX", worldPos.X)
@@ -1270,12 +1263,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 		local hostX = blockData.attributes.HostBlockX
 		local hostY = blockData.attributes.HostBlockY
 		local hostZ = blockData.attributes.HostBlockZ
-		print(string.format("[WP-LOAD] %s face=%s filled=%s grid=(%d,%d,%d) worldPos=(%.2f,%.2f,%.2f)",
-			blockData.blockType, wallpaperFace, tostring(blockData.attributes.FrameFillBlockId),
-			blockData.gridX, blockData.gridY, blockData.gridZ,
-			worldPos.X, worldPos.Y, worldPos.Z))
-		print(string.format("[WP-LOAD]   HostBlock=(%s,%s,%s) using worldPos as host",
-			tostring(hostX), tostring(hostY), tostring(hostZ)))
 		-- Always use worldPos for CFrame (it's the correct grid center)
 		cframe = WallpaperPlacement.GetWallpaperCFrame(worldPos, wallpaperFace)
 		if not blockData.attributes.FrameFillBlockId then
@@ -1297,9 +1284,6 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 		block:SetAttribute("HostBlockX", worldPos.X)
 		block:SetAttribute("HostBlockY", worldPos.Y)
 		block:SetAttribute("HostBlockZ", worldPos.Z)
-		print(string.format("[WP-LOAD]   CFrame.pos=(%.2f,%.2f,%.2f) gridAligned=(%.2f,%.2f,%.2f)",
-			cframe.Position.X, cframe.Position.Y, cframe.Position.Z,
-			worldPos.X, worldPos.Y, worldPos.Z))
 	elseif blockData.blockType == "SnowLayer" then
 		-- SnowLayer: resize based on saved layer count and position at bottom of grid cell
 		local snowLayers = blockData.attributes.SnowLayers or 1
@@ -1356,24 +1340,63 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 		local chestRotation = blockData.attributes.Rotation or 0
 		local chestCenter = worldPos
 		local isDouble = blockData.attributes.IsDoubleChest
-		-- Double chests: position at midpoint between primary and secondary cells
-		if isDouble and blockData.attributes.ChestSecondaryX then
-			local secX = blockData.attributes.ChestSecondaryX
-			local secY = blockData.attributes.ChestSecondaryY
-			local secZ = blockData.attributes.ChestSecondaryZ
-			local secondaryWorldPos = Vector3.new(secX, secY, secZ)
+		-- Double chests: position at midpoint between primary and secondary cells.
+		-- Secondary is now stored ORIGIN-RELATIVE (csx/csy/csz) and reconstructed with the
+		-- same transform as the primary worldPos, so it re-bases correctly at any origin.
+		-- Legacy saves only have absolute ChestSecondaryX/Y/Z; for those we recover the
+		-- offset direction from the saved rotation (cells are always one block apart along
+		-- the facing axis) so the chest still lands inside the plot.
+		local secondaryWorldPos = nil
+		if isDouble then
+			local relSecX = blockData.attributes.csx
+			local relSecY = blockData.attributes.csy
+			local relSecZ = blockData.attributes.csz
+			if relSecX and relSecY and relSecZ then
+				-- New relative format: same transform as primary worldPos
+				secondaryWorldPos = Vector3.new(
+					relSecX * BLOCK_SIZE + offset.X,
+					relSecY * BLOCK_SIZE + offset.Y,
+					relSecZ * BLOCK_SIZE + offset.Z
+				)
+			elseif blockData.attributes.ChestSecondaryX then
+				-- Legacy absolute format: reconstruct from primary + rotation axis. The two
+				-- cells differ by one BLOCK_SIZE along X (rot 0/180) or Z (rot 90/270); recover
+				-- the sign from the stale absolute pair (their delta is origin-independent).
+				local legSecX = blockData.attributes.ChestSecondaryX
+				local legSecZ = blockData.attributes.ChestSecondaryZ
+				local legPrimX = blockData.attributes.BlockPosX
+				local legPrimZ = blockData.attributes.BlockPosZ
+				local rotNorm = chestRotation % 360
+				local axisIsX = (rotNorm == 0 or rotNorm == 180)
+				local sign = 1
+				if legPrimX and legPrimZ then
+					if axisIsX then
+						sign = (legSecX - legPrimX) >= 0 and 1 or -1
+					else
+						sign = (legSecZ - legPrimZ) >= 0 and 1 or -1
+					end
+				end
+				local delta = axisIsX and Vector3.new(sign * BLOCK_SIZE, 0, 0) or Vector3.new(0, 0, sign * BLOCK_SIZE)
+				secondaryWorldPos = worldPos + delta
+			end
+		end
+		if isDouble and secondaryWorldPos then
 			chestCenter = (worldPos + secondaryWorldPos) / 2
-			print(string.format("[Chest] LOAD DOUBLE CFrame: worldPos=(%.2f,%.2f,%.2f) secondary=(%.2f,%.2f,%.2f) midpoint=(%.2f,%.2f,%.2f) rot=%d",
-				worldPos.X, worldPos.Y, worldPos.Z, secX, secY, secZ, chestCenter.X, chestCenter.Y, chestCenter.Z, chestRotation))
 		else
-			print(string.format("[Chest] LOAD SINGLE CFrame: worldPos=(%.2f,%.2f,%.2f) rot=%d isDouble=%s hasSecondary=%s",
-				worldPos.X, worldPos.Y, worldPos.Z, chestRotation, tostring(isDouble), tostring(blockData.attributes.ChestSecondaryX ~= nil)))
 		end
 		cframe = CFrame.new(chestCenter) * CFrame.Angles(0, math.rad(chestRotation), 0) * CFrame.new(0, 0, 0.15)
 		-- Store clean grid position so next save cycle doesn't drift
 		block:SetAttribute("BlockPosX", worldPos.X)
 		block:SetAttribute("BlockPosY", worldPos.Y)
 		block:SetAttribute("BlockPosZ", worldPos.Z)
+		-- Re-write the live secondary attributes to the CORRECTED world coords so that
+		-- split-on-break and the next save use accurate positions (the generic attribute
+		-- loop below would otherwise restore stale/relative values).
+		if isDouble and secondaryWorldPos then
+			block:SetAttribute("ChestSecondaryX", secondaryWorldPos.X)
+			block:SetAttribute("ChestSecondaryY", secondaryWorldPos.Y)
+			block:SetAttribute("ChestSecondaryZ", secondaryWorldPos.Z)
+		end
 	elseif isBed then
 		-- Bed: saved rotation, Y offset to sit on cell floor
 		local bedRotation = blockData.attributes.Rotation or 0
@@ -1584,6 +1607,11 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 			if attrName == "_WireConnections" then
 				wireConnections = attrValue
 			end
+		elseif attrName == "ChestSecondaryX" or attrName == "ChestSecondaryY" or attrName == "ChestSecondaryZ"
+			or attrName == "csx" or attrName == "csy" or attrName == "csz" then
+			-- Chest secondary position is handled explicitly in the chest CFrame block above
+			-- (reconstructed from relative csx/csy/csz or legacy absolutes). Skip here so the
+			-- corrected world coords aren't clobbered by stale/relative saved values.
 		else
 			block:SetAttribute(attrName, attrValue)
 		end
@@ -1992,11 +2020,16 @@ local function createBlockFromSave(blockData, blocksFolder, placedByUserId, orig
 			end
 		end
 		-- DoubleChest: resize hitbox to span both cells
-		if blockData.attributes.IsDoubleChest and blockData.attributes.ChestSecondaryX then
+		if blockData.attributes.IsDoubleChest then
 			local hitbox = block:FindFirstChild("Hitbox")
-			if hitbox then
+			-- Read the CORRECTED secondary off the block (set in the chest CFrame block above),
+			-- not the stale/relative saved attributes.
+			local csX = block:GetAttribute("ChestSecondaryX")
+			local csY = block:GetAttribute("ChestSecondaryY")
+			local csZ = block:GetAttribute("ChestSecondaryZ")
+			if hitbox and csX and csY and csZ then
 				local chestRot = (blockData.attributes.Rotation or 0) % 360
-				local secPos = Vector3.new(blockData.attributes.ChestSecondaryX, blockData.attributes.ChestSecondaryY, blockData.attributes.ChestSecondaryZ)
+				local secPos = Vector3.new(csX, csY, csZ)
 				local midpoint = (worldPos + secPos) / 2
 				hitbox.Size = Vector3.new(
 					(chestRot == 0 or chestRot == 180) and BLOCK_SIZE * 2 or BLOCK_SIZE,
@@ -2197,7 +2230,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 	
 	-- If no save data, reset to fresh plot
 	if not saveData then
-		print("[SaveManager] No save data in slot " .. slotNumber .. " for " .. player.Name .. ", resetting to fresh plot")
 		
 		-- Notify clients to clear particles before we clear the plot
 		PlotClearing:FireClient(player, plotNumber)
@@ -2245,8 +2277,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 	end
 	
 	-- Debug
-	print(string.format("[SaveManager] DEBUG LOAD: Slot %d has %d blocks", 
-		slotNumber, saveData.blocks and #saveData.blocks or 0))
 	
 	-- Deserialize
 	local plotData, deserializeError = PlotSerializer.DeserializePlot(saveData)
@@ -2285,7 +2315,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 		end
 		
 		plotData.dugPositions = reconstructedDug
-		print(string.format("[SaveManager] Reconstructed %d dug positions", #reconstructedDug))
 	end
 	
 	-- Notify clients to clear particles before we clear the plot
@@ -2310,7 +2339,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 			if plotData.terrainMaxHeight then buildArea:SetAttribute("TerrainMaxHeight", plotData.terrainMaxHeight) end
 			if plotData.terrainStartX then buildArea:SetAttribute("TerrainStartX", plotData.terrainStartX) end
 			if plotData.terrainStartZ then buildArea:SetAttribute("TerrainStartZ", plotData.terrainStartZ) end
-			print(string.format("[SaveManager] Restored saved height map for exact terrain restoration"))
 		end
 		
 		local originalSettings = TerrainGenerator.GetGenerationSettings()
@@ -2353,6 +2381,12 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 		)
 	end
 	
+	-- Accumulates every dug world-position handed to MarkPositionsAsDug below, so we can
+	-- seed the owning client's terrain prediction with the same set once the load finishes.
+	-- Prevents the "phantom terrain flashes into old holes on first dig" glitch. Consumed by
+	-- the TerrainDugPositions:FireClient block at the end of this function.
+	local clientDugSeed = {}
+	
 	-- Remove dug terrain blocks (optimized with budget-based yielding)
 	local dugRemoved = 0
 	local lastYield = os.clock()
@@ -2487,12 +2521,15 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 			end
 			
 			TerrainGenerator.MarkPositionsAsDug(dugWorldPositions, plotNumber)
+			-- Mirror into the client seed (see clientDugSeed above)
+			for _, p in ipairs(dugWorldPositions) do clientDugSeed[#clientDugSeed + 1] = p end
 			
-			-- Spawn edge walls for dug positions at plot boundaries
-			-- This fills the gaps visible when digging at the edge, saving, and reloading
-			if TerrainGenerator.SpawnEdgeWallsForDugPositions then
-				TerrainGenerator.SpawnEdgeWallsForDugPositions(buildArea, plotNumber, dugWorldPositions)
-			end
+			-- Edge walls DISABLED: floor is deep enough now that edge gaps don't need filling.
+			-- This load-time fill was spawning unbreakable (CanQuery=false) dark-tinted dirt
+			-- columns around the plot border whenever the player dug near the edge and reloaded.
+			-- if TerrainGenerator.SpawnEdgeWallsForDugPositions then
+			-- 	TerrainGenerator.SpawnEdgeWallsForDugPositions(buildArea, plotNumber, dugWorldPositions)
+			-- end
 		end
 	end
 	
@@ -2602,7 +2639,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 		end
 		
 		if removedTerrainCount > 0 then
-			print(string.format("[SaveManager] Removed %d terrain-generated blocks (trees/decorations/etc) from loaded save", removedTerrainCount))
 		end
 	end
 	
@@ -2628,6 +2664,8 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 		
 		if #allDugPositions > 0 then
 			TerrainGenerator.MarkPositionsAsDug(allDugPositions, plotNumber)
+			-- Mirror into the client seed (see clientDugSeed above)
+			for _, p in ipairs(allDugPositions) do clientDugSeed[#clientDugSeed + 1] = p end
 		end
 	end
 	
@@ -2679,9 +2717,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 	createdBlocks = nil -- Free for GC
 	templateCtx = nil
 	
-	print(string.format("[SaveManager] Loaded slot %d for %s (%d blocks, %d failed)", 
-		slotNumber, player.Name, created, failed))
-	
 	-- Post-load: Remove decorations that overlap with player-placed blocks
 	-- Uses playerBlockPositions tracked during creation (no extra child scan needed)
 	local decoCleanupCount = 0
@@ -2715,7 +2750,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 		playerBlockPositions = nil -- Free for GC
 		
 		if decoCleanupCount > 0 then
-			print(string.format("[SaveManager] Cleaned up %d decorations overlapping with player blocks", decoCleanupCount))
 		end
 	end
 	
@@ -2728,10 +2762,7 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 	end
 	
 	if #glassBlocks > 0 then
-		local DecorationsSourceFolder = getDecorationsFolder()
-		local glassFolder = DecorationsSourceFolder and DecorationsSourceFolder:FindFirstChild("Glass")
-		
-		if glassFolder then
+		do
 			local glassUpdated = 0
 			
 			local function snapToGrid(pos)
@@ -2743,7 +2774,8 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 			end
 			
 			local function createGlassBlock(glassTemplateName, glassType, glassPos, glassRotationCF, placedBy, connectionType, primaryDir, secondaryDir, orientation)
-				local template = glassFolder:FindFirstChild(glassTemplateName)
+				-- Resolve per-block: standard glass -> Decorations/Glass, CandyLattice -> RewardSets/CandyBlocks.
+				local template = resolveGlassTemplate(glassType, glassTemplateName)
 				if not template then return nil end
 				
 				local newGlass = template:Clone()
@@ -2866,7 +2898,6 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 	if TerrainGenerator and TerrainGenerator.RepairMissingTerrain then
 		local repaired = TerrainGenerator.RepairMissingTerrain(buildArea, plotNumber)
 		if repaired > 0 then
-			print(string.format("[SaveManager] Auto-repaired %d missing terrain blocks", repaired))
 		end
 	end
 	
@@ -2874,6 +2905,22 @@ local function loadPlotFromSlot(player, buildArea, plotNumber, slotNumber)
 	PlayerActiveSlot[player.UserId] = slotNumber
 	LoadSucceeded[player.UserId] = true -- Plot loaded successfully; saving is now safe.
 	
+	-- Seed the client's terrain prediction with this plot's dug positions BEFORE it can
+	-- predict into them. Without this, digging next to a previously-dug-out area makes the
+	-- client spawn phantom terrain into the saved hole (it has no session record that those
+	-- cells are dug), which then gets cleaned up a round-trip later -- the "blocks flash in,
+	-- then vanish" glitch. Chunked so no single remote payload is huge on heavily-mined plots.
+	if #clientDugSeed > 0 and TerrainDugPositions then
+		local SEED_CHUNK = 600
+		for i = 1, #clientDugSeed, SEED_CHUNK do
+			local batch = {}
+			for j = i, math.min(i + SEED_CHUNK - 1, #clientDugSeed) do
+				batch[#batch + 1] = clientDugSeed[j]
+			end
+			TerrainDugPositions:FireClient(player, batch)
+		end
+	end
+	
 	-- Update metadata
 	local meta = getPlayerMeta(player.UserId)
 	if meta then
@@ -2923,7 +2970,6 @@ local function deleteSlot(player, slotNumber)
 	meta.slots[slotNumber] = nil
 	savePlayerMeta(player.UserId, meta)
 	
-	print("[SaveManager] Deleted slot " .. slotNumber .. " for " .. player.Name)
 	return true
 end
 
@@ -2948,7 +2994,6 @@ local function purchaseExtraSlot(player)
 	if player.UserId == ownerUserId then
 		meta.maxSlots = meta.maxSlots + 1
 		savePlayerMeta(player.UserId, meta)
-		print("[SaveManager] Granted free slot to game owner (now has " .. meta.maxSlots .. " slots)")
 		return true, meta.maxSlots
 	end
 	
@@ -2992,7 +3037,6 @@ _G.RegisterReceiptHandler = function(productId, handler)
 		return
 	end
 	ExtraReceiptHandlers[productId] = handler
-	print("[SaveManager] Registered receipt handler for productId", productId)
 end
 
 local function processReceipt(receiptInfo)
@@ -3019,7 +3063,6 @@ local function processReceipt(receiptInfo)
 			warn("[SaveManager] Slot purchase save failed for", player.Name, ":", err)
 			return Enum.ProductPurchaseDecision.NotProcessedYet
 		end
-		print("[SaveManager] Added slot for " .. player.Name .. " (now has " .. meta.maxSlots .. " slots)")
 		SaveLoadResult:FireClient(player, "slotPurchased", true, meta.maxSlots)
 		return Enum.ProductPurchaseDecision.PurchaseGranted
 	end
@@ -3103,7 +3146,6 @@ local function connectEventHandlers()
 	-- Save to slot
 	SavePlot.OnServerEvent:Connect(function(player, slotNumber)
 		slotNumber = slotNumber or PlayerActiveSlot[player.UserId] or 1
-		print("[SaveManager] Save request to slot", slotNumber, "from", player.Name)
 		
 		local buildArea, plotNumber = getPlayerBuildArea(player)
 		if not buildArea then
@@ -3126,7 +3168,6 @@ local function connectEventHandlers()
 	-- Load from slot
 	LoadPlot.OnServerEvent:Connect(function(player, slotNumber)
 		slotNumber = slotNumber or PlayerActiveSlot[player.UserId] or 1
-		print("[SaveManager] Load request from slot", slotNumber, "for", player.Name)
 		
 		local buildArea, plotNumber = getPlayerBuildArea(player)
 		if not buildArea then
@@ -3144,7 +3185,6 @@ local function connectEventHandlers()
 	
 	-- Delete slot
 	DeleteSlot.OnServerEvent:Connect(function(player, slotNumber)
-		print("[SaveManager] Delete request for slot", slotNumber, "from", player.Name)
 		
 		local success, err = deleteSlot(player, slotNumber)
 		if success then
@@ -3156,7 +3196,6 @@ local function connectEventHandlers()
 	
 	-- Rename slot
 	RenameSlot.OnServerEvent:Connect(function(player, slotNumber, newName)
-		print("[SaveManager] Rename request for slot", slotNumber, "to", newName, "from", player.Name)
 		
 		local meta = getPlayerMeta(player.UserId)
 		if not meta then
@@ -3188,7 +3227,6 @@ local function connectEventHandlers()
 		local success, err = savePlayerMeta(player.UserId, meta)
 		
 		if success then
-			print("[SaveManager] Renamed slot " .. slotNumber .. " to '" .. newName .. "' for " .. player.Name)
 			SaveLoadResult:FireClient(player, "rename", true, newName)
 		else
 			SaveLoadResult:FireClient(player, "rename", false, "Failed to save: " .. tostring(err))
@@ -3197,7 +3235,6 @@ local function connectEventHandlers()
 	
 	-- Import legacy save to a slot
 	ImportLegacy.OnServerEvent:Connect(function(player, targetSlot)
-		print("[SaveManager] Import legacy request to slot", targetSlot, "from", player.Name)
 		
 		local legacyStore = getLegacyDataStore()
 		if not legacyStore then
@@ -3258,13 +3295,12 @@ local function connectEventHandlers()
 		}
 		savePlayerMeta(player.UserId, meta)
 		
-		print("[SaveManager] Imported legacy save (" .. blockCount .. " blocks) to slot " .. targetSlot .. " for " .. player.Name)
+		
 		SaveLoadResult:FireClient(player, "import", true, blockCount)
 	end)
 	
 	-- Purchase slot
 	PurchaseSlot.OnServerEvent:Connect(function(player)
-		print("[SaveManager] Slot purchase (Robux) request from", player.Name)
 		
 		local success, result = purchaseExtraSlot(player)
 		if success and type(result) == "number" then
@@ -3278,7 +3314,6 @@ local function connectEventHandlers()
 	
 	-- Coin-based slot purchase
 	PurchaseSlotWithCoins.OnServerEvent:Connect(function(player)
-		print("[SaveManager] Slot purchase (Coins) request from", player.Name)
 		
 		local meta = getPlayerMeta(player.UserId)
 		if not meta then
@@ -3308,7 +3343,6 @@ local function connectEventHandlers()
 		-- Grant the slot
 		meta.maxSlots = meta.maxSlots + 1
 		savePlayerMeta(player.UserId, meta)
-		print("[SaveManager] Granted coin-purchased slot to " .. player.Name .. " (now has " .. meta.maxSlots .. " slots, spent " .. price .. " coins)")
 		SaveLoadResult:FireClient(player, "slotPurchased", true, meta.maxSlots)
 	end)
 	
@@ -3335,9 +3369,8 @@ local function connectEventHandlers()
 				LastSaveTime[player.UserId] = 0
 				local success, blockCount = savePlotToSlot(player, buildArea, activeSlot)
 				if success then
-					print("[SaveManager] Emergency save succeeded for " .. player.Name .. " (" .. tostring(blockCount) .. " blocks)")
+					
 				else
-					warn("[SaveManager] Emergency save FAILED for " .. player.Name .. ": " .. tostring(blockCount))
 				end
 			end
 		end
@@ -3367,7 +3400,6 @@ local function connectEventHandlers()
 		end)
 	end
 	
-	print("[SaveManager] Event handlers connected")
 end
 
 connectEventHandlers()
@@ -3452,7 +3484,6 @@ AutoLoadPlot.OnInvoke = function(player, buildArea, plotNumber)
 		return false, "No save data"
 	end
 	
-	print("[SaveManager] Auto-loading slot " .. activeSlot .. " for " .. player.Name)
 	
 	local success, result = loadPlotFromSlot(player, buildArea, plotNumber, activeSlot)
 	
@@ -3486,7 +3517,6 @@ SavePlotSync.OnInvoke = function(player)
 	-- BindToClose fires first during Studio stop/server shutdown and may save the correct
 	-- build area. SavePlotSync firing afterwards could overwrite with stale/wrong data.
 	if SavedDuringShutdown[player.UserId] then
-		print("[SaveManager] SavePlotSync: Skipping - BindToClose already saved " .. player.Name)
 		SavedOnLeave[player.UserId] = true
 		return true, 0
 	end
@@ -3503,15 +3533,12 @@ SavePlotSync.OnInvoke = function(player)
 	if not activeSlot then
 		local meta = getPlayerMeta(player.UserId)
 		activeSlot = meta and meta.activeSlot or 1
-		print("[SaveManager] SavePlotSync: PlayerActiveSlot was nil, using meta.activeSlot:", activeSlot)
 	end
 	
 	-- Log build area details so we can verify we're saving the right plot
 	local sizeX = buildArea:GetAttribute("BasePlateSizeX") or 0
 	local sizeZ = buildArea:GetAttribute("BasePlateSizeZ") or 0
 	local terrainStyle = buildArea:GetAttribute("TerrainStyle") or "?"
-	print(string.format("[SaveManager] SavePlotSync starting for %s (slot %d, buildArea=%s, size=%.0fx%.0f, terrain=%s)",
-		player.Name, activeSlot, buildArea.Name, sizeX, sizeZ, terrainStyle))
 	
 	LastSaveTime[player.UserId] = 0
 	local success, blockCount, lifetimeBlocks = savePlotToSlot(player, buildArea, activeSlot)
@@ -3520,7 +3547,6 @@ SavePlotSync.OnInvoke = function(player)
 	SavedOnLeave[player.UserId] = true
 	
 	if success then
-		print(string.format("[SaveManager] Sync-saved to slot %d for %s (%d blocks)", activeSlot, player.Name, blockCount))
 		SavedDuringShutdown[player.UserId] = true
 		
 		if BetaPlayerManager then
@@ -3544,7 +3570,6 @@ end)
 -- ============================================
 
 game:BindToClose(function()
-	print("[SaveManager] Server shutting down, saving all player plots...")
 	
 	local saveThreads = {}
 	local skippedCount = 0
@@ -3563,14 +3588,14 @@ game:BindToClose(function()
 				if not activeSlot then
 					local meta = getPlayerMeta(player.UserId)
 					activeSlot = meta and meta.activeSlot or 1
-					print("[SaveManager] BindToClose: PlayerActiveSlot was nil for", player.Name, ", using meta.activeSlot:", activeSlot)
+					
 				end
 				
 				LastSaveTime[player.UserId] = 0
 				local success, result = savePlotToSlot(player, buildArea, activeSlot)
 				if success then
 					SavedDuringShutdown[player.UserId] = true
-					print("[SaveManager] BindToClose saved slot " .. activeSlot .. " for " .. player.Name)
+					
 				end
 			end)
 			table.insert(saveThreads, thread)
@@ -3589,7 +3614,7 @@ game:BindToClose(function()
 		task.wait(0.1)
 	end
 	
-	print(string.format("[SaveManager] BindToClose complete (skipped %d already-saved)", skippedCount))
+	
 end)
 
 -- ============================================
@@ -3614,8 +3639,6 @@ task.spawn(function()
 							LastSaveTime[userId] = 0 -- bypass cooldown for autosave
 							local success, result = savePlotToSlot(player, buildArea, activeSlot)
 							if success then
-								print(string.format("[SaveManager] Auto-saved %s slot %d (%d blocks)",
-									player.Name, activeSlot, result))
 							else
 								warn(string.format("[SaveManager] Auto-save failed for %s slot %d: %s",
 									player.Name, activeSlot, tostring(result)))
@@ -3648,8 +3671,5 @@ MarkTipsSeen.OnServerEvent:Connect(function(player)
 	if meta then
 		meta.tipsSeen = true
 		savePlayerMeta(player.UserId, meta)
-		print("[SaveManager] Marked tips as seen for", player.Name)
 	end
 end)
-
-print("[SaveManager] Multi-slot system initialized (instance:", INSTANCE_ID, ")")
diff --git a/public/src/Server/VineHandler.server.luau b/public/src/Server/VineHandler.server.luau
index c668849..55d11ef 100644
--- a/public/src/Server/VineHandler.server.luau
+++ b/public/src/Server/VineHandler.server.luau
@@ -42,9 +42,6 @@ local CONFIG = {
 	MAX_VINE_LENGTH = 24,
 }
 
--- Debug flag
-local DEBUG_VINES = false
-
 -- Track active plots for vine spreading
 local ActivePlots = {}
 local IsRunning = false
@@ -214,11 +211,6 @@ local function createVine(position, face, blocksFolder, placedBy)
 	-- Add ceiling piece if solid block above
 	updateCeilingVine(vine, position, blocksFolder)
 	
-	if DEBUG_VINES then
-		print(string.format("[VineHandler] Created vine at (%.1f, %.1f, %.1f) face=%s",
-			position.X, position.Y, position.Z, face))
-	end
-	
 	return vine
 end
 
@@ -260,17 +252,8 @@ updateCeilingVine = function(vineBlock, gridPosition, blocksFolder)
 		end
 		
 		ceiling.Parent = vineBlock
-		
-		if DEBUG_VINES then
-			print(string.format("[VineHandler] Added ceiling vine at (%.1f, %.1f, %.1f)",
-				gridPosition.X, gridPosition.Y, gridPosition.Z))
-		end
 	elseif not shouldHave and existingCeiling then
 		existingCeiling:Destroy()
-		if DEBUG_VINES then
-			print(string.format("[VineHandler] Removed ceiling vine at (%.1f, %.1f, %.1f)",
-				gridPosition.X, gridPosition.Y, gridPosition.Z))
-		end
 	end
 end
 
@@ -356,10 +339,6 @@ local function processPlotVines(blocksFolder)
 				if newVine then
 					spreadCount = spreadCount + 1
 					spread = true
-					if DEBUG_VINES then
-						print(string.format("[VineHandler] Vine spread DOWN from (%.1f,%.1f,%.1f) to (%.1f,%.1f,%.1f)",
-							vinePos.X, vinePos.Y, vinePos.Z, belowPos.X, belowPos.Y, belowPos.Z))
-					end
 				end
 			end
 		end
@@ -371,19 +350,11 @@ local function processPlotVines(blocksFolder)
 				if newVine then
 					spreadCount = spreadCount + 1
 					spread = true
-					if DEBUG_VINES then
-						print(string.format("[VineHandler] Vine spread UP from (%.1f,%.1f,%.1f) to (%.1f,%.1f,%.1f)",
-							vinePos.X, vinePos.Y, vinePos.Z, abovePos.X, abovePos.Y, abovePos.Z))
-					end
 				end
 			end
 		end
 	end
 	
-	if spreadCount > 0 and DEBUG_VINES then
-		print(string.format("[VineHandler] Spread %d vines this tick", spreadCount))
-	end
-	
 	-- Update ceiling vines (handles loaded saves, block changes above vines)
 	for _, vine in ipairs(vines) do
 		local posX = vine:GetAttribute("BlockPosX")
diff --git a/public/src/Shared/BlockPresets.luau b/public/src/Shared/BlockPresets.luau
index 7b5d900..cea6723 100644
--- a/public/src/Shared/BlockPresets.luau
+++ b/public/src/Shared/BlockPresets.luau
@@ -324,6 +324,8 @@ local DecorationPlacementRestrictions = {
 	["DarkOakSapling"] = { "Dirt", "GrassBlock" },
 	["EbonySapling"] = { "Dirt", "GrassBlock" },
 	["RedwoodSapling"] = { "Dirt", "GrassBlock" },
+	-- Candy reward sapling (Step 5) ΓÇö same restriction.
+	["CandyCaneSapling"] = { "Dirt", "GrassBlock" },
 }
 
 -- Color palette for customization
@@ -2011,16 +2013,28 @@ end
 -- Generate presets at module load
 local Presets = generatePresets()
 
--- Append auto-generated reward block presets (from ReplicatedStorage.RewardSets)
+-- Append auto-generated reward block presets (from ReplicatedStorage.RewardSets).
+-- Generate() returns (presets, themedTabs); the latter is a list of themed-pack
+-- tabs declared by manifests (e.g. Candy), which the inventory renders as right-
+-- side tabs gated by player unlocks.
+local ThemedPackTabs = {}
 local rewardPresetsOk, RewardBlockPresets = pcall(require, ReplicatedStorage:WaitForChild("RewardBlockPresets"))
 if rewardPresetsOk and RewardBlockPresets then
-	local rewardPresets = RewardBlockPresets.Generate()
+	local rewardPresets, themedTabs = RewardBlockPresets.Generate()
 	for _, preset in ipairs(rewardPresets) do
 		table.insert(Presets, preset)
 	end
+	if themedTabs then
+		ThemedPackTabs = themedTabs
+	end
 	if #rewardPresets > 0 then
 		print(string.format("[BlockPresets] Added %d reward-block preset(s) from RewardSets folder", #rewardPresets))
 	end
+	if #ThemedPackTabs > 0 then
+		local names = {}
+		for _, t in ipairs(ThemedPackTabs) do table.insert(names, t.Name) end
+		print(string.format("[BlockPresets] Themed-pack tabs: %s", table.concat(names, ", ")))
+	end
 else
 	warn("[BlockPresets] Could not load RewardBlockPresets module")
 end
@@ -2124,6 +2138,13 @@ local function GetAllCategories()
 	return Categories
 end
 
+-- Themed-pack tabs (right-side tabs declared by reward-set Manifest.Tab fields).
+-- Returns a list of { Name = "Candy", IconBlockName = "CandyBricks", SetName = "CandyBlocks" }.
+-- The inventory uses this to render dynamic right-side tabs gated by player unlocks.
+local function GetThemedPackTabs()
+	return ThemedPackTabs
+end
+
 local function GetAllColors()
 	return Colors
 end
@@ -2147,11 +2168,26 @@ local function IsGlass(id)
 end
 
 local function IsSapling(id)
-	return SaplingBlocks[id] ~= nil
+	if SaplingBlocks[id] ~= nil then return true end
+	-- Reward-set saplings are tagged via the manifest (IsSaplingItem=true). Consult
+	-- the preset list so SaplingHandler.RegisterSapling fires for them at placement.
+	for _, preset in ipairs(Presets) do
+		if preset.Id == id and preset.IsSaplingItem then
+			return true
+		end
+	end
+	return false
 end
 
 local function GetSaplingTreeType(id)
-	return SaplingBlocks[id]
+	if SaplingBlocks[id] then return SaplingBlocks[id] end
+	-- Reward-set saplings carry TreeType on the preset itself.
+	for _, preset in ipairs(Presets) do
+		if preset.Id == id and preset.TreeType then
+			return preset.TreeType
+		end
+	end
+	return nil
 end
 
 local function IsLadder(id)
@@ -2307,6 +2343,7 @@ return {
 	GetShape = GetShape,
 	GetAllPresets = GetAllPresets,
 	GetAllCategories = GetAllCategories,
+	GetThemedPackTabs = GetThemedPackTabs,
 	GetAllColors = GetAllColors,
 	IsDecoration = IsDecoration,
 	IsDoor = IsDoor,
diff --git a/public/src/Shared/Config/ChangelogConfig.luau b/public/src/Shared/Config/ChangelogConfig.luau
index b47a388..828f057 100644
--- a/public/src/Shared/Config/ChangelogConfig.luau
+++ b/public/src/Shared/Config/ChangelogConfig.luau
@@ -6,16 +6,15 @@
 
 local ChangelogConfig = {
 	-- Current version (increment this with each update)
-	VERSION = 22,
+	VERSION = 24,
 	
 	-- Version display name (shown to players)
-	VERSION_NAME = "v1.7.3",
+	VERSION_NAME = "v1.7.5",
 	
 	-- Developer Notes (optional) - shown in a highlighted box above changes
 	-- Set to nil or empty string to hide the notes section
 	-- Use this for bug awareness, upcoming features, warnings, etc.
 	NOTES = "",
-	
 	-- Notes style: "warning" (yellow), "info" (blue), "success" (green), "error" (red)
 	NOTES_STYLE = "warning",
 	
@@ -25,7 +24,7 @@ local ChangelogConfig = {
 	IMAGES = {
 		-- Example:
 		{ imageId = "rbxassetid://139684623132536", caption = "Added more parity blocks!" },
-		{ imageId = "rbxassetid://72629514451612", caption = "Added vines and an arch frame variant" },
+		{ imageId = "rbxaspsetid://72629514451612", caption = "Added vines and an arch frame variant" },
 	},
 	
 	-- Changelog entries for the current version
@@ -40,6 +39,8 @@ local ChangelogConfig = {
 		{ icon = "", text = "Tweaked campfire smoke and lighting system" },
 		{ icon = "", text = "Tweaked saving/loading system to be more robust" },
 		{ icon = "", text = "Fixed daily rewards system" },
+		{ icon = "", text = "Fixed terrain around the edge of the plot" },
+		{ icon = "", text = "Fixed weird grass bug" },
 		{ icon = "", text = "Fixed more bugs and issues as usual!" },
 	},
 	
diff --git a/public/src/Shared/GlassPlacement.luau b/public/src/Shared/GlassPlacement.luau
index 1e0ebdf..4113e27 100644
--- a/public/src/Shared/GlassPlacement.luau
+++ b/public/src/Shared/GlassPlacement.luau
@@ -49,11 +49,59 @@ GlassPlacement.TEMPLATE_NAME = "Glass_0"
 -- Map glass block IDs to their base type
 GlassPlacement.GLASS_BASE_TYPES = {
 	["Glass"] = "Glass",
+	["CandyLattice"] = "CandyLattice",
 	-- Add colored glass variants here later if needed
 	-- ["RedGlass"] = "Glass",
 	-- ["BlueGlass"] = "Glass",
 }
 
+-- Per-type model source: which folder the pane/corner/tee/cross models live in,
+-- and the actual model names for each connection shape. The connection-detection
+-- functions below all speak the generic Glass_0 / Glass_Corner / Glass_Tee /
+-- Glass_4_Way vocabulary; ResolveTemplate() translates that into the real folder
+-- + model name for whatever glass-style block is being placed. This keeps the
+-- whole engine generic so new glass-style sets (CandyLattice, future tinted
+-- panes) need only an entry here -- no BlockHandler changes.
+--
+-- folder is relative to ReplicatedStorage (matches preset.BlockModelFolder).
+GlassPlacement.GLASS_TYPE_CONFIG = {
+	Glass = {
+		folder = "Decorations/Glass",
+		single  = "Glass_0",
+		corner  = "Glass_Corner",
+		tee     = "Glass_Tee",
+		fourway = "Glass_4_Way",
+	},
+	CandyLattice = {
+		folder = "RewardSets/CandyBlocks",
+		single  = "CandyLattice",
+		corner  = "CandyLatticeCorner",
+		tee     = "CandyLatticeT",
+		fourway = "CandyLatticeCross",
+	},
+}
+
+-- Map a generic template name (as produced by the connection-detection code)
+-- to the config key holding the real model name.
+local GENERIC_TEMPLATE_TO_KEY = {
+	["Glass_0"]     = "single",
+	["Glass_Corner"] = "corner",
+	["Glass_Tee"]   = "tee",
+	["Glass_4_Way"] = "fourway",
+}
+
+-- Resolve a glass type + generic template name into the real folder + model name.
+-- glassType: the block's BlockType (e.g. "Glass", "CandyLattice").
+-- genericName: one of Glass_0 / Glass_Corner / Glass_Tee / Glass_4_Way.
+-- Returns folder (string, relative to ReplicatedStorage) and modelName (string).
+-- Falls back to the Glass config for unknown types so existing glass is unaffected.
+function GlassPlacement.ResolveTemplate(glassType, genericName)
+	local baseType = GlassPlacement.GLASS_BASE_TYPES[glassType] or "Glass"
+	local cfg = GlassPlacement.GLASS_TYPE_CONFIG[baseType] or GlassPlacement.GLASS_TYPE_CONFIG.Glass
+	local key = GENERIC_TEMPLATE_TO_KEY[genericName] or "single"
+	return cfg.folder, cfg[key]
+end
+
 -- Valid orientations
 GlassPlacement.Orientations = {
 	NS = "NS",  -- North/South - flat face perpendicular to Z
@@ -193,7 +241,6 @@ function GlassPlacement.GetPlacementInfo(blockType, position, blocksFolder, orie
 	
 	-- Summary logging at level 1+
 	if debugOutput or DEBUG_GLASS_LEVEL >= 1 then
-		print(string.format("[Glass] PlacementInfo: template=Glass_0, orientation=%s", orientation))
 	end
 	
 	return {
@@ -216,9 +263,6 @@ function GlassPlacement.GetGlassCFrame(position, rotationCFrame, positionOffset,
 	
 	-- Summary logging at level 1+
 	if DEBUG_GLASS_LEVEL >= 1 then
-		print(string.format("[Glass] GetGlassCFrame: inputPos=(%.2f,%.2f,%.2f) finalPos=(%.2f,%.2f,%.2f)",
-			position.X, position.Y, position.Z,
-			finalPosition.X, finalPosition.Y, finalPosition.Z))
 	end
 	
 	return CFrame.new(finalPosition) * rotationCFrame
diff --git a/public/src/Shared/PlotSerializer.luau b/public/src/Shared/PlotSerializer.luau
index 124bed5..4a18795 100644
--- a/public/src/Shared/PlotSerializer.luau
+++ b/public/src/Shared/PlotSerializer.luau
@@ -21,9 +21,6 @@ local ReplicatedStorage = game:GetService("ReplicatedStorage")
 
 local PlotSerializer = {}
 
--- Glass debug levels: 0=off, 1=summary, 2=verbose
-local DEBUG_GLASS_LEVEL = 1
-
 -- Current save format version (increment when format changes)
 PlotSerializer.VERSION = 2  -- Updated for multi-depth dug tracking
 
@@ -131,6 +128,12 @@ local AttributeBlacklist = {
 	["PathBaseX"] = true,        -- Used for grid calculation during save only
 	["PathBaseY"] = true,        -- Used for grid calculation during save only
 	["PathBaseZ"] = true,        -- Used for grid calculation during save only
+	-- ChestSecondary absolute coords are NOT saved; we save origin-relative csx/csy/csz
+	-- instead (see chest block below). Saving absolutes caused double chests to teleport
+	-- out of the plot when reloaded at a different origin.
+	["ChestSecondaryX"] = true,
+	["ChestSecondaryY"] = true,
+	["ChestSecondaryZ"] = true,
 }
 
 local ShortToExtraKey = {}
@@ -218,16 +221,6 @@ local function deserializeExtraData(extraData)
 			-- Check if it's a short code, otherwise use key as-is (full name)
 			local fullKey = ShortToExtraKey[key] or key
 			attributes[fullKey] = value
-			
-			-- Debug: print slab orientation when deserializing
-			if fullKey == "SlabOrientation" then
-				print(string.format("[PlotSerializer] Deserialized SlabOrientation: %s", tostring(value)))
-			end
-			
-			-- Debug: print door attributes when deserializing
-			if fullKey == "DoorDirection" or fullKey == "DoorDepth" or fullKey == "DoorHingeSide" then
-				print(string.format("[PlotSerializer] Deserialized %s: %s (type=%s) from key=%s", fullKey, tostring(value), type(value), key))
-			end
 		end
 	end
 	
@@ -353,44 +346,6 @@ local function serializeBlock(block, blockPos, BLOCK_SIZE, terrainOrigin)
 		end
 	end
 	
-	-- Debug logging for lazy-loaded blocks
-	if block:GetAttribute("IsLazyLoaded") then
-		print(string.format("[SaveDebug] %s LazyLoaded -> Grid(%d,%d,%d) from %s",
-			blockType, gridX, gridY, gridZ, positionSource))
-	end
-	
-	-- Debug logging for doors (use string matching to catch all doors)
-	local isDoorBlock = string.find(string.lower(blockType), "door") ~= nil
-	if isDoorBlock then
-		print(string.format("[SaveDebug] Door %s -> Grid(%d,%d,%d) from %s",
-			blockType, gridX, gridY, gridZ, positionSource))
-		-- Log all door attributes being saved
-		print(string.format("[SaveDebug] Door attributes on block: DoorDirection=%s, DoorDepth=%s, DoorHingeSide=%s, Rotation=%s, IsDoor=%s",
-			tostring(block:GetAttribute("DoorDirection")),
-			tostring(block:GetAttribute("DoorDepth")),
-			tostring(block:GetAttribute("DoorHingeSide")),
-			tostring(block:GetAttribute("Rotation")),
-			tostring(block:GetAttribute("IsDoor"))))
-	end
-	
-	-- Debug logging for wallpaper frames
-	if block:GetAttribute("IsWallpaperFrame") then
-		local hostX = block:GetAttribute("HostBlockX")
-		local hostY = block:GetAttribute("HostBlockY")
-		local hostZ = block:GetAttribute("HostBlockZ")
-		local bpX = block:GetAttribute("BlockPosX")
-		local bpY = block:GetAttribute("BlockPosY")
-		local bpZ = block:GetAttribute("BlockPosZ")
-		local face = block:GetAttribute("WallpaperFace") or "?"
-		local fillId = block:GetAttribute("FrameFillBlockId")
-		print(string.format("[WP-SAVE] %s face=%s filled=%s -> Grid(%d,%d,%d) from %s",
-			blockType, face, tostring(fillId), gridX, gridY, gridZ, positionSource))
-		print(string.format("[WP-SAVE]   BlockPos=(%s,%s,%s) HostBlock=(%s,%s,%s) visualPos=(%.2f,%.2f,%.2f)",
-			tostring(bpX), tostring(bpY), tostring(bpZ),
-			tostring(hostX), tostring(hostY), tostring(hostZ),
-			blockPos.X, blockPos.Y, blockPos.Z))
-	end
-	
 	-- Register type and get ID
 	local typeId = registerType(blockType)
 	
@@ -400,15 +355,6 @@ local function serializeBlock(block, blockPos, BLOCK_SIZE, terrainOrigin)
 	-- Get extra data (attributes)
 	local extra = serializeExtraData(block)
 	
-	-- Debug: log door extra data after serialization
-	if isDoorBlock and extra then
-		local extraStr = ""
-		for k, v in pairs(extra) do
-			extraStr = extraStr .. string.format("%s=%s, ", k, tostring(v))
-		end
-		print(string.format("[SaveDebug] Door serialized extra data: {%s}", extraStr))
-	end
-	
 	-- Mark decorations
 	if block:GetAttribute("IsDecoration") then
 		extra = extra or {}
@@ -426,12 +372,6 @@ local function serializeBlock(block, blockPos, BLOCK_SIZE, terrainOrigin)
 							 block:GetAttribute("IsLadder") or
 							 block:GetAttribute("IsGlass")
 	
-	-- Debug: print slab orientation when saving
-	if string.find(blockType, "Slab") then
-		local slabOrientation = block:GetAttribute("SlabOrientation")
-		print(string.format("[PlotSerializer] Saving slab %s with orientation: %s", blockType, tostring(slabOrientation)))
-	end
-	
 	if isTemplateBlock then
 		local color = extractBlockColor(block)
 		if color then
@@ -439,27 +379,24 @@ local function serializeBlock(block, blockPos, BLOCK_SIZE, terrainOrigin)
 			extra["cr"] = math.round(color.R * 255)
 			extra["cg"] = math.round(color.G * 255)
 			extra["cb"] = math.round(color.B * 255)
-			
-			-- Debug: log ladder color being saved
-			if string.find(blockType, "Ladder") then
-				print(string.format("[Ladder SAVE] %s color: R=%d G=%d B=%d", 
-					blockType, extra["cr"], extra["cg"], extra["cb"]))
-			end
-		else
-			-- Debug: log if color extraction failed
-			if string.find(blockType, "Ladder") then
-				print(string.format("[Ladder SAVE] %s - WARNING: extractBlockColor returned nil!", blockType))
-			end
 		end
 	end
 	
-	-- Debug: log chest save data
+	-- Chest: save the secondary cell as ORIGIN-RELATIVE grid coords (csx/csy/csz).
+	-- The absolute ChestSecondaryX/Y/Z attributes are blacklisted (not saved) because
+	-- absolute coords don't survive a reload at a different plot origin -- that bug threw
+	-- double chests out of the plot. Relative coords travel correctly like the primary.
 	if block:GetAttribute("IsChest") then
 		local isDouble = block:GetAttribute("IsDoubleChest")
 		local secX = block:GetAttribute("ChestSecondaryX")
-		print(string.format("[Chest] SAVE type=%s grid=(%d,%d,%d) isDouble=%s hasSecondary=%s extraKeys=%s",
-			blockType, gridX, gridY, gridZ, tostring(isDouble), tostring(secX ~= nil),
-			extra and table.concat((function() local keys = {} for k in pairs(extra) do table.insert(keys, k) end return keys end)(), ",") or "nil"))
+		local secY = block:GetAttribute("ChestSecondaryY")
+		local secZ = block:GetAttribute("ChestSecondaryZ")
+		if isDouble and secX and secY and secZ then
+			extra = extra or {}
+			extra["csx"] = math.round((secX - originX) / BLOCK_SIZE)
+			extra["csy"] = math.round((secY - originY) / BLOCK_SIZE)
+			extra["csz"] = math.round((secZ - originZ) / BLOCK_SIZE)
+		end
 	end
 	
 	if extra then
@@ -491,9 +428,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 	local sizeX = buildArea:GetAttribute("BasePlateSizeX")
 	local sizeZ = buildArea:GetAttribute("BasePlateSizeZ")
 	
-	print(string.format("[PlotSerializer] DEBUG SAVE: terrainSurfaceY=%.4f, posX=%.4f, posZ=%.4f, sizeX=%.4f, sizeZ=%.4f, BLOCK_SIZE=%d",
-		terrainSurfaceY or -999, posX or -999, posZ or -999, sizeX or -999, sizeZ or -999, BLOCK_SIZE))
-	
 	-- Calculate terrain origin for relative grid positions
 	local terrainOrigin = nil
 	if posX and posZ and sizeX and sizeZ then
@@ -502,8 +436,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 			y = terrainSurfaceY or 0,
 			z = posZ - sizeZ / 2 + BLOCK_SIZE / 2,
 		}
-		print(string.format("[PlotSerializer] Using terrain origin: (%.4f, %.4f, %.4f)", 
-			terrainOrigin.x, terrainOrigin.y, terrainOrigin.z))
 	end
 	
 	local blocks = {}
@@ -574,12 +506,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 					relGridY = math.round((blockPos.Y - terrainOrigin.y) / BLOCK_SIZE)
 					relGridZ = math.round((blockPos.Z - terrainOrigin.z) / BLOCK_SIZE)
 					missingBlockPosCount = missingBlockPosCount + 1
-					
-					-- Log first few missing BlockPos blocks for debugging
-					if missingBlockPosCount <= 3 then
-						print(string.format("[PlotSerializer] WARNING: Terrain block missing BlockPosX/Y/Z! actualPos=(%.2f,%.2f,%.2f) -> relGrid=(%d,%d,%d)",
-							blockPos.X, blockPos.Y, blockPos.Z, relGridX, relGridY, relGridZ))
-					end
 				else
 					-- No terrain origin, use absolute coords (fallback)
 					relGridX = math.round(blockPos.X / BLOCK_SIZE)
@@ -590,23 +516,10 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 				local key = relGridX .. "," .. relGridY .. "," .. relGridZ
 				existingTerrainPositions[key] = true
 				terrainBlockCount = terrainBlockCount + 1
-				
-				-- Log sample terrain positions (first 3 blocks)
-				if terrainBlockCount <= 3 then
-					print(string.format("[PlotSerializer] Sample terrain: relKey=%s storedPos=(%.2f,%.2f,%.2f) usedStored=%s",
-						key, storedX or blockPos.X, storedY or blockPos.Y, storedZ or blockPos.Z, tostring(usedStoredPos)))
-				end
 			end
 			
 			local serialized = serializeBlock(block, blockPos, BLOCK_SIZE, terrainOrigin)
 			if serialized then
-				-- Summary logging at level 1+
-				if isGlass and DEBUG_GLASS_LEVEL >= 1 then
-					local connType = block:GetAttribute("GlassConnectionType") or "unknown"
-					print(string.format("[Glass SAVE] type=%s pivotPos=(%.2f,%.2f,%.2f) -> grid=(%d,%d,%d)",
-						connType, blockPos.X, blockPos.Y, blockPos.Z,
-						(serialized :: {any})[1], (serialized :: {any})[2], (serialized :: {any})[3]))
-				end
 				table.insert(blocks, serialized)
 				totalBlocks = totalBlocks + 1
 			else
@@ -622,8 +535,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 		end
 	end
 	
-	print(string.format("[PlotSerializer] Found %d terrain blocks (%d missing BlockPos attrs)", terrainBlockCount, missingBlockPosCount))
-	
 	-- Calculate expected surface blocks for validation
 	local expectedSurfaceBlocks = 0
 	if sizeX and sizeZ then
@@ -663,9 +574,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 		local gridSizeX = buildArea:GetAttribute("TerrainGridSizeX") or math.floor(sizeX / BLOCK_SIZE)
 		local gridSizeZ = buildArea:GetAttribute("TerrainGridSizeZ") or math.floor(sizeZ / BLOCK_SIZE)
 		
-		print(string.format("[PlotSerializer] Checking for dug positions: gridSize=%dx%d, flat=%s", 
-			gridSizeX, gridSizeZ, tostring(isFlatTerrain)))
-		
 		-- For Natural terrain, we need the height map to know expected surface Y at each position
 		local heightMap = nil
 		if not isFlatTerrain then
@@ -684,7 +592,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 						heightMap[gx][gz] = tonumber(heights[index]) or 0
 					end
 				end
-				print(string.format("[PlotSerializer] Loaded height map for Natural terrain (%dx%d)", storedGridSizeX, storedGridSizeZ))
 			end
 		end
 		
@@ -716,12 +623,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 				if not existingTerrainPositions[surfaceKey] then
 					-- Surface is dug - record as RELATIVE grid coords
 					table.insert(dugPositions, {gx, relGridY, gz})
-					
-					-- Log first few dug positions for debugging
-					if #dugPositions <= 5 then
-						print(string.format("[PlotSerializer] Dug detected: relKey=%s NOT FOUND (expectedSurfaceY=%.2f)", 
-							surfaceKey, expectedSurfaceY))
-					end
 				end
 			end
 			
@@ -763,9 +664,6 @@ function PlotSerializer.SerializePlot(buildArea, BLOCK_SIZE, options)
 	-- Calculate expected surface blocks
 	local expectedSurfaceBlocks = (math.floor(sizeX / BLOCK_SIZE)) * (math.floor(sizeZ / BLOCK_SIZE))
 	
-	print(string.format("[PlotSerializer] Serialized %d blocks, %d dug positions (skipped %d terrain, found %d terrain blocks)", 
-		totalBlocks, #dugPositions, skippedTerrain, terrainBlockCount))
-	
 	-- STRIP PATTERN DETECTION: Check if an entire row or column is falsely marked as dug
 	-- This catches the edge strip bug at save time to prevent corrupting save data
 	if #dugPositions > 0 then
@@ -885,8 +783,6 @@ function PlotSerializer.DeserializePlot(saveData)
 		})
 	end
 	
-	print(string.format("[PlotSerializer] Deserialized %d blocks, %d dug positions", #blocks, #dugPositions))
-	
 	return {
 		biome = saveData.biome or "Plains",
 		season = saveData.season or "Summer",  -- Pass season for grass/leaf coloring
diff --git a/public/src/Shared/RewardBlockPresets.luau b/public/src/Shared/RewardBlockPresets.luau
index d6ea330..306faf2 100644
--- a/public/src/Shared/RewardBlockPresets.luau
+++ b/public/src/Shared/RewardBlockPresets.luau
@@ -52,7 +52,9 @@ local ReplicatedStorage = game:GetService("ReplicatedStorage")
 
 local RewardBlockPresets = {}
 
--- Valid inventory tabs (must match Categories list in BlockPresets.luau)
+-- Valid inventory tabs (must match Categories list in BlockPresets.luau).
+-- THEMED-PACK tabs (Tab field in Manifest) are added dynamically and live
+-- alongside these ΓÇö they don't need to be listed here.
 local VALID_CATEGORIES = {
 	Nature = true,
 	Building = true,
@@ -97,6 +99,68 @@ local function coerceColor(value)
 	return nil
 end
 
+-- Resolve a manifest PaletteSubset spec into a list of { color3, colorName, displayName, index }.
+-- Three accepted forms:
+--   "all"                              -> every palette color (in ColorPalette.ColorOrder)
+--   { "Red", "Blue", ... }             -> just those colors (names from ColorPalette)
+--   { { Color="Red", Name="..." }, .. } -> custom display name per color
+-- `index` is each variant's position in the canonical palette order, used for
+-- in-tab sorting so colors group together visually instead of alphabetically.
+local function resolvePaletteSubset(spec, ColorPalette, setName, blockName, colorOverrides)
+	local entries = {}
+	local orderedNames = ColorPalette.ColorOrder
+	local nameToIndex = {}
+	for i, n in ipairs(orderedNames) do nameToIndex[n] = i end
+	-- colorOverrides (optional): { [colorName] = Color3 } from the manifest. When a
+	-- color is listed here, use that exact Color3 instead of ColorPalette's version.
+	-- Lets a set use punchier colors than the shared palette (e.g. candy canes).
+	colorOverrides = colorOverrides or {}
+	local function colorFor(name)
+		return colorOverrides[name] or ColorPalette.Colors[name] or ColorPalette.getColor(name)
+	end
+	
+	-- Form 1: "all"
+	if spec == "all" then
+		for i, n in ipairs(orderedNames) do
+			table.insert(entries, { colorName = n, color3 = colorFor(n), displayName = nil, index = i })
+		end
+		return entries
+	end
+	
+	if type(spec) ~= "table" then
+		warn(string.format("[RewardBlockPresets] %s.%s: PaletteSubset must be 'all' or a table - skipping variants", setName, blockName))
+		return {}
+	end
+	
+	for _, item in ipairs(spec) do
+		local colorName, displayName
+		if type(item) == "string" then
+			-- Form 2: list of names
+			colorName = item
+		elseif type(item) == "table" and type(item.Color) == "string" then
+			-- Form 3: { Color, Name } table
+			colorName = item.Color
+			displayName = item.Name
+		else
+			warn(string.format("[RewardBlockPresets] %s.%s: PaletteSubset entry has unexpected shape - skipping", setName, blockName))
+			continue
+		end
+		local color3 = colorFor(colorName)
+		if not color3 then
+			warn(string.format("[RewardBlockPresets] %s.%s: PaletteSubset references unknown color '%s' - skipping",
+				setName, blockName, tostring(colorName)))
+			continue
+		end
+		table.insert(entries, {
+			colorName = colorName,
+			color3 = color3,
+			displayName = displayName,
+			index = nameToIndex[colorName] or 999,
+		})
+	end
+	return entries
+end
+
 -- Shallow-merge defaults into a config table without overwriting per-block keys
 local function applyDefaults(blockCfg, defaults)
 	if not defaults then return blockCfg end
@@ -106,12 +170,24 @@ local function applyDefaults(blockCfg, defaults)
 	return merged
 end
 
--- Build a single preset table from a set name, block name, and merged config
-local function buildPreset(setName, blockName, cfg)
-	-- Validate category
-	local category = cfg.Category
-	if not category or not VALID_CATEGORIES[category] then
-		warn(string.format("[RewardBlockPresets] %s.%s: invalid or missing Category '%s' (valid: Nature/Building/Decoration/Colors/Lights/Frames/Wiring/Items) - skipping",
+-- Build a single preset table from a set name, block name, and merged config.
+-- manifestTab: the set-level Tab field (themed-pack tab name), if any. Used as
+-- the fallback Category when no per-block Category is set.
+local function buildPreset(setName, blockName, cfg, manifestTab)
+	-- Resolve category: per-block Category wins; otherwise fall back to the
+	-- set-level Tab field; otherwise we have no destination and must skip.
+	local category = cfg.Category or manifestTab
+	local isThemedPackBlock = (cfg.Category == nil and manifestTab ~= nil)
+	if not category then
+		warn(string.format("[RewardBlockPresets] %s.%s: no Category and no set-level Tab - skipping",
+			setName, blockName))
+		return nil
+	end
+	-- Built-in categories must be in the allow-list. Themed-pack categories
+	-- (where Category == manifestTab) bypass this check since they're declared
+	-- by the manifest itself.
+	if not isThemedPackBlock and not VALID_CATEGORIES[category] then
+		warn(string.format("[RewardBlockPresets] %s.%s: invalid Category '%s' (valid: Nature/Building/Decoration/Colors/Lights/Frames/Wiring/Items) - skipping",
 			setName, blockName, tostring(category)))
 		return nil
 	end
@@ -132,8 +208,21 @@ local function buildPreset(setName, blockName, cfg)
 		-- Tagging for the unlock-gating system
 		IsRewardBlock = true,
 		BelongsToSet = setName,
+		-- True when this block lives in a themed-pack tab (set-level Tab field).
+		-- Used by the inventory to gate tab visibility per-player.
+		IsThemedPackBlock = isThemedPackBlock or nil,
 		-- Behavior config (drives ColorCycleController, placement quirks, etc.)
 		Behavior = cfg.Behavior,
+		-- Decoration support (saplings, flowers, etc. living in reward sets).
+		-- BlockHandler's decoration placement path uses BlockModelFolder to look
+		-- up the template, instead of the hardcoded Decorations folder.
+		IsDecoration = cfg.IsDecoration or nil,
+		DecorationName = cfg.DecorationName or nil,
+		-- Sapling support: IsSaplingItem flags the preset as a sapling for the
+		-- inventory UI; TreeType identifies what tree it grows into (consumed by
+		-- SaplingHandler via its SAPLING_TO_TREE map).
+		IsSaplingItem = cfg.IsSaplingItem or nil,
+		TreeType = cfg.TreeType or nil,
 	}
 	
 	-- Back-compat shim: if the manifest declares ColorCycle behavior, also set
@@ -150,6 +239,17 @@ local function buildPreset(setName, blockName, cfg)
 		Category = true, DisplayName = true, Material = true, MaterialVariant = true,
 		Color = true, DefaultColor = true, Shape = true, SortPriority = true, ApplyColor = true,
 		Behavior = true,
+		-- Set-level fields that shouldn't pass through to per-block presets:
+		Tab = true, TabIcon = true,
+		-- Variant-expansion fields (handled by the caller, not the per-preset row):
+		PaletteSubset = true, NamePattern = true, ColorOverrides = true,
+		-- Decoration / sapling fields (handled inline above; don't pass through
+		-- as random extra preset keys):
+		IsDecoration = true, DecorationName = true,
+		IsSaplingItem = true, TreeType = true,
+		-- StripeColorMode + BaseBlock DO pass through; the block-creation pipeline
+		-- reads StripeColorMode to know how to tint, and BaseBlock identifies the
+		-- unlock group for variant presets.
 	}
 	for k, v in pairs(cfg) do
 		if not KNOWN_FIELDS[k] and preset[k] == nil then
@@ -164,14 +264,17 @@ end
 -- PUBLIC API
 -- ============================================
 
--- Walk RewardSets and return an array of preset tables for every block
+-- Walk RewardSets and return an array of preset tables for every block.
+-- Also returns themed-pack tab info as a second return value:
+--   { { Name = "Candy", IconBlockName = "CandyBricks", SetName = "CandyBlocks" }, ... }
 function RewardBlockPresets.Generate()
 	local results = {}
+	local themedTabs = {}     -- list of { Name, IconBlockName, SetName }
+	local seenThemedTabs = {} -- dedupe by tab name in case two sets share one
 	
 	local setsFolder = ReplicatedStorage:FindFirstChild("RewardSets")
 	if not setsFolder then
-		-- Not an error - folder doesn't exist yet
-		return results
+		return results, themedTabs
 	end
 	
 	local modelsFolder = ReplicatedStorage:FindFirstChild("RewardModels")
@@ -205,6 +308,17 @@ function RewardBlockPresets.Generate()
 		end
 		
 		local defaults = manifest.Defaults
+		local manifestTab = manifest.Tab  -- set-level themed-pack tab name (or nil)
+		
+		-- Record themed-pack tab if this set declares one
+		if manifestTab and not seenThemedTabs[manifestTab] then
+			seenThemedTabs[manifestTab] = true
+			table.insert(themedTabs, {
+				Name = manifestTab,
+				IconBlockName = manifest.TabIcon,  -- may be nil; consumer should fall back
+				SetName = setName,
+			})
+		end
 		
 		-- Generate one preset per block listed in the manifest
 		for blockName, blockCfg in pairs(manifest.Blocks) do
@@ -218,9 +332,56 @@ function RewardBlockPresets.Generate()
 			end
 			
 			local merged = applyDefaults(blockCfg, defaults)
-			local preset = buildPreset(setName, blockName, merged)
-			if preset then
-				table.insert(results, preset)
+			
+			-- PaletteSubset: expand into N variant presets, one per color, the way
+			-- Concrete works. The original (uncolored) preset is suppressed; only
+			-- the variants appear in the inventory.
+			if merged.PaletteSubset then
+				local ColorPalette = require(ReplicatedStorage:WaitForChild("Config"):WaitForChild("ColorPalette"))
+				local entries = resolvePaletteSubset(merged.PaletteSubset, ColorPalette, setName, blockName, merged.ColorOverrides)
+				local namePattern = merged.NamePattern or "{color} {base}"
+				local baseDisplayName = merged.DisplayName or autoSpace(blockName)
+				local basePriority = merged.SortPriority or 500
+				
+				for _, entry in ipairs(entries) do
+					-- Build a per-variant config: variant Id, name, color, and a
+					-- SortPriority that interleaves variants alongside the base sort
+					-- order (base*100 + paletteIndex) so all colors of a block group
+					-- together AND respect the palette's display order.
+					local variantCfg = {}
+					for k, v in pairs(merged) do variantCfg[k] = v end
+					variantCfg.PaletteSubset = nil
+					variantCfg.NamePattern = nil
+					variantCfg.DefaultColor = entry.color3
+					variantCfg.SortPriority = basePriority * 100 + entry.index
+					variantCfg.BaseBlock = blockName  -- unlock check looks at base, not variant
+					-- Per-color display name: custom > pattern > derived
+					if entry.displayName then
+						variantCfg.DisplayName = entry.displayName
+					else
+						local humanColor = (entry.colorName:gsub("(%l)(%u)", "%1 %2"))
+						local name = namePattern:gsub("{color}", humanColor):gsub("{base}", baseDisplayName)
+						variantCfg.DisplayName = name
+					end
+					
+					local variantId = blockName .. "_" .. entry.colorName
+					local preset = buildPreset(setName, variantId, variantCfg, manifestTab)
+					if preset then
+						-- Override BlockModelName: variants share the SAME template, only
+						-- the preset Id and color differ. buildPreset defaulted this to
+						-- variantId (which doesn't exist on disk) ΓÇö fix that.
+						preset.BlockModelName = blockName
+						preset.IsVariant = true
+						preset.VariantColorName = entry.colorName
+						table.insert(results, preset)
+					end
+				end
+			else
+				-- Single preset, no expansion
+				local preset = buildPreset(setName, blockName, merged, manifestTab)
+				if preset then
+					table.insert(results, preset)
+				end
 			end
 		end
 		
@@ -233,7 +394,7 @@ function RewardBlockPresets.Generate()
 		end
 	end
 	
-	return results
+	return results, themedTabs
 end
 
 return RewardBlockPresets
diff --git a/public/src/Shared/RewardModels/CandyBlocks/CandyBricks/init.meta.json b/public/src/Shared/RewardModels/CandyBlocks/CandyBricks/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardModels/CandyBlocks/CandyBricks/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardModels/CandyBlocks/ChocolateBlock/init.meta.json b/public/src/Shared/RewardModels/CandyBlocks/ChocolateBlock/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardModels/CandyBlocks/ChocolateBlock/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardModels/CandyBlocks/Union/init.meta.json b/public/src/Shared/RewardModels/CandyBlocks/Union/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardModels/CandyBlocks/Union/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardModels/CandyBlocks/init.meta.json b/public/src/Shared/RewardModels/CandyBlocks/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardModels/CandyBlocks/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyBricks/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyBricks/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyBricks/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneFull/Texture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneFull/Texture/init.meta.json
new file mode 100644
index 0000000..5f5911f
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneFull/Texture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Texture"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneFull/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneFull/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneFull/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Bark/Texture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Bark/Texture/init.meta.json
new file mode 100644
index 0000000..5f5911f
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Bark/Texture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Texture"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Bark/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Bark/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Bark/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Core/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Core/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/Core/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneLog/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/Background/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/Background/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/Background/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/Stripes/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/Stripes/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/Stripes/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyCaneSapling/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/CandyLatticeTexture/Texture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/CandyLatticeTexture/Texture/init.meta.json
new file mode 100644
index 0000000..5f5911f
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/CandyLatticeTexture/Texture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Texture"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/CandyLatticeTexture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/CandyLatticeTexture/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/CandyLatticeTexture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/LatticeStructure/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/LatticeStructure/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/LatticeStructure/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLattice/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLattice/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLattice/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLattice/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLatticeTexture/Texture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLatticeTexture/Texture/init.meta.json
new file mode 100644
index 0000000..5f5911f
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLatticeTexture/Texture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Texture"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLatticeTexture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLatticeTexture/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/CandyLatticeTexture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/Model/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCorner/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/CandyLatticeTexture/Texture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/CandyLatticeTexture/Texture/init.meta.json
new file mode 100644
index 0000000..5f5911f
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/CandyLatticeTexture/Texture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Texture"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/CandyLatticeTexture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/CandyLatticeTexture/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/CandyLatticeTexture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/LatticeStructure/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/LatticeStructure/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/LatticeStructure/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/CandyLattice/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeCross/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/CandyLatticeTexture/Texture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/CandyLatticeTexture/Texture/init.meta.json
new file mode 100644
index 0000000..5f5911f
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/CandyLatticeTexture/Texture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Texture"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/CandyLatticeTexture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/CandyLatticeTexture/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/CandyLatticeTexture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/LatticeStructure/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/LatticeStructure/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/LatticeStructure/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLattice/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLatticeTexture/Texture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLatticeTexture/Texture/init.meta.json
new file mode 100644
index 0000000..5f5911f
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLatticeTexture/Texture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Texture"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLatticeTexture/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLatticeTexture/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/CandyLatticeTexture/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/Union/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/Union/init.meta.json
new file mode 100644
index 0000000..ffa2b07
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/Union/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "UnionOperation"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/init.meta.json
new file mode 100644
index 0000000..d54c6d1
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/CandyLatticeT/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Model"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/ChocolateBlock/init.meta.json b/public/src/Shared/RewardSets/CandyBlocks/ChocolateBlock/init.meta.json
new file mode 100644
index 0000000..c7dea6d
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/ChocolateBlock/init.meta.json
@@ -0,0 +1,3 @@
+{
+  "className": "Part"
+}
diff --git a/public/src/Shared/RewardSets/CandyBlocks/Manifest.luau b/public/src/Shared/RewardSets/CandyBlocks/Manifest.luau
new file mode 100644
index 0000000..e86d19c
--- /dev/null
+++ b/public/src/Shared/RewardSets/CandyBlocks/Manifest.luau
@@ -0,0 +1,177 @@
+-- ReplicatedStorage/RewardSets/CandyBlocks/Manifest
+-- Candy reward set. Rare-tier daily reward set.
+--
+-- Build order (see chat plan):
+--   Step 1 (DONE): Manifest + 3 plain blocks below.
+--   Step 2 (DONE): CandyCaneLog registered ΓÇö log rotation is automatic because
+--                  the name contains "Log" (see LogPlacement.IsLog).
+--   Step 3 (DONE): ChocolateBlock + CandyCaneLog + CandyCaneFull expanded into
+--                  per-color variants via PaletteSubset (see field docs below).
+--   Step 4: CandyLattice as a glass-style pane (needs corner geometry first).
+--   Step 5 (DONE): CandyCaneSapling ΓÇö placed like a regular sapling, grows after
+--                  10s into a J-shape candy cane with random height/curl/color.
+
+-- Bright candy-flavor colors. These intentionally override the shared
+-- ColorPalette (whose reds/greens are muted) so placed + inventory candy blocks
+-- match the vivid colors used by grown candy canes. MUST stay in sync with
+-- CANDY_PALETTE in Server/SaplingHandler.server.luau.
+local candyColors = {
+	White  = Color3.fromRGB(255, 255, 255),
+	Red    = Color3.fromRGB(240, 30, 50),
+	Green  = Color3.fromRGB(50, 200, 70),
+	Pink   = Color3.fromRGB(255, 110, 185),
+	Purple = Color3.fromRGB(165, 70, 230),
+	Orange = Color3.fromRGB(255, 130, 25),
+	Yellow = Color3.fromRGB(255, 220, 40),
+}
+
+-- Richer, warmer chocolate tones. The shared ColorPalette's browns read as
+-- washed-out grey-browns; these are more saturated cocoa colors so the
+-- chocolate blocks actually look like chocolate. Keyed by the palette color
+-- name each variant uses (see ChocolateBlock.PaletteSubset below).
+local chocolateColors = {
+	White    = Color3.fromRGB(245, 224, 180), -- creamy white chocolate (warmer than pure white)
+	Tan      = Color3.fromRGB(165, 100, 52),  -- milk chocolate
+	Brown    = Color3.fromRGB(96, 52, 26),    -- dark chocolate
+	DarkGrey = Color3.fromRGB(54, 30, 18),    -- extra dark chocolate
+	Pink     = Color3.fromRGB(225, 120, 120), -- strawberry chocolate
+}
+
+return {
+	-- Set-wide rarity. Drives the weighted random pick in RewardSetPicker.
+	-- Common = weight 10, Uncommon = 5, Rare = 2.
+	Rarity = "Rare",
+
+	-- This set gets its own dedicated tab in the inventory (themed-pack tab),
+	-- rendered on the right side under Tools. The tab is HIDDEN until the player
+	-- has unlocked at least one block from this set (or owns the pack, once shop
+	-- packs exist). The owner sees it always. Toggle with /preview to test as a
+	-- fresh player.
+	Tab = "Candy",
+	TabIcon = "CandyBricks",  -- block model name shown in the tab's viewport
+
+	-- Defaults merged into every block below unless that block overrides the key.
+	Defaults = {
+		-- NOTE: Category is NOT defaulted here because the Tab field above routes
+		-- all blocks to the "Candy" tab automatically. If we later want a single
+		-- candy block to live in a built-in tab (e.g. Decoration), give that
+		-- block an explicit Category and it'll override the Tab.
+		ApplyColor = false,     -- plain blocks keep their authored textures (no tint)
+		-- Material intentionally NOT defaulted: each template keeps the Material /
+		-- MaterialVariant it was authored with in Studio.
+	},
+
+	Blocks = {
+		-- PaletteSubset (optional, set per-block below): expands one block template
+		-- into multiple inventory entries, one per color, the way Concrete works.
+		-- Three forms:
+		--   "all"                                  -> all 17 palette colors
+		--   { "Red", "Blue", ... }                 -> just those colors
+		--   { { Color="Red", Name="Cherry" }, ... } -> custom display name per color
+		-- The original (uncolored) preset is suppressed from the inventory when a
+		-- PaletteSubset is set ΓÇö only the variants appear, matching how concrete works.
+		-- StripeColorMode (optional):
+		--   "Part"    -> tint the block's BasePart Color3 (default; works for solid blocks)
+		--   "Texture" -> tint every Texture descendant's Color3 (for blocks whose
+		--                pattern lives in grayscale Texture images, like candy canes)
+
+		ChocolateBlock = {
+			DisplayName = "Chocolate Block",
+			SortPriority = 60,
+			BaseBlock = "ChocolateBlock",
+			ApplyColor = true,  -- override the set-wide false (variants need coloring)
+			StripeColorMode = "Part",  -- explicit for clarity (this is the default)
+			PaletteSubset = {
+				{ Color = "White",    Name = "White Chocolate" },
+				{ Color = "Tan",      Name = "Milk Chocolate" },
+				{ Color = "Brown",    Name = "Dark Chocolate" },
+				{ Color = "DarkGrey", Name = "Extra Dark Chocolate" },
+				{ Color = "Pink",     Name = "Strawberry Chocolate" },
+			},
+			-- Use richer cocoa tones instead of the muted shared-palette browns.
+			ColorOverrides = chocolateColors,
+		},
+
+		CandyBricks = {
+			DisplayName = "Candy Bricks",
+			-- 6100 is between chocolate variants (60xx) and candy cane variants (62xx, 63xx)
+			-- because variant blocks get SortPriority = base * 100 + paletteIndex.
+			SortPriority = 6100,
+			BaseBlock = "CandyBricks",
+			-- No PaletteSubset: stays a single fixed-multicolor block.
+		},
+
+		-- Solid candy-cane cube. Player-placeable, and ALSO reused as the corner
+		-- piece when the sapling grows (its faces have no log seam, unlike the log).
+		-- 17 color variants ΓÇö the stripe texture is grayscale, so tinting it produces
+		-- a candy cane in whatever flavor color the player picks.
+		CandyCaneFull = {
+			DisplayName = "Candy Cane Block",
+			SortPriority = 62,
+			BaseBlock = "CandyCaneFull",
+			ApplyColor = true,
+			StripeColorMode = "Texture",  -- tint Texture descendants, not the Part
+			-- Curated to colors that actually look like real candy canes. Skips
+			-- things like Cyan/Magenta/Lime which read more arts-and-crafts than candy.
+			PaletteSubset = { "White", "Red", "Green", "Pink", "Purple", "Orange", "Yellow" },
+			-- Use the bright candy colors instead of the muted shared palette so these
+			-- match grown candy canes.
+			ColorOverrides = candyColors,
+		},
+
+		-- Candy-cane log (Step 2). The name contains "Log", so LogPlacement.IsLog
+		-- auto-detects it and the building code rotates it like Oak/Birch/etc.:
+		-- vertical when placed on top/bottom, horizontal when placed on a side.
+		-- Used as the straight segments of the sapling's grown structure (Step 5);
+		-- CandyCaneFull fills in at the two bends so there's no log seam at corners.
+		-- 17 color variants, same tint mechanism as CandyCaneFull.
+		CandyCaneLog = {
+			DisplayName = "Candy Cane Log",
+			SortPriority = 63,
+			BaseBlock = "CandyCaneLog",
+			ApplyColor = true,
+			StripeColorMode = "Texture",
+			-- Same curated set as CandyCaneFull ΓÇö they're meant to pair.
+			PaletteSubset = { "White", "Red", "Green", "Pink", "Purple", "Orange", "Yellow" },
+			ColorOverrides = candyColors,
+		},
+
+		-- Sapling (Step 5). Placed like any sapling ΓÇö grows into a J-shaped candy
+		-- cane after the standard growth delay (10s). Random height (3ΓÇô8), random
+		-- curl direction (L/R), random color from the 7 candy-cane flavors. All
+		-- saplings look identical (all-white) regardless of what they grow into.
+		-- The block is a Decoration shape, so placement-wise it behaves like
+		-- OakSapling etc. (top-of-grass/dirt placement, decoration billboard).
+		-- Template lives in RewardSets/CandyBlocks/CandyCaneSapling ΓÇö BlockHandler's
+		-- decoration path honors the preset's BlockModelFolder so it'll find it
+		-- there instead of the usual Decorations folder.
+		CandyCaneSapling = {
+			DisplayName = "Candy Cane Sapling",
+			SortPriority = 64,
+			BaseBlock = "CandyCaneSapling",
+			Shape = "Decoration",
+			IsDecoration = true,
+			IsSaplingItem = true,
+			TreeType = "CandyCane",
+			-- DecorationName + BlockModelFolder (set by RewardBlockPresets) route the
+			-- placement template lookup to RewardSets/CandyBlocks/CandyCaneSapling
+			-- rather than the hardcoded Decorations folder.
+			DecorationName = "CandyCaneSapling",
+		},
+
+		-- Glass-style lattice pane (Step 4). Routed through the glass placement system
+		-- via IsGlass = true: it auto-connects into corner/tee/cross pieces exactly like
+		-- glass panes do, pulling its four models (CandyLattice / CandyLatticeCorner /
+		-- CandyLatticeT / CandyLatticeCross) from this RewardSets/CandyBlocks folder.
+		-- GlassPlacement.GLASS_TYPE_CONFIG maps the type -> folder + model names; the
+		-- BlockHandler/SaveManager glass paths read preset.BlockModelFolder (set
+		-- automatically to "RewardSets/CandyBlocks") instead of the hardcoded glass folder.
+		-- Center pivot, fixed (non-tintable) brown-in-texture look, so no ApplyColor.
+		CandyLattice = {
+			DisplayName = "Candy Lattice",
+			SortPriority = 61,
+			BaseBlock = "CandyLattice",
+			IsGlass = true,
+		},
+	},
+}
diff --git a/public/src/Shared/WallpaperPlacement.luau b/public/src/Shared/WallpaperPlacement.luau
index ebce122..40ffddd 100644
--- a/public/src/Shared/WallpaperPlacement.luau
+++ b/public/src/Shared/WallpaperPlacement.luau
@@ -86,16 +86,19 @@ function WallpaperPlacement.IsValidHost(hitBlock, face)
 			end
 		end
 		
-		-- Map stair direction to the back wall face name
-		-- Direction 0 (NORTH, open +Z): back wall = "North" (-Z face)
-		-- Direction 1 (EAST, open +X): back wall = "West" (-X face)
-		-- Direction 2 (SOUTH, open -Z): back wall = "South" (+Z face)
-		-- Direction 3 (WEST, open -X): back wall = "East" (+X face)
+		-- Map stair direction to the back wall (tall flat solid side, opposite the
+		-- stepped/open side). Verified against live StairDirection values: e.g. dir 2
+		-- has its solid back on the -Z (North) face. The previous table was 180 deg
+		-- off, which silently rejected the real back wall on every orientation.
+		-- Dir 0: back wall = "South" (+Z)
+		-- Dir 1: back wall = "East"  (+X)
+		-- Dir 2: back wall = "North" (-Z)
+		-- Dir 3: back wall = "West"  (-X)
 		local backWallFaces = {
-			[0] = "North",
-			[1] = "West",
-			[2] = "South",
-			[3] = "East",
+			[0] = "South",
+			[1] = "East",
+			[2] = "North",
+			[3] = "West",
 		}
 		local backFace = backWallFaces[stairDir]
 		return face == backFace
@@ -185,8 +188,20 @@ end
 function WallpaperPlacement.GetPlacementInfo(hitBlock, hitPosition, hitNormal, hitBlockPosition)
 	local face = WallpaperPlacement.GetFaceFromNormal(hitNormal)
 	
+	-- [BB-WPDEBUG] TEMP diagnostic for wallpaper-on-stair placement. Remove after fix.
+	local _valid = WallpaperPlacement.IsValidHost(hitBlock, face)
+	print(string.format("[BB-WPDEBUG] host=%s type=%s shape=%s isStairAttr=%s stairDir=%s rot=%s normal=(%.3f,%.3f,%.3f) face=%s valid=%s",
+		hitBlock and hitBlock.Name or "nil",
+		tostring(hitBlock and hitBlock:GetAttribute("BlockType")),
+		tostring(hitBlock and hitBlock:GetAttribute("Shape")),
+		tostring(hitBlock and hitBlock:GetAttribute("IsStair")),
+		tostring(hitBlock and hitBlock:GetAttribute("StairDirection")),
+		tostring(hitBlock and hitBlock:GetAttribute("Rotation")),
+		hitNormal.X, hitNormal.Y, hitNormal.Z,
+		tostring(face), tostring(_valid)))
+	
 	-- Validate the host block
-	if not WallpaperPlacement.IsValidHost(hitBlock, face) then
+	if not _valid then
 		return nil
 	end
 	
